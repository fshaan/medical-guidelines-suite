---
phase: 03-pipeline-run-subcommand-interface-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline.py
autonomous: true
requirements:
  - PIP-01
  - PIP-02
  - PIP-03
  - PIP-04
  - PIP-05
  - PIP-06
must_haves:
  truths:
    - "调用 run_pipeline(args) 后,每个 patient 的最终状态都落到 Output/patients/<pid>.json 或 Output/_failed/<pid>.json 中,绝不丢失"
    - "单 patient 的 transport / schema / build / cancellation 异常被分级捕获,不击垮 sibling patients"
    - "--resume 模式扫到 patients/<pid>.json 跳过该 patient;扫到 _failed/<pid>.json 删旧 _failed 文件后重跑"
    - "退出码:全部写到 patients/(含 partial) → 0;任一进 _failed/ → 1"
    - "run 末尾输出 Output/rag_results.json 是 patients/ + _failed/ 的派生 aggregate"
  artifacts:
    - path: "scripts/pipeline.py"
      provides: "run_pipeline + _run_one_patient + compute_citation_coverage + build_patient_prompt + _atomic_write_json + _merge_rag_results + _scan_resume + _write_failed + _dedupe_hits + _load_patients + _load_kb_metadata"
      min_lines: 250
      exports:
        - "run_pipeline"
        - "_run_one_patient"
        - "compute_citation_coverage"
        - "build_patient_prompt"
        - "_atomic_write_json"
        - "_merge_rag_results"
  key_links:
    - from: "scripts/pipeline.py:run_pipeline"
      to: "scripts/retriever.py:AsyncQMDService"
      via: "AsyncQMDService(http_client=http, semaphore=qmd_sem) 注入式构造"
      pattern: "AsyncQMDService\\(\\s*http_client="
    - from: "scripts/pipeline.py:run_pipeline"
      to: "scripts/llm_client.py:AsyncLLMClient"
      via: "AsyncLLMClient(profile, http=http, semaphore=...) 共享 httpx.AsyncClient"
      pattern: "AsyncLLMClient\\(\\s*profile"
    - from: "scripts/pipeline.py:_run_one_patient"
      to: "scripts/llm_client.py:complete_structured_with_feedback"
      via: "传入 feedback_check=compute_citation_coverage 与 threshold=0.5"
      pattern: "complete_structured_with_feedback"
    - from: "scripts/pipeline.py:_run_one_patient"
      to: "scripts/kb_metadata.py:filter_chunks_by_disease / filter_orgs_by_disease / normalize_disease / load_synonym_map"
      via: "双层病种过滤 + canonical 归一化"
      pattern: "filter_chunks_by_disease|filter_orgs_by_disease|normalize_disease"
---

<objective>
新增 `scripts/pipeline.py` 模块,实现按患者并发的总编排器:顶层 `asyncio.gather` + 患者级 `Semaphore(N)`,共享 `httpx.AsyncClient` 注入到 `AsyncQMDService` 与 `AsyncLLMClient`。覆盖 6 条 PIP 要求(PIP-01..06)。

Purpose: 这是 v3.1 milestone 的 ship gate——把"LLM 人工 50min + QMD 串行 3min"路径替换为「单 patient 全自动 + N 路并发」流水线,10 例 wall time <10min。

Output: `scripts/pipeline.py` 单文件(预估 ~280 LOC),供 Plan 03-02 在 `batch_pipeline.py:main` 中通过 `subparsers.add_parser("run", ...)` 暴露给 CLI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-SUMMARY.md
@.planning/phases/02-llm-client-schema-unit-tests/02-SUMMARY.md
@scripts/retriever.py
@scripts/llm_client.py
@scripts/kb_metadata.py
@scripts/batch_pipeline.py
@CLAUDE.md

<interfaces>
<!-- Phase 1/2 已落地、Plan 03-01 必须直接调用的契约。 -->

From `scripts/retriever.py:AsyncQMDService` (Phase 1 落地):
```python
class AsyncQMDService:
    def __init__(
        self,
        port: Optional[int] = None,
        *,
        timeout_s: float = 60.0,
        semaphore: Optional[asyncio.Semaphore] = None,
        http_client: Optional[httpx.AsyncClient] = None,
    ): ...
    async def __aenter__(self) -> "AsyncQMDService": ...
    async def __aexit__(self, exc_type, exc_val, exc_tb) -> None: ...
    async def query(self, text: str, top_k: int = 10, *, filter_paths=None) -> list[dict]: ...
    async def search(self, text: str, top_k: int = 10) -> list[dict]: ...
```
注入 `http_client` 时 `_owns_http=False`,`__aexit__` 不关闭 http(调用方负责)。

From `scripts/llm_client.py` (Phase 2 落地):
```python
class LLMFailure(Exception):
    def __init__(self, patient_id, last_error: Exception, stage: str): ...
    # stage in {"transport","schema","feedback"}

class LLMProfile:
    name: str; base_url: str; model: str
    api_key_env: str; timeout_s: int; structured_mode: str
    concurrency: int; temperature: float; max_tokens: int
    @classmethod
    def from_env(cls, name: Optional[str] = None, yaml_path: Optional[Path] = None) -> "LLMProfile": ...

class AsyncLLMClient:
    def __init__(self, profile: LLMProfile, http: httpx.AsyncClient, *, semaphore=None): ...
    async def complete_structured(self, messages, schema, *, schema_name="patient_recommendation", patient_id=None) -> dict: ...
    async def complete_structured_with_feedback(
        self, messages, schema, *,
        schema_name="patient_recommendation",
        feedback_check: Callable[[dict], float],
        threshold: float = 0.5,
        feedback_template: Optional[str] = None,
        patient_id: Optional[str] = None,
    ) -> tuple[dict, float, str]:  # (result, score, status="ok"|"partial")
        ...

PATIENT_RECOMMENDATION_SCHEMA: dict  # 顶层导出常量
```

From `scripts/kb_metadata.py` (Phase 1 落地):
```python
def load_synonym_map(kb_root: Path) -> dict[str, list[str]]: ...
def normalize_disease(disease_type: str, synonym_map: dict) -> Optional[str]: ...
def filter_orgs_by_disease(coverage: dict, canonical_key: Optional[str]) -> list[str]: ...
def filter_chunks_by_disease(hits: list[dict], chunks_meta: dict, canonical_key: Optional[str]) -> list[dict]: ...
```

From `scripts/batch_pipeline.py` (Phase 3 复用,**只读**调用):
```python
def extract_patient_features(patient: dict) -> dict   # batch_pipeline.py:558
def build_queries(patient: dict, features: dict) -> list[str]   # batch_pipeline.py:436
```
这两个函数仍在 `batch_pipeline.py`,Plan 03-01 必须从那里 import,**不重写**。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline.py 工具层 + build_patient_prompt + compute_citation_coverage + _atomic_write_json + _merge_rag_results + _scan_resume + _write_failed</name>
  <files>scripts/pipeline.py, tests/test_pipeline_helpers.py</files>
  <read_first>
    <!-- 必读,按读取顺序 -->
    - `.planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md` §D-08, §D-09, §D-14, §D-15, §specifics(Output schema 三个示例)
    - `.planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md` §4 _atomic_write_json, §5 build_patient_prompt, §6 compute_citation_coverage, §7 _merge_rag_results
    - `scripts/kb_metadata.py:236-247` (`_atomic_write_json` 实现模板,字节复制)
    - `scripts/batch_pipeline.py:743-887` (`generate_batch_prompt` 迁移源——只读,不修改)
    - `scripts/batch_pipeline.py:1376-1463` (`cmd_merge` 输出 schema 参考)
    - `CLAUDE.md` (Python 3.9.6 禁用 list)
  </read_first>
  <behavior>
    - **compute_citation_coverage(result: dict) -> float**
      - 扫 `result["guideline_results"][*]["recommendation"]` 文本中的 `[n]` (正则 `\[(\d+)\]`)
      - 收集 unique 编号集合,除以 `sum(len(gr["retrieval_sources"]) for gr in guideline_results)`
      - 边界:`total_sources == 0` → 返回 `1.0`;`guideline_results` 缺失或空 → 返回 `0.0`
      - 测试 case T1: 2 个 guideline_results × 2 sources,recommendation 引用 `[1] [2]` → coverage = 0.5(2 unique / 4 sources)
      - 测试 case T2: total_sources=0 → 1.0
      - 测试 case T3: recommendation 全无 `[n]` 但 total_sources=4 → 0.0
      - 测试 case T4: result 缺 `guideline_results` key → 0.0
    - **build_patient_prompt(patient: dict, retrieval_hits: list) -> list[dict]**
      - 返回 `[{"role":"system","content":<persona>}, {"role":"user","content":<patient + hits>}]`
      - system 内容固定文本(见 §D-14 + PATTERNS §5):medical persona + "至少引用 2 个 retrieval_sources 编号 [n]" + "全部 user-facing 文本使用简体中文"
      - user 文本:`## 患者 <name> (<id>)\n` + 患者字段逐行(跳过 `features` / `retrieval_results` / None 值) + `\n## 检索结果\n` + 检索 hits 编号 `[i] <path> (score=<x>)\n<content>\n`
      - 测试 case T5: patient {id="p001", name="贾常山", disease_type="结直肠癌"} + 2 hits → 返回 list 长度 2,messages[0]["role"]=="system",messages[1]["content"] 含 "[1]" 与 "[2]" 与 "贾常山"
      - 测试 case T6: 空 retrieval_hits → user content 含 "## 检索结果" 但无 "[" 编号行
    - **_atomic_write_json(path: Path, data: dict) -> None**
      - 字节复制 `kb_metadata.py:236-247` 实现(tmp + Path.replace)
      - tmp 路径 = `path.with_suffix(path.suffix + ".tmp")`,写完 `tmp.replace(path)`
      - JSON dump:`indent=2, sort_keys=True, ensure_ascii=False`,末尾换行
      - 测试 case T7: 写入临时 tmp_path,断言文件存在 + JSON 解析回来 == 原 dict + `.tmp` 文件不残留
      - 测试 case T8: 父目录不存在 → 抛 FileNotFoundError(不静默创建)
    - **_write_failed(output_dir: Path, patient_id: str, error: str, stage: str, last_llm_output: Optional[str]) -> None**
      - 落到 `output_dir / "_failed" / f"{patient_id}.json"`
      - 内容 schema:`{"patient_id","error","stage","last_llm_output","attempted_at"}`(attempted_at = `datetime.utcnow().isoformat() + "Z"`)
      - 用 `_atomic_write_json` 落盘
      - 测试 case T9: 调用后 `_failed/p005.json` 存在 + 含全部 5 字段
    - **_merge_rag_results(output_dir: Path, wall_time_s: float) -> dict**
      - 扫 `output_dir/"patients"/*.json` 与 `output_dir/"_failed"/*.json`(sorted)
      - 返回 `{"patients":[...],"failures":[...],"summary":{"total","ok","partial","failed","wall_time_s"}}`,字段语义见 PATTERNS §7
      - 每个 patients 项 `{"id","status","citation_coverage","result"}`
      - 每个 failures 项 `{"id","error","stage"}`
      - 测试 case T10: 预放 2 ok + 1 partial + 1 failed → summary={total:4, ok:2, partial:1, failed:1}
    - **_scan_resume(patients: list[dict], output_dir: Path, resume: bool) -> list[dict]**
      - `resume=False` → 返回原 patients 列表不动
      - `resume=True`:
        - 若 `patients/<pid>.json` 存在 → 跳过(从 to_run 移除)
        - 若 `_failed/<pid>.json` 存在 → 加入 to_run **并删除旧的 `_failed/<pid>.json` 文件**(本次重跑可能成功,旧 fail 必须清掉)
        - 否则 → 加入 to_run
      - 测试 case T11: 3 patients [p1,p2,p3];预放 `patients/p1.json` + `_failed/p2.json`,resume=True → 返回 [p2,p3],且 `_failed/p2.json` 已删除
      - 测试 case T12: resume=False → 返回 [p1,p2,p3] 原序
    - **_dedupe_hits(hits: list[dict]) -> list[dict]**
      - 去重 key = `hit.get("path", "")` + `hit.get("content","")[:50]`(path + content 头 50 char,防同 path 不同段)
      - 保留首次出现,O(n)
      - 测试 case T13: 4 hits 含 2 个 path 完全相同 + content 一样 → 返回 3 个
    - **_load_patients(path: Path) -> list[dict]**
      - 读 `patients.json`,返回顶层 `data["patients"]`(若是 list 顶层则直接返回)
      - 测试 case T14: 输入 `{"patients":[{"patient_id":"p1"}]}` → 返回 `[{"patient_id":"p1"}]`
    - **_load_kb_metadata(kb_root: Path) -> tuple[dict, dict]**
      - 读 `<kb_root>/.metadata/chunks.json` 与 `<kb_root>/.metadata/org_disease_coverage.json`,任一缺失返回 `({}, {})`(degrade-friendly:KB 未 index 时仍能跑,只是双层过滤失效)
      - 测试 case T15: 临时 KB root 无 .metadata/ → 返回 `({}, {})` 不抛错
    - **_hit_org(hit: dict) -> str**
      - 从 `hit["path"]`(形如 `qmd://nccn/...` 或 `/path/to/NCCN/extracted/x.md`)提取 org 名(uppercase)
      - 启发式:split "/" 后取倒数第 3 段(若有 `extracted` 子目录) 或前缀 `qmd://<org>/...` 的第二段
      - 测试 case T16: `qmd://nccn/foo.md` → "NCCN";`qmd://esmo/x.md` → "ESMO";`/x/NCCN/extracted/y.md` → "NCCN";无法提取 → ""
  </behavior>
  <action>
    创建 `scripts/pipeline.py`,顶部按 PATTERNS §1 写文档字符串、`from __future__ import annotations`、imports(argparse / asyncio / json / os / re / sys / time / datetime + pathlib + typing.Optional + httpx + Phase 1/2 三个模块所有必需名 + `from scripts.batch_pipeline import extract_patient_features, build_queries`)。

    按 §behavior 的 11 个函数实现以下(顺序无所谓,但 `_atomic_write_json` 必须先实现,被其它函数调用):
    1. `_CITATION_RE = re.compile(r"\[(\d+)\]")` 模块级
    2. `_atomic_write_json` —— 字节复制 `kb_metadata.py:236-247`(D-09 + PATTERNS §4)
    3. `compute_citation_coverage` —— 按 §D-15 算法
    4. `build_patient_prompt` —— 按 §D-14 + PATTERNS §5 模板(system 文本严格遵循上面 §behavior 给出的 5 条要求,user 文本按"## 患者 ... ## 检索结果 ..."双段)
    5. `_write_failed` —— 落 `_failed/<pid>.json` 含 5 字段
    6. `_load_patients` / `_load_kb_metadata` / `_hit_org` / `_dedupe_hits` —— 短工具函数
    7. `_scan_resume` —— 实现 D-08 两段扫描(`_failed/<pid>.json` 命中要 `unlink()` 旧文件)
    8. `_merge_rag_results` —— 按 PATTERNS §7 字面实现(注意 sorted glob,patient_id 不缺失)

    所有函数类型注解用 `Optional[X]` / `List[X]` / `Dict[X,Y]`,**禁用 `X | None`**(WR-06 立约,Phase 3 沿用)。

    创建 `tests/test_pipeline_helpers.py`,16 个 case 全部用 pytest 同步函数(不需要 asyncio)实现,T7/T9/T10/T11/T15 用 `tmp_path` fixture。

    Task 1 **不**实现 `run_pipeline` 与 `_run_one_patient`(留 Task 2)。
  </action>
  <verify>
    <automated>cd /Users/f.sh/Workspace/devs/medical-guidelines-suite && python3 -m pytest tests/test_pipeline_helpers.py -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pipeline.py` 存在且 `python3 -c "from scripts.pipeline import compute_citation_coverage, build_patient_prompt, _atomic_write_json, _merge_rag_results, _scan_resume, _write_failed, _dedupe_hits, _load_patients, _load_kb_metadata, _hit_org"` 无 ImportError
    - `tests/test_pipeline_helpers.py` 16 个 case 全部 PASS
    - `grep -nE 'X \| Y|: list\[|: dict\[' scripts/pipeline.py | grep -v '^#'` 输出为空(无 PEP 604 / runtime-evaluated 泛型)
    - `grep -nE 'TaskGroup|asyncio\.timeout|except\*' scripts/pipeline.py | grep -v '^#'` 输出为空
    - `_atomic_write_json` 实现使用 `tmp.replace(path)`(grep `\.replace\(path\)` 命中)
    - `compute_citation_coverage` 用 `_CITATION_RE.finditer`(grep `_CITATION_RE\.finditer` 命中 1 次)
    - `_scan_resume` 在 `_failed/<pid>.json` 命中时调用 `.unlink()`(grep `\.unlink\(\)` 在 `_scan_resume` 函数体内命中)
    - build_patient_prompt 返回的 system content 含 "简体中文" 字样(测试断言)
  </acceptance_criteria>
  <done>
    `scripts/pipeline.py` 工具层 10 个函数全部就位;16 case 单元测试全绿;PEP 604 / TaskGroup 等 Python 3.9.6 禁忌 zero 出现。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_pipeline 顶层编排 + _run_one_patient 单患者子流水线 + 异常分级落盘 + 进度行</name>
  <files>scripts/pipeline.py, tests/test_pipeline.py, tests/fixtures/mock_qmd_hit.json, tests/fixtures/mock_patient.json</files>
  <read_first>
    <!-- 必读,按读取顺序 -->
    - `.planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md` §D-01..D-07, §D-16(进度行格式)
    - `.planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md` §2 (run_pipeline 骨架), §3 (_run_one_patient stages), §Pattern A/B/F (注入式 + gather + 分级 except), §Anti-Patterns(Python 3.9.6)
    - `scripts/retriever.py:222-289` (AsyncQMDService 注入接口) + `:382-410` (query 签名)
    - `scripts/llm_client.py:214-355` (AsyncLLMClient + complete_structured_with_feedback)
    - `scripts/batch_pipeline.py:436-475:build_queries` + `:558-685:extract_patient_features` (Stage 1+2 input 形态)
    - `tests/test_retriever_async.py` (mock httpx 双桩路由参考)
    - `tests/test_llm_client.py` (in_flight peak counter / AsyncMock patterns)
    - `tests/fixtures/patient_001.json` + `tests/fixtures/mock_llm_response.json` (现有 fixture 形态)
    - Plan 03-01 Task 1 落地的 `scripts/pipeline.py` 当前内容
  </read_first>
  <behavior>
    - **run_pipeline(args: argparse.Namespace) -> int** —— PIP-01 + PIP-06
      - 用 `LLMProfile.from_env(name=args.llm_profile)` 读 profile
      - `_load_patients(args.patients)` → list
      - `output_dir = Path(args.output_dir).resolve()`,`mkdir(parents=True, exist_ok=True)` 两个子目录 `patients/` 与 `_failed/`
      - `to_run = _scan_resume(patients, output_dir, resume=args.resume)`
      - `synonym_map = load_synonym_map(Path(args.kb_root or os.environ.get("MEDICAL_GUIDELINES_DIR",".")))`
      - `chunks_meta, coverage = _load_kb_metadata(...)`
      - 顶层 `async with httpx.AsyncClient(timeout=httpx.Timeout(profile.timeout_s)) as http:`
      - 三个独立 Semaphore:`pat_sem = asyncio.Semaphore(args.concurrency_patients)` / `qmd_sem = asyncio.Semaphore(args.concurrency_qmd)` / `llm_sem = asyncio.Semaphore(profile.concurrency)` —— D-04 三 sem **互不嵌套**
      - `async with AsyncQMDService(http_client=http, semaphore=qmd_sem) as qmd:`(注入式,_owns_http=False)
      - `llm = AsyncLLMClient(profile, http=http, semaphore=llm_sem)`
      - 顶层 `try: ... finally:` 包住 gather,**finally 块中**计算 wall_time + 调 `_merge_rag_results` + `_atomic_write_json(output_dir/"rag_results.json", aggregate)` —— D-07:Ctrl-C 也要尽力写 rag_results.json
      - `await asyncio.gather(*[_run_one_patient(p, ...) for p in to_run], return_exceptions=True)`
      - 返回:`0 if aggregate["summary"]["failed"] == 0 else 1`(D-06)
      - 进度行 summary 末尾打印一行:`Total: N  OK: x  Partial: y  Failed: z  Wall: <s>s  Exit: <code>` flush
    - **_run_one_patient(patient, *, qmd, llm, output_dir, pat_sem, synonym_map, chunks_meta, coverage) -> None** —— PIP-02 + PIP-03 + PIP-05
      - 入口 `async with pat_sem:` 限流(D-04)
      - 6 个 stages 顺序(PATTERNS §3):extract_features → build_queries → normalize_disease → `await asyncio.gather(*[qmd.query(q) for q in queries])` flatten → `_dedupe_hits` → `filter_orgs_by_disease`(过滤 `_hit_org(h) in allowed_orgs`)→ `filter_chunks_by_disease` → `build_patient_prompt` → `complete_structured_with_feedback(messages, PATIENT_RECOMMENDATION_SCHEMA, feedback_check=compute_citation_coverage, threshold=0.5, patient_id=pid)` → 写 shard
      - 成功(返回三元组 `(result, score, status)`)→ shard schema = `{patient_id, status, citation_coverage, wall_time_s, result}`,落 `patients/<pid>.json`
      - 进度行:`print(f"[{pid}] {pname} ... {status.upper()} ({wall:.1f}s, coverage={score:.2f})", flush=True)`
      - 异常分级(D-05 + PATTERNS §F):
        - `except LLMFailure as e:` → `_write_failed(output_dir, pid, str(e.last_error), e.stage, last_llm_output=None)` + 打印 `[{pid}] {pname} ... FAIL ({e.stage}: {e.last_error})`
        - `except (KeyError, ValueError) as e:` → `_write_failed(..., stage="build", last_llm_output=None)` + 打印 `FAIL (build: {e})`
        - `except asyncio.CancelledError: raise` —— **不写 shard,必须透传**,让 gather 优雅取消
        - **不要** 加 `except Exception` 通配(D-05 + PATTERNS §F 明令禁止——会吞 CancelledError)
    - **测试 7 cases**(D-17,严格命名):
      - `test_run_pipeline_happy_path` —— 3 patients × mock httpx 全 200 → 3 个 `patients/*.json` 写入,无 `_failed/`,run_pipeline 返回 0
      - `test_run_pipeline_partial_accepted` —— 1 patient 触发 feedback(complete_structured_with_feedback 第一次 coverage<0.5 + 第二次仍<0.5)→ shard `status="partial"`,其它 2 个 ok,exit 0
      - `test_run_pipeline_one_failure_isolated` —— 1 patient mock httpx.TimeoutException 持续(transport 3 次重试均超时)→ Phase 2 抛 `LLMFailure(stage="transport")` → 该 patient 写 `_failed/`,其它 2 patient 在 `patients/`,exit 1
      - `test_run_pipeline_resume_skips_existing_shards` —— 预放 `patients/p001.json`,resume=True 跑 3 patient → mock_http 收到的 chat/completions 请求数 < 3(p001 不走 LLM)
      - `test_run_pipeline_resume_retries_failed_shards` —— 预放 `_failed/p001.json`,resume=True 跑 → run 后 `_failed/p001.json` 不存在(被 _scan_resume 删除),`patients/p001.json` 存在(本次成功)
      - `test_atomic_write_no_partial_on_crash` —— mock `Path.write_text` 抛异常 → 目标 `.json` 不存在(`.tmp` 可能残留)
      - `test_concurrency_caps_in_flight` —— `pat_sem=2` + 5 patients,mock 慢响应 → in_flight peak ≤ 2(用 dict counter + asyncio.sleep)
      - 全部测试用 `pytest.mark.asyncio` + `AsyncMock(spec=httpx.AsyncClient)`,URL 路由桩(/mcp 走 QMD response,/chat/completions 走 LLM response),**不**调真实 vLLM / QMD
    - **测试 fixtures**:
      - `tests/fixtures/mock_qmd_hit.json` —— 单条 QMD hit `{content, path, score, context}` per PATTERNS §mock_qmd_hit
      - `tests/fixtures/mock_patient.json` —— 单 patient per PATTERNS §mock_patient(贾常山,disease_type="结直肠癌")
  </behavior>
  <action>
    在 Task 1 落地的 `scripts/pipeline.py` 后追加:
    1. `run_pipeline(args)` —— 严格按 PATTERNS §2 骨架实现。注意三个细节:
       - **三 sem 互不嵌套**(D-04):pat_sem 创建在 run_pipeline 顶层不传给 QMD/LLM;qmd_sem 通过 `AsyncQMDService(semaphore=qmd_sem)` 注入;llm_sem 通过 `AsyncLLMClient(..., semaphore=llm_sem)` 注入。**禁止**复用同一 Semaphore 实例。
       - **共享 http**(D-03):同一 `httpx.AsyncClient` 注入到 QMD 与 LLM,`_owns_http=False` 自动生效,`__aexit__` 不关闭。
       - **finally 块**(D-07):用 `try/finally` 包 gather,finally 中**无论成功失败**都要计算 `wall = time.monotonic() - wall_start` + `_merge_rag_results` + `_atomic_write_json(... "rag_results.json", aggregate)`。这样 `KeyboardInterrupt`(Ctrl-C) 也能尽力 merge。
    2. `_run_one_patient(...)` —— 按 PATTERNS §3 字面实现,6 stages 串行。注意:
       - Stage 1 的 `extract_patient_features` / `build_queries` 用 `from scripts.batch_pipeline import extract_patient_features, build_queries`(**模块顶部 import**,不在函数内 inline import,避免每个 patient 重复 import 开销)
       - Stage 3 的 `asyncio.gather(*[qmd.query(q) for q in queries])` flatten 用 list comprehension:`hits = _dedupe_hits([h for sub in hits_per_query for h in sub])`
       - Stage 4 双层过滤顺序:**先** `filter_orgs_by_disease`(对 `_hit_org(h) in allowed_orgs` 做过滤)**再** `filter_chunks_by_disease`(Phase 1 D-10 函数,自带 chunk_keys=空时保守保留 KBM-06)
       - Stage 5 build_patient_prompt 用 Task 1 实现的函数
       - Stage 6 `complete_structured_with_feedback` 返回 `(result, score, status)`,直接拼装 shard 落盘
       - except 顺序:`LLMFailure` → `(KeyError, ValueError)` → `asyncio.CancelledError`(re-raise)。**禁止** `except Exception`(WR-08 立约 + D-05)
    3. 创建 `tests/fixtures/mock_qmd_hit.json` 与 `tests/fixtures/mock_patient.json`(完整 schema 见 PATTERNS §mock_qmd_hit / §mock_patient)
    4. 创建 `tests/test_pipeline.py`,7 个 case 严格按 D-17 命名实现。共享 fixtures:
       - `mock_http` —— `AsyncMock(spec=httpx.AsyncClient)`,`post.side_effect = _routed_post`(按 URL 路由 QMD/LLM 响应,见 PATTERNS §双桩路由)
       - `mock_patients` —— 三 patient list,patient_id ∈ {p001,p002,p003}
       - `tmp_output_dir` —— `tmp_path / "Output"`,创建 patients/ + _failed/
       - `runtime_args` —— `argparse.Namespace(patients=..., output_dir=..., llm_profile="qwen3-vllm-lan", concurrency_patients=2, concurrency_qmd=4, resume=False, kb_root=str(tmp_path))`
       - 关键:用 `@patch("scripts.retriever.subprocess.Popen")` 阻止真实 QMD 启动;用 `@patch("scripts.retriever.AsyncQMDService._wait_for_ready_async", new=AsyncMock(return_value=None))` 跳过 ready 等待
    5. 测试 case 5(resume_retries_failed_shards)的桩:预放 `_failed/p001.json` 后,本次跑 mock 让 p001 成功(httpx 返回 valid LLM response)→ 断言 `_failed/p001.json` 不存在 + `patients/p001.json` 存在
    6. 测试 case 7(concurrency_caps_in_flight)用 `in_flight = {"current":0, "peak":0}` + 慢响应模式(PATTERNS §测试 7 关键复用)
    7. 严格遵守 Python 3.9.6 anti-patterns:`Optional[X]` 而非 `X | None`、`List[X]`、`gather + return_exceptions`、**禁止** `TaskGroup` / `asyncio.timeout` / `except*` / 运行时 `dict[...]` / `list[...]` 泛型注解
  </action>
  <verify>
    <automated>cd /Users/f.sh/Workspace/devs/medical-guidelines-suite && python3 -m pytest tests/test_pipeline.py tests/test_pipeline_helpers.py -v 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pipeline.py` 7 cases 全部 PASS,且严格命名(`test_run_pipeline_happy_path / _partial_accepted / _one_failure_isolated / _resume_skips_existing_shards / _resume_retries_failed_shards / test_atomic_write_no_partial_on_crash / test_concurrency_caps_in_flight`)
    - `tests/test_pipeline_helpers.py` 16 cases 全部 PASS(Task 1 不回归)
    - `python3 -c "from scripts.pipeline import run_pipeline, _run_one_patient; import inspect; assert inspect.iscoroutinefunction(run_pipeline) and inspect.iscoroutinefunction(_run_one_patient)"` 无错
    - `grep -nE 'TaskGroup|asyncio\.timeout|except\*' scripts/pipeline.py | grep -v '^#'` 输出空
    - `grep -nE 'X \| Y' scripts/pipeline.py | grep -v '^#'` 输出空
    - `grep -n 'except Exception' scripts/pipeline.py | grep -v '^#'` 输出空(D-05 + WR-08)
    - `grep -nc 'asyncio\.gather' scripts/pipeline.py` ≥ 2(顶层 patients gather + Stage 3 queries gather)
    - `grep -nc 'return_exceptions=True' scripts/pipeline.py` ≥ 1(D-01)
    - `grep -n 'http_client=http\|http=http' scripts/pipeline.py | wc -l` ≥ 2(D-03,QMD + LLM 共享 http 实例)
    - run_pipeline 函数体内 finally 块有 `_merge_rag_results` 调用(grep `finally:` 后跟 `_merge_rag_results`)
    - `python3 -m pytest tests/ -q 2>&1 | tail -3` 显示 0 failure(整套测试套件未回归)
  </acceptance_criteria>
  <done>
    `scripts/pipeline.py` 完整(11 工具函数 + run_pipeline + _run_one_patient,~280 LOC);`tests/test_pipeline.py` 7 cases + `tests/test_pipeline_helpers.py` 16 cases 全部 PASS;两个 fixture 文件就位;Python 3.9.6 anti-patterns zero 出现;整套测试套件无回归。
  </done>
</task>

</tasks>

<verification>
**Phase 3 Plan 01 单元层验收**(由 Plan 03-03 端到端 E2E 命令补全):

```bash
# 1. 工具层单测(Task 1)
python3 -m pytest tests/test_pipeline_helpers.py -v

# 2. 顶层 + 子流水线单测(Task 2)
python3 -m pytest tests/test_pipeline.py -v

# 3. 整套测试套件不回归(Phase 1+2 共 247 测试 + Plan 01 新 23 测试 = 270+)
python3 -m pytest tests/ -q

# 4. Python 3.9.6 anti-pattern 检查
grep -nE 'TaskGroup|asyncio\.timeout|except\*|: list\[|: dict\[' scripts/pipeline.py | grep -v '^#' | wc -l   # 必须 = 0
grep -nE 'X \| Y|Optional\b' scripts/pipeline.py | grep -v '^#' | grep -c 'Optional'   # 应有多处 Optional 使用

# 5. 必要 import 完整
python3 -c "from scripts.pipeline import run_pipeline, _run_one_patient, compute_citation_coverage, build_patient_prompt, _atomic_write_json, _merge_rag_results, _scan_resume, _write_failed"
```
</verification>

<success_criteria>
- `scripts/pipeline.py` ~280 LOC 单文件,内含 11 个工具函数 + `run_pipeline` + `_run_one_patient`
- `tests/test_pipeline_helpers.py` 16 cases + `tests/test_pipeline.py` 7 cases 全绿
- `tests/fixtures/mock_qmd_hit.json` 与 `tests/fixtures/mock_patient.json` 落地
- Python 3.9.6 anti-pattern 0 出现(TaskGroup / asyncio.timeout / except* / `X | Y` / 运行时 dict/list 泛型 / `except Exception`)
- 整套测试套件 0 失败(Phase 1+2 不回归)
- D-01..D-09(并发原语 / 异常分级 / atomic write / resume / 退出码 / 派生 aggregate)全部在代码或测试中可见
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-run-subcommand-interface-extension/03-01-SUMMARY.md` with:
- **What was built**: scripts/pipeline.py 11 helpers + run_pipeline + _run_one_patient
- **Test counts**: helpers (16) + pipeline (7) = 23 new tests
- **Anti-pattern audit**: grep counts of TaskGroup / except*/ etc. = 0
- **Key contracts surfaced**: run_pipeline async signature + _run_one_patient stage list + shard schema
- **Phase 2 contracts used**: complete_structured_with_feedback 返回 (result, score, status) 三元组
- **Cross-reference**: 下一 Plan 03-02 将在 `batch_pipeline.py:main` 中通过 `subparsers.add_parser("run", ...)` 与 `asyncio.run(pipeline.run_pipeline(args))` 暴露
- **Decisions implemented (D-XX)**: D-01..D-09 + D-14..D-16
- **Open items for 03-03**: 端到端 E2E 验收命令(D-18)
</output>
