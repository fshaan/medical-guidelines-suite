---
phase: 03-pipeline-run-subcommand-interface-extension
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/test_pipeline_e2e_smoke.py
  - docs/phase3_e2e_acceptance.md
autonomous: false
requirements:
  - QG-01
  - QG-02
  - QG-03
  - QG-04
  - QG-05
  - QG-06
must_haves:
  truths:
    - "Plan 03-01 与 03-02 联合产物可端到端在 10 例 fixture 上 wall time <10min(QG-01)"
    - "所有 10 例 patients/<pid>.json 通过 validate --patients-dir,0 FAIL,citation_coverage >= 0.5 或标 partial(QG-02)"
    - "结直肠癌 3 例(贾常山/李学/肖庆周)的 ESMO/JGCA/CACA guideline_results 不含'胃癌'字样(QG-03)"
    - "全部 evidence_level 落在 PATIENT_RECOMMENDATION_SCHEMA enum 内(QG-04),零 JSON 解析错误(QG-05)"
    - "pytest tests/ 全绿,与现有基线对齐(QG-06)"
  artifacts:
    - path: "tests/test_pipeline_e2e_smoke.py"
      provides: "可在 CI(无真实 vLLM)运行的 E2E smoke test,3-patient fixture 走完整 run_pipeline → rag_results.json 链路;真实 vLLM 验收靠 docs/phase3_e2e_acceptance.md 手工命令"
      min_lines: 100
    - path: "docs/phase3_e2e_acceptance.md"
      provides: "10 例 fixture 真实 vLLM + QMD 验收命令清单(D-18):time run / validate / jq 跨指南反例 / pytest 回归"
      min_lines: 60
  key_links:
    - from: "tests/test_pipeline_e2e_smoke.py"
      to: "scripts.pipeline.run_pipeline"
      via: "asyncio.run(run_pipeline(args)) 全链路调用 + mock httpx 双桩"
      pattern: "run_pipeline\\("
    - from: "docs/phase3_e2e_acceptance.md"
      to: "scripts/batch_pipeline.py run"
      via: "time batch_pipeline.py run --patients ... --output-dir ... --llm-profile qwen3-vllm-lan"
      pattern: "batch_pipeline\\.py run"
---

<objective>
落地 Phase 3 ship gate 的端到端验收层:

1. **CI 可运行的 smoke E2E**(`tests/test_pipeline_e2e_smoke.py`)—— 3 例 mock patient × mock httpx 双桩(QMD + LLM)走完整 `run_pipeline` 链路,断言:`Output/patients/*.json` × 3 + `Output/rag_results.json` 派生 aggregate + 退出码 0
2. **真实 vLLM 手工验收清单**(`docs/phase3_e2e_acceptance.md`)—— D-18 明确 E2E 真实环境验收为人工命令,本文件固化命令 + 检查清单 + 失败诊断 + 验收 Sign-off 模板
3. **Phase 3 完成 gate checkpoint**(`checkpoint:human-verify`)—— 用户跑完真实 10 例 fixture 并把 wall time / `_failed/` 状态 / jq 反例校验粘贴到 checkpoint 输出,确认 Phase 3 ship gate 达成

Purpose: ROADMAP Phase 3 success criteria #1/#3/#4/#5 涉及真实 LLM + QMD 调用,无法纯 mock 通过(QG-01 wall time 必须真实测;QG-03 反例校验依赖真实 LLM 输出)。CI 层只能做 smoke 验收(链路通 + schema 正确),真实 wall-time gate 必须有 human verify。

Output:
- 新文件 `tests/test_pipeline_e2e_smoke.py`(~120 LOC,3 cases 覆盖完整链路 + rag_results.json + 退出码语义)
- 新文件 `docs/phase3_e2e_acceptance.md`(人工验收命令清单 + sign-off 表)
- 一个 `checkpoint:human-verify` 任务,用户跑完真实 10 例后填写 sign-off 表
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-01-SUMMARY.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-02-SUMMARY.md
@scripts/pipeline.py
@scripts/batch_pipeline.py
@tests/test_pipeline.py
@tests/test_cli_subcommands.py
@CLAUDE.md

<interfaces>
<!-- Plan 03-03 仅消费 Plan 03-01 / 03-02 已落地接口,不引入新代码契约。 -->

From scripts/pipeline.py (Plan 03-01):
- async def run_pipeline(args) -> int   # 顶层入口
- compute_citation_coverage / build_patient_prompt / _atomic_write_json (helpers)

From scripts/batch_pipeline.py (Plan 03-02):
- main() 已注册 run 子命令并 dispatch 到 pipeline.run_pipeline
- cmd_validate 接受 --patients-dir 参数
- cmd_generate 接受 --patients-dir 参数

E2E fixture(已存在 KB 与 patient 数据,Plan 03-03 不创建,只引用):
- Input/2026-4-23.xlsx (10 例患者真实数据,refactor_plan §九已确认)
- $MEDICAL_GUIDELINES_DIR (97 份指南 + Phase 1 .metadata/ 已落地)
- config/llm_profiles.yaml::qwen3-vllm-lan (Phase 2 落地)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_pipeline_e2e_smoke.py — 3 cases 验证完整 run_pipeline + main() dispatch 链路打通(mock httpx)</name>
  <files>tests/test_pipeline_e2e_smoke.py</files>
  <read_first>
    - tests/test_pipeline.py (Plan 03-01 落地的 7 个 case,理解 mock httpx 双桩路由模式)
    - tests/test_cli_subcommands.py (Plan 03-02 落地,理解 CLI dispatch 测试模式)
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md sections D-06 (退出码) / D-07 (rag_results.json 派生) / D-18 (E2E 验收策略)
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md sections 关于 mock httpx 双桩路由
    - scripts/pipeline.py 完整内容(Plan 03-01 输出)
    - scripts/batch_pipeline.py main() 改造后版本(Plan 03-02 输出)
  </read_first>
  <action>
    创建 tests/test_pipeline_e2e_smoke.py,共 3 cases,定位为"smoke E2E"(全链路打通 + schema 正确,但不需要真实 vLLM/QMD)。区别于 Plan 03-01 test_pipeline.py 7 cases 的"单元级 mock":本文件用 subprocess 调 batch_pipeline.py 入口 OR 直接 monkeypatch + asyncio.run(run_pipeline) 走完整 CLI 路径,断言端到端产物 schema 与 ROADMAP success criteria 对齐。

    顶部 imports:
    - import asyncio, json, os, sys, argparse
    - from pathlib import Path
    - from unittest.mock import AsyncMock, MagicMock, patch
    - import httpx, pytest

    Fixtures:
    - mock_e2e_patients(3 example): 贾常山(结直肠癌)/ 王某某(胃癌)/ 李某某(肺癌),每例字段满足 extract_patient_features 最低要求(patient_id / patient_name / disease_type / primary_site / stage)
    - mock_e2e_http(AsyncMock spec=httpx.AsyncClient): post.side_effect 按 URL 路由 — /mcp 路径返回 2-3 条 QMD hits(贾常山的返回包含 nccn-colorectal chunk,内容含 "FOLFOX"),/chat/completions 返回符合 PATIENT_RECOMMENDATION_SCHEMA 的有效 JSON(recommendation 引用 [1] [2],evidence_level 在 enum 内)
    - tmp_output_dir(tmp_path / "Output",创建 patients/ 与 _failed/ 子目录)
    - 用 patch("scripts.retriever.subprocess.Popen") 阻止真实 QMD 启动 + patch("scripts.retriever.AsyncQMDService._wait_for_ready_async", new=AsyncMock(return_value=None)) 跳过等待

    3 Cases:

    1. test_e2e_full_pipeline_produces_rag_results
       - 准备:patients.json 落 tmp 路径,含 3 mock patients
       - 调:argparse.Namespace 直接传 run_pipeline(args=Namespace(patients=str(tmp/"patients.json"), output_dir=str(tmp_output_dir), llm_profile="qwen3-vllm-lan", concurrency_patients=2, concurrency_qmd=4, resume=False, kb_root=str(tmp_path)))
       - 用 @patch("httpx.AsyncClient") 注入 mock_e2e_http(让 run_pipeline 顶层 async with httpx.AsyncClient(...) 拿到 mock)
       - 等待 asyncio.run(run_pipeline(args)) 返回
       - 断言:
         - 返回退出码 0
         - tmp_output_dir/"patients" 下有 3 个 *.json 文件
         - tmp_output_dir/"_failed" 为空
         - tmp_output_dir/"rag_results.json" 存在且 schema = {patients: list[3], failures: list[0], summary: {total:3, ok:3, partial:0, failed:0, wall_time_s: number}}
         - 每个 patient shard 含必填字段 patient_id / status / citation_coverage / wall_time_s / result
         - result.guideline_results 列表非空,每条 evidence_level 在 PATIENT_RECOMMENDATION_SCHEMA enum 内

    2. test_e2e_main_cli_dispatch_run
       - 测 Plan 03-02 改造的 main() dispatch 是否正确路由 run 子命令到 pipeline.run_pipeline
       - 用 monkeypatch 替换 sys.argv = ["x","run","--patients",str(tmp/"patients.json"),"--output-dir",str(tmp_output_dir),"--llm-profile","qwen3-vllm-lan","--concurrency-patients","2","--concurrency-qmd","4","--kb-root",str(tmp_path)]
       - patch scripts.pipeline.run_pipeline = AsyncMock(return_value=0) 拦截真实调用
       - patch httpx.AsyncClient 避免真实 HTTP
       - 调 scripts.batch_pipeline.main() 用 pytest.raises(SystemExit) 捕获 sys.exit
       - 断言:scripts.pipeline.run_pipeline.assert_called_once_with(ANY namespace),且传入 namespace 的 concurrency_patients == 2、concurrency_qmd == 4、llm_profile == "qwen3-vllm-lan"
       - 断言:SystemExit.code == 0

    3. test_e2e_partial_and_failure_propagate_exit_code
       - 准备:3 patients,其中 1 个 mock LLM 返回 citation_coverage=0.3 两次(成 partial),另 1 个 mock httpx 持续 TimeoutException(transport 3 次重试均超时 → LLMFailure(stage="transport") → 写 _failed/),第 3 个正常 ok
       - 调 run_pipeline
       - 断言:
         - 退出码 1(任一 _failed/ 即 1,D-06)
         - patients/ 下 2 文件(1 ok + 1 partial)
         - _failed/ 下 1 文件,内容含 patient_id / error / stage="transport" / last_llm_output / attempted_at
         - rag_results.json summary == {total:3, ok:1, partial:1, failed:1, wall_time_s: number}

    所有 case 必须独立运行(无 case 间状态泄漏),用 tmp_path fixture 自动隔离。
    禁止 Python 3.9.6 anti-patterns(同 Plan 03-01)。
  </action>
  <verify>
    <automated>cd /Users/f.sh/Workspace/devs/medical-guidelines-suite && python3 -m pytest tests/test_pipeline_e2e_smoke.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_pipeline_e2e_smoke.py 3 cases 全部 PASS
    - case 1 完整断言 rag_results.json schema 与 patient shard 内 evidence_level enum 校验
    - case 2 验证 batch_pipeline.py main() 改造正确路由到 pipeline.run_pipeline 且参数透传无误
    - case 3 验证退出码 1 + _failed/<pid>.json schema 与 rag_results.json summary 数值一致
    - python3 -m pytest tests/ -q 2>&1 | tail -3 显示 0 failure(整套测试套件未回归)
    - grep -cE "TaskGroup|asyncio\\.timeout|except\\*" tests/test_pipeline_e2e_smoke.py 等于 0
  </acceptance_criteria>
  <done>
    tests/test_pipeline_e2e_smoke.py 落地,3 cases 全绿,覆盖完整链路(run_pipeline + main dispatch + 异常 + 退出码 + rag_results.json schema)。整套测试套件无回归。
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/phase3_e2e_acceptance.md — 真实 vLLM + QMD 10 例验收命令清单 + sign-off 模板</name>
  <files>docs/phase3_e2e_acceptance.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 3 success criteria 5 条(逐条对照命令)
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md section D-18 (E2E 验收策略)
    - .planning/REQUIREMENTS.md QG-01..QG-06
    - docs/refactor_plan_2026-05-11.md section 九(端到端验证方案,如果存在)
  </read_first>
  <action>
    创建 docs/phase3_e2e_acceptance.md,结构如下(全部简体中文):

    # Phase 3 端到端验收命令清单

    本文件固化 Phase 3 ship gate(refactor_plan §九 + ROADMAP success criteria 5 条)。所有命令在真实环境(内网 vLLM + 本机 QMD + 完整 KB)执行,不进 CI。完成后填写下方 Sign-off 表。

    ## 0. 前置条件检查

    - 列 4 条:vLLM up(curl base_url/v1/models 返回 200)、QMD 索引完整($MEDICAL_GUIDELINES_DIR/.metadata/ 三文件存在)、env 已设置(LLM_PROFILE / LLM_BASE_URL / MEDICAL_GUIDELINES_DIR)、Input/2026-4-23.xlsx 存在

    ## 1. 执行 parse → run 流水线(QG-01)

    ```
    python3 scripts/batch_pipeline.py parse --input Input/2026-4-23.xlsx --output Output/patients.json
    time python3 scripts/batch_pipeline.py run --patients Output/patients.json --output-dir Output/ --llm-profile qwen3-vllm-lan --concurrency-patients 5 --concurrency-qmd 8
    ```

    **验收门**:wall time 必须 < 10 min(QG-01)

    若失败诊断指引:列 3 条 — wall time 超时但 QMD/LLM 单调用正常 → 并发未生效,检查 sem 注入;_failed/ 多于预期 → 看 _failed/<pid>.json.error 字段判断 transport/schema/build;Ctrl-C 中断 → rag_results.json 应在 finally 块仍生成(D-07)

    ## 2. 批验证 patients/(QG-02 + QG-04 + QG-05)

    ```
    python3 scripts/batch_pipeline.py validate --patients-dir Output/patients/ --patients Output/patients.json
    ```

    **验收门**:输出 0 FAIL,所有 10 例 citation_coverage >= 0.5 或 status == "partial"(QG-02);全部 evidence_level 在 enum 内(QG-04);全部 JSON 解析成功无报错(QG-05)

    ## 3. 结直肠癌反例校验(QG-03)

    ```
    for pid in 贾常山 李学 肖庆周; do
      echo "=== $pid ==="
      jq -r '.result.guideline_results[] | select(.guideline | IN("ESMO","JGCA","CACA")) | .recommendation' Output/patients/${pid}.json | grep -c "胃癌" || echo 0
    done
    ```

    **验收门**:三次 grep -c 输出必须全部为 0(refactor_plan §九)

    若任一非 0 → 双层过滤失效:检查 KB/.metadata/chunks.json 是否含 colorectal 标签;检查 normalize_disease 是否把"结直肠癌"映射到 canonical_key="colorectal";检查 filter_chunks_by_disease 是否被 _run_one_patient stage 4 调用

    ## 4. evidence_level enum 校验(QG-04 二次)

    ```
    jq -r '.patients[].result.guideline_results[].evidence_level' Output/rag_results.json | sort -u
    ```

    **验收门**:输出每行都在 PATIENT_RECOMMENDATION_SCHEMA evidence_level enum 内(23 个值)

    ## 5. 生成报告(generate --patients-dir)

    ```
    python3 scripts/batch_pipeline.py generate --patients-dir Output/patients/ --output-dir Output/ --format md
    ```

    **验收门**:Output/report.md 存在,10 例 patient 每例一段,跨指南推荐表格非空

    ## 6. pytest 回归(QG-06)

    ```
    python3 -m pytest tests/ -q
    ```

    **验收门**:全绿,与现有 247 测试基线 + Plan 03 新增 ~30 测试对齐(预期 270+ tests passing)

    ## 7. 旧 batch 子命令 hidden 但仍可调用(CLI-05)

    ```
    python3 scripts/batch_pipeline.py --help | grep -E "split|orchestrate|verify-batch|merge"
    python3 scripts/batch_pipeline.py orchestrate --help | head -3
    ```

    **验收门**:第一行输出 0 行(hidden);第二行返回正常 help(仍可调用)

    ## Sign-off 表

    | 检查项 | 通过 | 实测值 | 备注 |
    |--------|------|--------|------|
    | wall time < 10min (QG-01) | [ ] | _____ s | |
    | 10 例 0 FAIL (QG-02) | [ ] | __ FAIL | |
    | 结直肠癌不含"胃癌" (QG-03) | [ ] | grep counts 三个值 | |
    | evidence_level enum (QG-04) | [ ] | __ unique values | |
    | 0 JSON 解析错误 (QG-05) | [ ] | __ parse errors | |
    | pytest 全绿 (QG-06) | [ ] | __ passed, __ failed | |
    | 4 子命令 hidden (CLI-05) | [ ] | __ visible in help | |

    Sign-off by: ___________  Date: ___________

    所有检查项 [x] 后,Phase 3 ship gate 达成,可触发 Phase 4 stabilize 一周倒计时(refactor_plan §七)。
  </action>
  <verify>
    <automated>test -f /Users/f.sh/Workspace/devs/medical-guidelines-suite/docs/phase3_e2e_acceptance.md && wc -l /Users/f.sh/Workspace/devs/medical-guidelines-suite/docs/phase3_e2e_acceptance.md | awk '{print $1; exit ($1>=60)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - docs/phase3_e2e_acceptance.md 存在且 >= 60 行
    - 包含 7 个验收章节(0 前置 + 1 run + 2 validate + 3 反例 + 4 enum + 5 generate + 6 pytest + 7 hidden)
    - 每个章节含至少 1 个可复制 shell 命令
    - 包含 Sign-off 表 7 行 + 责任人/日期占位
    - QG-01..QG-06 在文件中字面出现(grep -c "QG-0" 输出 >= 6)
    - CLI-05 字面出现
  </acceptance_criteria>
  <done>
    docs/phase3_e2e_acceptance.md 落地,7 章节 + sign-off 表 + 失败诊断指引齐全。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 用户执行 docs/phase3_e2e_acceptance.md 全 7 步真实环境验收 + 填写 Sign-off 表</name>
  <what-built>
    Plan 03-01 落地的 scripts/pipeline.py(run_pipeline + _run_one_patient + 11 helpers)+ Plan 03-02 落地的 batch_pipeline.py main() 改造(run 子命令 + 4 hidden + validate/generate --patients-dir 互斥)+ Task 1 落地的 smoke E2E 测试(3 cases,mock httpx)+ Task 2 落地的真实环境验收清单文档。
  </what-built>
  <how-to-verify>
    在真实环境(内网 vLLM + 本机 QMD + $MEDICAL_GUIDELINES_DIR 完整 KB)逐条执行 docs/phase3_e2e_acceptance.md 中的 7 个验收章节:

    1. 前置条件:确认 vLLM 可达 / QMD .metadata/ 完整 / env 已设置 / Input/2026-4-23.xlsx 存在
    2. parse + time run 跑完 10 例,记录 wall time(必须 < 10min)
    3. validate --patients-dir 必须输出 0 FAIL
    4. 三例结直肠癌 grep -c "胃癌" 必须全部为 0
    5. jq evidence_level | sort -u 检查所有值都在 enum 23 个变体内
    6. generate --patients-dir 产出 Output/report.md
    7. pytest tests/ -q 必须全绿
    8. batch_pipeline.py --help 不显示 split/orchestrate/merge/verify-batch,但单独调 orchestrate --help 仍可达

    把 Sign-off 表 7 行的实测值填入 docs/phase3_e2e_acceptance.md(直接编辑文件),并把表格作为回复粘贴到 checkpoint 输出。

    若任一验收门失败:贴出失败章节的 stdout + stderr + 你的诊断猜测(transport / schema / 双层过滤 / 并发 / fixture 问题等),由 planner/executor 决定下一步是补丁修复(同 phase 内回滚 Plan 03-01/03-02)还是开启 Phase 3.5 gap-closure。
  </how-to-verify>
  <resume-signal>
    在以下两种回应中选一:
    - "approved + sign-off filled" + 把填好的 Sign-off 表粘到回复中 → Phase 3 ship gate 达成,触发 phase transition + Phase 4 stabilize 计时
    - "failed at step N: <details>" + 失败章节诊断 → 进入 gap-closure 子流程
  </resume-signal>
</task>

</tasks>

<verification>
Plan 03-03 验收:

```
# 1. smoke E2E 测试就位
python3 -m pytest tests/test_pipeline_e2e_smoke.py -v

# 2. 验收文档完整
test -f docs/phase3_e2e_acceptance.md && wc -l docs/phase3_e2e_acceptance.md

# 3. 用户跑完真实环境 7 步 + 填好 Sign-off 表(checkpoint 完成信号)
```
</verification>

<success_criteria>
- tests/test_pipeline_e2e_smoke.py 3 cases 全绿(链路打通 smoke gate)
- docs/phase3_e2e_acceptance.md 7 章节 + Sign-off 表落地
- 用户在真实环境跑完 10 例并填写 Sign-off 表全部通过([x] 全部勾选)
- ROADMAP Phase 3 success criteria #1/#3/#4/#5 全部由 Sign-off 表验证
- QG-01..QG-06 全部覆盖
</success_criteria>

<output>
After completion, create .planning/phases/03-pipeline-run-subcommand-interface-extension/03-03-SUMMARY.md with:
- What was built: smoke E2E 3 cases + 真实环境验收清单 docs + checkpoint sign-off
- Sign-off 实测值: 把用户填好的 Sign-off 表完整复制到本 SUMMARY
- Decisions implemented (D-XX): D-18 (E2E 验收策略)
- Quality gates passed: QG-01..QG-06 实测值与对应章节交叉引用
- Open items for Phase 4: 待 stabilize 一周后启动(refactor_plan §七)
- Performance baseline 更新: 10 例 wall time 从 ~60min 降至 ___s
</output>
