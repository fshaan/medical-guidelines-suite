---
phase: 03-pipeline-run-subcommand-interface-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/batch_pipeline.py
  - tests/test_cli_subcommands.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CFG-03
must_haves:
  truths:
    - "batch_pipeline.py run --patients X --output-dir Y --llm-profile Z 调用 asyncio.run(pipeline.run_pipeline(args)),退出码透传"
    - "validate / generate 同时接受 --input(deprecated) 与 --patients-dir(v3.1 主路径) 两种入口,互斥组保证只能选一个"
    - "split / orchestrate / merge / verify-batch 四个子命令在 batch_pipeline.py --help 中不出现,但 batch_pipeline.py SUBCMD --help 仍可调用"
    - "env LLM_PROFILE / PIPELINE_CONCURRENCY_PATIENTS / PIPELINE_CONCURRENCY_QMD / MEDICAL_GUIDELINES_DIR 在 CLI 默认值中被消费,CLI flag 显式传入则覆盖 env"
    - "cmd_index 调用 build_sidecar 后,KB .metadata/ 下三文件存在(已由 Phase 1 落地,Plan 02 仅加 smoke test 校验未回归)"
  artifacts:
    - path: "scripts/batch_pipeline.py"
      provides: "run subcommand registration + main() routing to pipeline.run_pipeline + p_validate/p_gen mutually-exclusive --input/--patients-dir + 4 old subparsers marked help=argparse.SUPPRESS + cmd_validate/cmd_generate 函数顶部分支处理 patients-dir"
    - path: "tests/test_cli_subcommands.py"
      provides: "argparse 注册行为与 hidden 子命令 smoke test (8 cases)"
      min_lines: 80
  key_links:
    - from: "scripts/batch_pipeline.py:main"
      to: "scripts/pipeline.py:run_pipeline"
      via: "args.command == 'run' 分支 + asyncio.run(pipeline.run_pipeline(args))"
      pattern: "asyncio\\.run\\(.*run_pipeline"
    - from: "scripts/batch_pipeline.py:p_validate / p_gen"
      to: "argparse.add_mutually_exclusive_group"
      via: "--input 与 --patients-dir 互斥"
      pattern: "add_mutually_exclusive_group"
    - from: "scripts/batch_pipeline.py:p_split / p_orch / p_merge / p_verify"
      to: "argparse.SUPPRESS"
      via: "help=argparse.SUPPRESS"
      pattern: "help=argparse\\.SUPPRESS"
---

<objective>
改造 `scripts/batch_pipeline.py:main()` CLI 注册层。新增 `run` 子命令路由到 Plan 03-01 落地的 `scripts.pipeline.run_pipeline`;扩展 `validate` 与 `generate` 接受 `--patients-dir`(v3.1 主路径)同时保留 `--input`(deprecated 兼容);把 `split / orchestrate / merge / verify-batch` 注册为 hidden(`help=argparse.SUPPRESS`,仍可调用);env vars(`LLM_PROFILE / PIPELINE_CONCURRENCY_PATIENTS / PIPELINE_CONCURRENCY_QMD`)在 CLI 默认值层 wired。覆盖 CLI-01..05 + CFG-03 共 6 条要求。

Purpose: 让用户通过 `batch_pipeline.py run --patients X --output-dir Y` 一行命令触发 Plan 03-01 实现的全自动流水线;同时为 Phase 4 一次性删除铺路。

Output: `scripts/batch_pipeline.py` 在 main() 函数(原 lines 2277-2378)区域增减;`tests/test_cli_subcommands.py` 新文件覆盖 argparse 行为。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md
@.planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-03-SUMMARY.md
@scripts/batch_pipeline.py
@scripts/kb_metadata.py
@CLAUDE.md

<interfaces>
<!-- 仅声明 Plan 02 必须调用的契约;不依赖 Plan 03-01 的内部实现细节,只依赖 run_pipeline 入口签名。 -->

From scripts/pipeline.py (Plan 03-01 落地,Plan 03-02 调用):

```
async def run_pipeline(args: argparse.Namespace) -> int:
    args 必须含:patients(str path), output_dir(str path), llm_profile(str),
    concurrency_patients(int), concurrency_qmd(int), resume(bool), kb_root(Optional[str])
    返回 0 或 1 退出码
```

From scripts/kb_metadata.py (Phase 1 落地):

```
def build_sidecar(kb_root: Path) -> Dict[str, Path]:
    已被 cmd_index 在 Phase 1-03 接入,产出 chunks.json / org_disease_coverage.json / synonym_map.yaml 三个文件
```

From scripts/batch_pipeline.py (现状 main 结构,见 lines 2277-2378):

main() 中已有 8 个子命令注册:parse / split / orchestrate / merge / validate / index / verify-batch / generate。args.command 在末尾 if/elif 链 dispatch 到对应 cmd_X 函数。

cmd_validate(args) 当前(batch_pipeline.py:1697)读 args.input;cmd_generate 同理。Plan 03-02 必须**新增分支**判断 args.patients_dir 而**不破坏**旧 args.input 路径(deprecated warning + continue)。

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: argparse 注册改造 — run 子命令 + 4 个 hidden + p_validate/p_gen 互斥组 + main() dispatch 路由 + env defaults</name>
  <files>scripts/batch_pipeline.py</files>
  <read_first>
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md sections D-10 (run args list), D-11 (validate/generate extension), D-12 (index already wired), D-13 (4 hidden)
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-PATTERNS.md section scripts/batch_pipeline.py items 1/2/3/4 (字面 patch 指引)
    - scripts/batch_pipeline.py lines 2277-2378 (main 现状,定位改造点)
    - scripts/batch_pipeline.py lines 1697-1836 (cmd_validate 现状,需要 patch 顶部分支)
    - scripts/batch_pipeline.py cmd_generate 现状(grep 定位)
    - Plan 03-01 落地的 scripts/pipeline.py:run_pipeline 签名(确认 args 字段名)
  </read_first>
  <action>
    在 scripts/batch_pipeline.py:main() 中实施以下 5 项改造,精确按 PATTERNS section scripts/batch_pipeline.py items 1-4 字面:

    改造 1: p_split / p_orch / p_merge / p_verify 四个子命令全部加 help=argparse.SUPPRESS(D-13 + CLI-05)
    - p_split = sub.add_parser("split", help=argparse.SUPPRESS) — 删原字面 help 文案
    - p_orch = sub.add_parser("orchestrate", help=argparse.SUPPRESS)
    - p_merge = sub.add_parser("merge", help=argparse.SUPPRESS)
    - p_verify = sub.add_parser("verify-batch", help=argparse.SUPPRESS)
    - 其余 args(--input / --batch-size / --output-dir / --patients / --kb-root 等)全部保留,只动顶层 help 字段
    - main() 末尾 dispatch 路由的四个 elif 分支(cmd_split / cmd_orchestrate / cmd_merge / cmd_verify_batch)保留不动(仍可调用)

    改造 2: 新增 p_run 子命令注册(CLI-01 + CFG-03 + D-10),按 PATTERNS section scripts/batch_pipeline.py item 1 字面:

    p_run = sub.add_parser("run", help="按患者并发流水线(v3.1 主路径)")
    p_run.add_argument("--patients", required=True, help="patients.json 路径(parse 输出)")
    p_run.add_argument("--output-dir", required=True, help="输出目录(含 patients/ _failed/ rag_results.json)")
    p_run.add_argument("--llm-profile", default=os.environ.get("LLM_PROFILE", "qwen3-vllm-lan"), help="LLM profile 名(默认 env LLM_PROFILE 或 qwen3-vllm-lan)")
    p_run.add_argument("--concurrency-patients", type=int, default=int(os.environ.get("PIPELINE_CONCURRENCY_PATIENTS", "5")), help="患者级并发上限(默认 env 或 5)")
    p_run.add_argument("--concurrency-qmd", type=int, default=int(os.environ.get("PIPELINE_CONCURRENCY_QMD", "8")), help="QMD 在飞请求上限(默认 env 或 8)")
    p_run.add_argument("--resume", action="store_true", help="跳过已完成 patients 并重试 _failed/")
    p_run.add_argument("--kb-root", default=os.environ.get("MEDICAL_GUIDELINES_DIR"), help="知识库根(默认 env MEDICAL_GUIDELINES_DIR)")

    位置: p_parse 之后、p_split 之前(成为第二个子命令)。

    改造 3: p_validate 与 p_gen 接受 --input / --patients-dir 互斥(CLI-02 + CLI-03 + D-11)

    p_validate = sub.add_parser("validate", help="验证 RAG 结果质量与完整性")
    g_val = p_validate.add_mutually_exclusive_group(required=True)
    g_val.add_argument("--input", help="rag_results.json 路径(v3.0 兼容,deprecated)")
    g_val.add_argument("--patients-dir", help="Output/patients/ 目录(v3.1 主路径)")
    p_validate.add_argument("--patients", help="patients.json 路径(可选,用于完整性对比)")
    p_validate.add_argument("--kb-profile", help="orchestration_plan.json(可选)")

    p_gen 同款互斥(--input 与 --patients-dir 互斥,--output-dir / --format 保留)。

    改造 4: main() dispatch 末尾新增 run 分支(在 if args.command == "parse" 之后插入):

    elif args.command == "run":
        from scripts.pipeline import run_pipeline
        sys.exit(asyncio.run(run_pipeline(args)))

    顶部 import 需添加 import asyncio(若已存在 grep 确认,不重复 import)。

    改造 5: cmd_validate 与 cmd_generate 函数顶部分支

    cmd_validate(args) 函数体最上方插入:

    if getattr(args, "patients_dir", None):
        return _validate_patients_dir(Path(args.patients_dir).resolve(), args)
    if getattr(args, "input", None):
        import warnings
        warnings.warn(
            "--input is deprecated, use --patients-dir for v3.1 pipeline",
            DeprecationWarning, stacklevel=2,
        )
        # 落到原 cmd_validate 逻辑(读 rag_results.json)

    新增私有函数 _validate_patients_dir(patients_dir: Path, args) -> None:
    - 扫 patients_dir.glob("*.json") sorted
    - 每个文件 load 为 shard,复用现有 per-patient 校验逻辑(MIN_REC_LENGTH 检查 / evidence_level 检查 / source_file 检查 / citation_coverage >= 0.5 OR status == "partial" 才视为 OK)
    - shard 格式见 Plan 03-01 specifics(patient_id / status / citation_coverage / wall_time_s / result,result 内含 guideline_results / consensus / differences)
    - 输出格式与现 cmd_validate 对齐(errors / warnings / 末尾 summary 通过: x 失败: y + sys.exit(1 if errors else 0))
    - 若 args.patients(patients.json)提供 → 对比完整性(patients_dir 中的 patient_id 集合 vs patients.json 期望集合,缺失加入 errors)

    cmd_generate(args) 同款顶部分支:

    if getattr(args, "patients_dir", None):
        return _generate_from_patients_dir(Path(args.patients_dir).resolve(), Path(args.output_dir), args.format)

    新增 _generate_from_patients_dir: 扫 patients_dir 下 *.json,合并为单一 report.md(reuse 现有 md_escape / _evidence_level_meaning 等辅助函数,不重写)。每例 patient 一段 markdown: ## 患者 ID Name + 跨指南推荐表格。

    不要做的事:
    - 不要删除任何 cmd_split / cmd_orchestrate / cmd_merge / cmd_verify_batch 函数本体(Phase 4 才删除)
    - 不要修改 cmd_index(Phase 1 D-12 已就位,Plan 03-02 仅在 Task 2 smoke test)
    - 不要更改 cmd_validate 现有的 args.input 路径行为(只在顶部新增 patients_dir 早返回 + deprecated warning)
    - 不要破坏现有 parse / index 两个子命令注册

    Python 3.9.6 约束: 类型注解用 Optional[X]、Dict[X,Y]、List[X];禁 X | None、list[X]、dict[X,Y] 在 runtime-evaluated context。
  </action>
  <verify>
    <automated>cd /Users/f.sh/Workspace/devs/medical-guidelines-suite && python3 scripts/batch_pipeline.py --help 2>&1 | grep -E "run|parse|validate|index|generate" | head -10 ; python3 scripts/batch_pipeline.py --help 2>&1 | grep -cE "split|orchestrate|verify-batch|merge"</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/batch_pipeline.py --help 输出中包含 "run" 子命令 + 描述 "按患者并发流水线"
    - python3 scripts/batch_pipeline.py --help 输出中 split / orchestrate / merge / verify-batch 四个子命令名 0 次出现(被 SUPPRESS)
    - python3 scripts/batch_pipeline.py orchestrate --help 仍能调用并返回 help 文本(hidden 不等于不可用)
    - python3 scripts/batch_pipeline.py validate --help 输出中同时含 --input 与 --patients-dir 两个 flag
    - python3 scripts/batch_pipeline.py validate --input X --patients-dir Y 报互斥错误(argparse 退出码 2)
    - python3 scripts/batch_pipeline.py generate --help 同上含两个 flag 与互斥
    - python3 scripts/batch_pipeline.py run --help 输出含 --patients / --output-dir / --llm-profile / --concurrency-patients / --concurrency-qmd / --resume / --kb-root 七个 flag
    - PIPELINE_CONCURRENCY_PATIENTS=3 python3 scripts/batch_pipeline.py run --help 时显示默认值 3(env 优先于字面默认 5)
    - grep -c "help=argparse.SUPPRESS" scripts/batch_pipeline.py 等于 4(精确 4 个 hidden)
    - grep -c "add_mutually_exclusive_group" scripts/batch_pipeline.py 等于 2(p_validate + p_gen)
    - grep -n "asyncio.run" scripts/batch_pipeline.py 命中 1 次以上(run 分支调用)
    - grep -nE "TaskGroup|asyncio\\.timeout|except\\*" scripts/batch_pipeline.py | grep -v "^#" 输出空
    - python3 -c "from scripts.batch_pipeline import _validate_patients_dir, _generate_from_patients_dir" 无 ImportError(两个新私有函数已定义)
  </acceptance_criteria>
  <done>
    main() 改造完成: 1 个新子命令(run) + 4 个 hidden + 2 个互斥组 + 主 dispatch 新分支 + cmd_validate/cmd_generate 顶部分支 + 2 个新私有函数(_validate_patients_dir / _generate_from_patients_dir)。Python 3.9.6 anti-patterns 0 出现。所有 acceptance grep / help 检查通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: tests/test_cli_subcommands.py — argparse 注册行为 smoke test + cmd_index sidecar smoke test</name>
  <files>tests/test_cli_subcommands.py</files>
  <read_first>
    - .planning/phases/03-pipeline-run-subcommand-interface-extension/03-CONTEXT.md sections D-10 / D-11 / D-13
    - scripts/batch_pipeline.py main() 改造后版本(Task 1 输出)
    - scripts/kb_metadata.py:build_sidecar(Phase 1 落地)— 用于 cmd_index smoke test
    - tests/test_kb_metadata.py(Phase 1 落地,作为 fixture/structure 参考)
  </read_first>
  <action>
    创建 tests/test_cli_subcommands.py,共 8 个 cases:

    顶部 imports:
    - import subprocess, os, sys, json
    - import pytest
    - from pathlib import Path
    - import scripts.batch_pipeline as bp(用于直接调 main 与 helper funcs)

    Cases:

    1. test_run_subcommand_in_help — subprocess 调 python3 scripts/batch_pipeline.py --help,assert stdout 含 "run" 子命令名 + "按患者并发" 字样,且不含 "split/orchestrate/merge/verify-batch" 任一字样
    2. test_hidden_subcommands_still_callable — subprocess 调 python3 scripts/batch_pipeline.py orchestrate --help,assert 返回 code 0 + stdout 含 "--patients"(原有 arg)。同样 for split / merge / verify-batch
    3. test_validate_mutually_exclusive — subprocess 调 python3 scripts/batch_pipeline.py validate --input X --patients-dir Y,assert returncode != 0 + stderr 含 "not allowed with"(argparse 互斥提示)
    4. test_generate_mutually_exclusive — 同 case 3 for generate
    5. test_run_args_defaults_from_env(用 monkeypatch.setenv):
       - 设 PIPELINE_CONCURRENCY_PATIENTS=3 + PIPELINE_CONCURRENCY_QMD=4 + LLM_PROFILE=test-profile
       - 调 bp.main 通过 monkeypatch sys.argv = ["x","run","--patients","p.json","--output-dir","Out"](mock sys.exit + asyncio.run 让其不真跑)
       - assert 通过捕获 args(在 monkeypatch 替换 asyncio.run 中收 args.namespace),concurrency_patients == 3 / concurrency_qmd == 4 / llm_profile == "test-profile"
    6. test_run_cli_flag_overrides_env — 同 5,设 env 但传 --concurrency-patients 7,assert 最终 args.concurrency_patients == 7(CLI > env)
    7. test_cmd_index_produces_sidecar(smoke test for CLI-04 + D-12):
       - 用 tmp_path 创建 mock KB 结构(参考 tests/test_kb_metadata.py 的 fixture):tmp_path/NCCN/extracted/x.md 与 tmp_path/ESMO/extracted/y.md
       - mock subprocess.run(qmd 调用)避免真实 QMD 启动
       - 调 cmd_index(argparse.Namespace(kb_root=str(tmp_path), force=False))
       - assert (tmp_path/.metadata/chunks.json).exists() 与 (tmp_path/.metadata/org_disease_coverage.json).exists() 与 (tmp_path/.metadata/synonym_map.yaml).exists()
       - 若 cmd_index 内部 qmd embed 调用难以 mock,fallback 方案: 直接 mock scripts.kb_metadata.build_sidecar 并断言被调用 once with 正确 kb_root
    8. test_main_dispatch_run_calls_run_pipeline(behavior contract):
       - monkeypatch.setattr(scripts.pipeline, "run_pipeline", AsyncMock(return_value=0))
       - monkeypatch.setattr(asyncio, "run", lambda c: 0)
       - monkeypatch sys.argv = ["x","run","--patients","p.json","--output-dir","Out"]
       - assert pytest.raises(SystemExit, bp.main) 退出码 0
       - 若 monkeypatch 难以拦截 asyncio.run 入口,fallback: 直接 monkeypatch sys.exit 并断言被调用 once with 0

    所有 case 用 pytest fixture monkeypatch 与 tmp_path;避免真实 subprocess QMD 启动;subprocess 调 batch_pipeline.py --help 用 timeout=10 防卡住。

    禁止 Python 3.9.6 anti-patterns(同 Plan 03-01)。
  </action>
  <verify>
    <automated>cd /Users/f.sh/Workspace/devs/medical-guidelines-suite && python3 -m pytest tests/test_cli_subcommands.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli_subcommands.py 8 个 case 全部 PASS
    - case 1 - 4 (subprocess --help 检查) 均通过 stdout/stderr 内容断言
    - case 5/6 (env + CLI 默认值优先级) 验证 CLI flag > env > 字面默认(CFG-03)
    - case 7 (cmd_index sidecar) 三个 .metadata/ 文件均落地或 build_sidecar 被调用断言成立
    - case 8 (main dispatch → pipeline.run_pipeline) 链路打通
    - python3 -m pytest tests/ -q 2>&1 | tail -3 显示 0 failure(整套测试套件未回归)
    - grep -nE "TaskGroup|asyncio\\.timeout|except\\*" tests/test_cli_subcommands.py | grep -v "^#" 输出空
  </acceptance_criteria>
  <done>
    tests/test_cli_subcommands.py 落地,8 cases 全绿,覆盖 run 注册 / hidden 子命令仍可调用 / 互斥组 / env+CLI 优先级 / index sidecar 产出 / main dispatch 路由。整套测试套件无回归。
  </done>
</task>

</tasks>

<verification>
Plan 03-02 CLI 层验收(由 Plan 03-03 E2E 验收命令补全 wall-time gate):

```
# 1. argparse 注册行为(Task 1)
python3 scripts/batch_pipeline.py --help

# 2. 互斥组拒绝双标志(Task 1)
python3 scripts/batch_pipeline.py validate --input X --patients-dir Y

# 3. Hidden 子命令仍可调用(Task 1)
python3 scripts/batch_pipeline.py orchestrate --help

# 4. env 优先级(Task 1)
LLM_PROFILE=test-x python3 scripts/batch_pipeline.py run --help

# 5. CLI smoke 测试(Task 2)
python3 -m pytest tests/test_cli_subcommands.py -v

# 6. 整套测试套件不回归
python3 -m pytest tests/ -q

# 7. Python 3.9.6 anti-patterns
grep -nE "TaskGroup|asyncio\.timeout|except\*" scripts/batch_pipeline.py | grep -v "^#" | wc -l
```
</verification>

<success_criteria>
- scripts/batch_pipeline.py main() 6 项改造全部就位(1 新子命令 + 4 hidden + 2 互斥组 + 1 dispatch 分支 + 2 cmd 顶部分支 + 2 新私有函数)
- tests/test_cli_subcommands.py 8 cases 全绿
- batch_pipeline.py --help 中可见子命令包含 parse / run / validate / index / generate(其余 4 个 hidden)
- env vars 默认值正确流动: env > 字面默认; CLI flag 显式传入则覆盖 env
- Python 3.9.6 anti-pattern 0 出现
- 整套测试套件无回归
- CLI-01..05 + CFG-03 全部在代码或测试中可见
</success_criteria>

<output>
After completion, create .planning/phases/03-pipeline-run-subcommand-interface-extension/03-02-SUMMARY.md with:
- What was built: batch_pipeline.py main() 改造(run 注册 + 4 hidden + 2 互斥 + dispatch + cmd_validate/generate 分支)+ test_cli_subcommands.py 8 cases
- Args contract: run --patients X --output-dir Y --llm-profile Z --concurrency-patients N --concurrency-qmd M --resume --kb-root K
- env wiring: LLM_PROFILE / PIPELINE_CONCURRENCY_PATIENTS / PIPELINE_CONCURRENCY_QMD / MEDICAL_GUIDELINES_DIR
- Hidden subcommands: split / orchestrate / merge / verify-batch (--help 隐藏,直接调用可达)
- Decisions implemented (D-XX): D-10 (run args) / D-11 (validate/generate --patients-dir) / D-12 (index sidecar verified) / D-13 (4 hidden via SUPPRESS)
- Cross-reference: 下一 Plan 03-03 将基于 Plan 03-01 + 03-02 联合产物执行 E2E 验收(D-18)
- Open items for Plan 03-03: 端到端 wall-time gate + 结直肠癌反例校验(QG-01..05)
</output>
