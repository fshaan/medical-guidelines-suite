---
phase: 02-llm-client-schema-unit-tests
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/llm_client.py
  - config/llm_profiles.yaml
  - tests/test_llm_profile.py
autonomous: true
requirements: [LLM-03, CFG-01, CFG-02]
tags: [llm, profile, env, yaml, config, dataclass]

must_haves:
  truths:
    - "LLMProfile.from_env() 类方法存在，按 LLM_PROFILE env 决定 profile name（默认 'qwen3-vllm-lan'），再分字段读取 7 个 LLM_* env 变量构造 LLMProfile"
    - "Profile 加载优先级 env > yaml > 内置默认 —— env 缺失字段 fallback 到 yaml（若 config/llm_profiles.yaml 存在），yaml 缺失字段 fallback 到 dataclass 字段默认值"
    - "config/llm_profiles.yaml 入仓且含 qwen3-vllm-lan / deepseek-cloud 两个 profile，base_url 中 `10.0.x.x` 是占位符（运维侧用 LLM_BASE_URL env 覆盖），不含任何 secret"
    - "_load_profiles_yaml(path) 在文件不存在时返回 {} 不报错；yaml 解析失败时同样返回 {}（防御性）"
    - "LLM_TIMEOUT / LLM_CONCURRENCY 解析失败（非整数）抛 ValueError，**不**静默 fallback 到默认值"
    - "api_key 懒解析：from_env() 创建的 LLMProfile 不存明文 key，仅存 api_key_env 字段（指向 env 变量名）"
    - "7 个 env 变量 LLM_PROFILE / LLM_BASE_URL / LLM_MODEL / LLM_API_KEY / LLM_TIMEOUT / LLM_STRUCTURED_MODE / LLM_CONCURRENCY 全部 wired（LLM_API_KEY 通过 api_key_env 指针，不进 dataclass 字段值）"
  artifacts:
    - path: "scripts/llm_client.py"
      provides: "LLMProfile.from_env classmethod + _load_profiles_yaml 模块函数（追加到 Plan 02-01 落地的文件，不修改 AsyncLLMClient/LLMFailure/SchemaError/PATIENT_RECOMMENDATION_SCHEMA 任何已有 surface）"
      contains: "def from_env"
    - path: "config/llm_profiles.yaml"
      provides: "两个 profile 静态元数据（入仓，不含 secret）"
      contains: "qwen3-vllm-lan"
    - path: "tests/test_llm_profile.py"
      provides: "from_env + yaml 加载 + 优先级 + 错误路径单元测试（7-9 case）"
      contains: "test_llm_profile_from_env"
  key_links:
    - from: "scripts/llm_client.py::LLMProfile.from_env"
      to: "_load_profiles_yaml"
      via: "env 缺失字段 fallback 到 yaml dict"
      pattern: "_load_profiles_yaml"
    - from: "scripts/llm_client.py::_load_profiles_yaml"
      to: "yaml.safe_load"
      via: "open(path) + safe_load"
      pattern: "yaml\\.safe_load"
    - from: "config/llm_profiles.yaml"
      to: "LLMProfile.from_env"
      via: "yaml 文件被 _load_profiles_yaml 读取，profile name 索引到对应 dict"
      pattern: "profiles:"
---

<objective>
为 Plan 02-01 落地的 `LLMProfile` frozen dataclass 追加 `from_env()` 类方法与模块级 `_load_profiles_yaml(path)` 函数，落地 7 个 `LLM_*` env 变量到 LLMProfile 字段的映射（env > yaml > 内置默认 三级优先级），同时把 `config/llm_profiles.yaml` 入仓（含 `qwen3-vllm-lan` 主 profile + `deepseek-cloud` fallback profile，不含 secret）。

Purpose: Phase 3 `pipeline.py:cmd_run` 入口需要 `LLMProfile.from_env(name=args.llm_profile)` 一行构造客户端配置；运维侧通过 `LLM_BASE_URL` 等 env 覆盖入仓 yaml 中的 `10.0.x.x` 占位符，避免把内网 IP 提交进 git。本 plan 是 Phase 3 `run` 子命令的配置依赖。

Output:
- `scripts/llm_client.py`（**追加** ~70 LOC）—— `_load_profiles_yaml(path)` 模块函数 + `LLMProfile.from_env(name=None, *, yaml_path=None)` 类方法；**不修改** Plan 02-01 已落地的任何 surface（AsyncLLMClient / LLMFailure / SchemaError / PATIENT_RECOMMENDATION_SCHEMA / `_EVIDENCE_LEVEL_ENUM` / `_DEFAULT_FEEDBACK_TEMPLATE` 字节级不动；仅追加新方法与新模块函数）
- `config/llm_profiles.yaml`（新建，入仓）—— 两个 profile 静态元数据
- `tests/test_llm_profile.py`（新建，~150 LOC）—— 7-9 个 case 覆盖 env-all-set / env+yaml fallback / yaml+default fallback / yaml missing / invalid timeout / lazy api_key / real-yaml-file 加载
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-llm-client-schema-unit-tests/02-CONTEXT.md
@.planning/phases/02-llm-client-schema-unit-tests/02-PATTERNS.md
@.planning/phases/02-llm-client-schema-unit-tests/02-01-PLAN.md
@docs/refactor_plan_2026-05-11.md
@scripts/llm_client.py
@scripts/kb_metadata.py
@requirements.txt

<interfaces>
<!-- Plan 02-01 已落地的契约（本 plan 必须保持字节级兼容）：scripts/llm_client.py 顶部 -->

@dataclass(frozen=True)
class LLMProfile:
    name: str
    base_url: str
    model: str
    api_key_env: str = "LLM_API_KEY"
    max_tokens: int = 4096
    temperature: float = 0.1
    timeout_s: int = 180
    structured_mode: str = "json_schema"
    concurrency: int = 5

# 本 plan 追加的契约（追加到 LLMProfile dataclass 末尾，frozen 不影响 classmethod）：

class LLMProfile:
    @classmethod
    def from_env(
        cls,
        name: str | None = None,
        *,
        yaml_path: "str | os.PathLike | None" = None,
    ) -> "LLMProfile":
        """从 env + yaml + 内置默认（三级优先级）构造 LLMProfile。

        Args:
            name: profile 名；为 None 时取 os.environ.get("LLM_PROFILE", "qwen3-vllm-lan")
            yaml_path: yaml 文件路径；为 None 时取 config/llm_profiles.yaml

        Resolution order per field: env > yaml[name] > dataclass default
        """
        ...

# 模块级函数（不挂 LLMProfile 静态方法，便于测试单独 patch）：
def _load_profiles_yaml(
    path: "str | os.PathLike | None" = None,
) -> dict[str, dict]:
    """读 yaml；不存在或解析失败返回 {}（env-only 路径下完全可工作）。

    Returns: {profile_name: {field: value, ...}}
    """
    ...

# 默认 yaml 路径常量
_DEFAULT_YAML_PATH: Path = Path("config/llm_profiles.yaml")

# env 变量名常量（便于测试引用，也便于 grep）：
_ENV_PROFILE = "LLM_PROFILE"
_ENV_BASE_URL = "LLM_BASE_URL"
_ENV_MODEL = "LLM_MODEL"
_ENV_API_KEY = "LLM_API_KEY"   # value 不进 dataclass；api_key_env 字段指向此变量名（默认）
_ENV_TIMEOUT = "LLM_TIMEOUT"
_ENV_STRUCTURED_MODE = "LLM_STRUCTURED_MODE"
_ENV_CONCURRENCY = "LLM_CONCURRENCY"

<!-- yaml 加载参考实现：scripts/kb_metadata.py:load_synonym_map（Phase 1 落地）-->
def load_synonym_map(path: Path = ...) -> dict:
    """yaml 不存在则返回 seed 默认；存在则 yaml.safe_load。"""
    if not path.exists():
        return SEED_DICT
    with path.open(encoding="utf-8") as f:
        return yaml.safe_load(f) or SEED_DICT
</interfaces>

<runtime_constraints>
- Python 3.9.6（与 Plan 02-01 一致）—— `from __future__ import annotations` 已在 Plan 02-01 文件顶部存在，本 plan 追加新方法时**保持现状**
- `pyyaml>=6.0` 已在 requirements.txt（Phase 1 + Plan 02-01 均已落地）
- **不允许**触碰 Plan 02-01 已落地的 surface（`AsyncLLMClient` 类、`LLMFailure`、`SchemaError`、`PATIENT_RECOMMENDATION_SCHEMA`、`_EVIDENCE_LEVEL_ENUM`、`_DEFAULT_FEEDBACK_TEMPLATE`）—— `git diff scripts/llm_client.py` 必须只显示**纯追加**（无 `-` 行除非是为了把新 import 放到 `from dataclasses import dataclass` 之后的字母序，或者在 `import os` 之后追加 `from pathlib import Path` —— 这些 import 调整必须最小化并在 acceptance_criteria 中显式允许）
- `LLMProfile` 是 `@dataclass(frozen=True)`，本 plan 给它**追加 classmethod** 不影响 frozen（PEP 557 frozen 仅限实例字段写入，类层 method 任意添加）
- `_load_profiles_yaml` 必须**对解析异常防御**：`yaml.YAMLError` 也返回 `{}`，避免运维侧 yaml 手抖把整个 from_env 弄崩
- 入仓 yaml **绝不**含 api_key 明文，**绝不**含真实内网 IP（仅 `10.0.x.x` 占位符）
</runtime_constraints>
</context>

<critical_conflict_resolution>
**Wave 归属**（CONTEXT.md / 用户输入对 Wave 1 vs Wave 2 描述含糊，本 plan 锁定 Wave 2）：

- 用户 prompt 给出两种选项：(A) 把 LLMProfile 全放 02-01 → 02-02 仅追加 `from_env`，Wave 都是 1；(B) 02-02 在 02-01 之后顺序执行。
- **本 plan 实施层决策**：采取 Option A 的**结构** + Option B 的**波次**：
  - 02-01 已经定义 `LLMProfile` dataclass 全部字段（structural ownership 归属 02-01）
  - 02-02 仅向同一文件追加 classmethod 与模块函数 + 新建 yaml + 新建测试文件 —— 这是**纯追加**操作
  - 但因为 02-02 修改的文件 `scripts/llm_client.py` 与 02-01 完全重叠（`files_modified` 字段交集非空），按 GSD 计划器 dependency graph 规则「files_modified overlap → forces later wave」，**02-02 必须置于 Wave 2，depends_on=["02-01"]**
- **trade-off 公开**：这让 Wave 1 只有 02-01 单条 plan，并发收益为 0；但避免了同一文件两人并发写造成 merge conflict。Phase 2 整体 2 plans 顺序执行，符合 ROADMAP 估时 2d。
- 替代方案（不采纳）：拆 `scripts/llm_client.py` 为两个文件（`llm_client.py` + `llm_config.py`），强行并发 —— 拆分代价（import 链 + Phase 3 调用方需要双 import）大于并发收益。
</critical_conflict_resolution>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 config/llm_profiles.yaml（入仓）</name>
  <read_first>
    - CONTEXT.md D-12 yaml 样例（已读）
    - PATTERNS.md §2.2 yaml 内容 + 入仓决策（已读）
    - `ls config/ 2>/dev/null`（已知：目录不存在，本 task 同时创建目录与文件）
  </read_first>
  <files>
    - config/llm_profiles.yaml（新建）
  </files>
  <action>
    1. **创建 `config/` 目录**（仓库现无此目录）。
    2. **新建 `config/llm_profiles.yaml`**，内容按 CONTEXT.md D-12 字面落库（**不**改动任何字段名/值/缩进）：
       ```yaml
       # Medical Guidelines Suite — LLM Profiles
       # 入仓策略：仅元数据（base_url / model / timeout 等），不含 secret。
       # `api_key_env` 指向 env 变量名；运维侧通过 LLM_BASE_URL 覆盖 `10.0.x.x` 占位符。

       profiles:
         qwen3-vllm-lan:
           base_url: http://10.0.x.x:8000/v1
           model: Qwen3.5-35B-A3B
           api_key_env: LLM_API_KEY
           timeout_s: 180
           structured_mode: json_schema
           concurrency: 5
         deepseek-cloud:
           base_url: https://api.deepseek.com/v1
           model: deepseek-chat
           api_key_env: DEEPSEEK_API_KEY
           timeout_s: 60
           structured_mode: json_object
           concurrency: 10
       ```
    3. 检查 `.gitignore`，确认 `config/` 目录**未被** ignore（仓库现 `.gitignore` 中无 `config/` 条目）。若发现被 ignore，**不要**修改 .gitignore，改为在本 task 报错并暂停，提示用户决策。
    4. 不要再创建 `config/README.md` 或 `config/.gitkeep`（yaml 本身就是 tracked 文件）。
  </action>
  <verify>
    <automated>test -f config/llm_profiles.yaml && python3 -c "import yaml; d = yaml.safe_load(open('config/llm_profiles.yaml')); assert 'profiles' in d; assert 'qwen3-vllm-lan' in d['profiles']; assert 'deepseek-cloud' in d['profiles']; assert d['profiles']['qwen3-vllm-lan']['model'] == 'Qwen3.5-35B-A3B'; assert d['profiles']['deepseek-cloud']['api_key_env'] == 'DEEPSEEK_API_KEY'; assert 'api_key' not in d['profiles']['qwen3-vllm-lan']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f config/llm_profiles.yaml` 退出码 0
    - `python3 -c "import yaml; yaml.safe_load(open('config/llm_profiles.yaml'))"` 退出码 0（合法 yaml）
    - `python3 -c "import yaml; d = yaml.safe_load(open('config/llm_profiles.yaml')); assert set(d['profiles'].keys()) == {'qwen3-vllm-lan', 'deepseek-cloud'}"` 退出码 0
    - `grep -v '^#' config/llm_profiles.yaml | grep -c "api_key:"` 输出 `0`（**绝不**含明文 api_key 字段；只允许 api_key_env 字段名）
    - `grep -c "api_key_env" config/llm_profiles.yaml` 输出 `2`（两个 profile 各一条）
    - `grep -c "10.0.x.x" config/llm_profiles.yaml` 输出 `1`（占位符，禁止替换为真实 IP）
    - `git check-ignore config/llm_profiles.yaml` 退出码非 0（即文件未被 ignore，可入仓）
    - `python3 -m pytest tests/ -q --no-header 2>&1 | tail -2` 整体测试通过数与基线（195）持平 —— 本 task 不引入测试
    - **git diff scripts/ tests/ 输出为空**（本 task 不动 scripts/ / tests/）
  </acceptance_criteria>
  <done>config/llm_profiles.yaml 入仓，含 qwen3-vllm-lan + deepseek-cloud 两 profile；不含明文 api_key；可被 yaml.safe_load 解析；git 不忽略</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 追加 LLMProfile.from_env classmethod 与 _load_profiles_yaml 模块函数到 scripts/llm_client.py</name>
  <read_first>
    - `scripts/llm_client.py` 全文（Plan 02-01 已落地，确认 `LLMProfile` dataclass 当前形态、import 顺序）
    - `scripts/kb_metadata.py` 中 yaml 加载实现（Phase 1 落地的 `load_synonym_map`，模式参考）
    - CONTEXT.md D-11 + D-12（已读）—— env > yaml > default 三级优先级语义
    - PATTERNS.md §2.2 `_load_profiles_yaml` 骨架代码（已读）
  </read_first>
  <behavior>
    本 task 给 `scripts/llm_client.py` 追加新行为，必须满足：

    - **C-1 from_env 全 env 覆盖**：7 个 env 全设 → LLMProfile 字段一一对应 env 值（int 字段经 `int(...)` 强制类型）
    - **C-2 from_env 部分 env + yaml fallback**：仅 `LLM_PROFILE=qwen3-vllm-lan` 设，其余 env 未设；yaml 存在该 profile dict → 缺失字段取 yaml 值
    - **C-3 from_env yaml + default fallback**：env 仅设 LLM_PROFILE；yaml 该 profile dict 缺 `timeout_s` 字段 → timeout_s 取 dataclass 默认值 180
    - **C-4 yaml missing → 全 default**：env 仅设 LLM_PROFILE，yaml 文件不存在 → 除 name 外字段全部 dataclass 默认（但 `base_url` / `model` 是 required 字段无默认 —— 此时 from_env 应抛 `ValueError("base_url is required: set LLM_BASE_URL env or define in yaml")`，避免构造出半残实例）
    - **C-5 LLM_TIMEOUT 非整数 → ValueError**：`LLM_TIMEOUT=abc` → 抛 `ValueError`（不静默 fallback）
    - **C-6 LLM_CONCURRENCY 非整数 → ValueError**：同 C-5
    - **C-7 api_key 懒解析**：from_env 创建的 profile.api_key_env == "LLM_API_KEY"（默认）或 env LLM_API_KEY_ENV 覆盖时取覆盖值；profile 实例 `repr()` 不含任何明文 key 值
    - **C-8 `_load_profiles_yaml` 文件不存在 → {}**
    - **C-9 `_load_profiles_yaml` 解析失败（malformed yaml）→ {}**（防御性，CONTEXT.md `<code_context>` 建立的 pattern）
    - **C-10 `_load_profiles_yaml` 真实 `config/llm_profiles.yaml` → 含两 profile key**

    **关键决策（critical_conflict_resolution）**：
    - LLM_API_KEY 是**敏感值**，不进 from_env 的字段映射 —— 它的 value 由 AsyncLLMClient._auth_headers property 懒解析（Plan 02-01 已落地）；本 task 只负责把 `api_key_env` 字段指针写对（**默认** `"LLM_API_KEY"`；yaml 中 profile.api_key_env 字段可覆盖为 `"DEEPSEEK_API_KEY"` 等）
    - 引入新 env 变量 `LLM_API_KEY_ENV`（**指针的指针**）→ 用户可通过 env 覆盖 yaml 中的 api_key_env 字段：
      - env 优先级最高：若 `LLM_API_KEY_ENV=CUSTOM_KEY` 设，则 profile.api_key_env="CUSTOM_KEY"
      - 否则取 yaml profile.api_key_env
      - 否则 dataclass 默认 "LLM_API_KEY"
      —— 这是对 CFG-01「7 个 LLM_* env wired」的诚实解释：LLM_API_KEY 的**值**通过 AsyncLLMClient 懒解析消费（Plan 02-01），LLM_API_KEY_ENV 作为**指针覆盖**通过 LLMProfile.from_env 消费（本 plan）。两条路径合起来覆盖 CFG-01 的语义。
  </behavior>
  <files>
    - scripts/llm_client.py（追加 ~70 LOC）
  </files>
  <action>
    1. **审查 `scripts/llm_client.py` 现有 import**：Plan 02-01 已 import `asyncio` / `json` / `os` / `httpx` / `jsonschema` / `typing.Callable` / `dataclasses.dataclass`。本 task 需要追加：
       - `from pathlib import Path`
       - `import yaml`
       - `from typing import Any`（若 yaml dict 类型注解需要 —— 可不用，使用 `dict` 即可）

       若已有 import 风格是「标准库分组 + 三方库分组」，本 task 在对应分组内插入新 import 时保持字母序。**允许**的 import 区域调整（不算 surface 修改）：在 `import os` 之后追加 `from pathlib import Path`；在 `import jsonschema` 后追加 `import yaml`（字母序之后）。

    2. **在 `LLMProfile` dataclass 之后、`AsyncLLMClient` 类之前**追加模块常量：
       ```python
       _DEFAULT_YAML_PATH = Path("config/llm_profiles.yaml")

       _ENV_PROFILE = "LLM_PROFILE"
       _ENV_BASE_URL = "LLM_BASE_URL"
       _ENV_MODEL = "LLM_MODEL"
       _ENV_API_KEY_ENV = "LLM_API_KEY_ENV"   # 指针的指针：覆盖 profile.api_key_env 字段；
                                              # LLM_API_KEY 的 value 由 AsyncLLMClient._auth_headers
                                              # 在请求时 os.environ.get(api_key_env, "") 懒解析消费
       _ENV_TIMEOUT = "LLM_TIMEOUT"
       _ENV_STRUCTURED_MODE = "LLM_STRUCTURED_MODE"
       _ENV_CONCURRENCY = "LLM_CONCURRENCY"

       _DEFAULT_PROFILE_NAME = "qwen3-vllm-lan"
       ```

    3. **追加 `_load_profiles_yaml` 模块函数**（在常量后、AsyncLLMClient 类前）：
       ```python
       def _load_profiles_yaml(
           path: "str | os.PathLike | None" = None,
       ) -> dict:
           """读 yaml；文件不存在或解析失败均返回 {}（env-only 路径可独立工作）。

           Returns: {profile_name: {field: value, ...}}
           """
           p = Path(path) if path is not None else _DEFAULT_YAML_PATH
           if not p.exists():
               return {}
           try:
               with p.open(encoding="utf-8") as f:
                   data = yaml.safe_load(f) or {}
           except yaml.YAMLError:
               return {}
           if not isinstance(data, dict):
               return {}
           profiles = data.get("profiles", {})
           return profiles if isinstance(profiles, dict) else {}
       ```

    4. **在 `LLMProfile` dataclass 末尾追加 `from_env` classmethod**（追加方式：把 dataclass 的现有定义保留不动，仅在其方法区追加 classmethod —— frozen=True 不影响 classmethod）：
       ```python
       @classmethod
       def from_env(
           cls,
           name: str | None = None,
           *,
           yaml_path: "str | os.PathLike | None" = None,
       ) -> "LLMProfile":
           """构造 LLMProfile，优先级 env > yaml > 内置默认。

           Args:
               name: profile 名；None 时取 os.environ.get("LLM_PROFILE", "qwen3-vllm-lan")
               yaml_path: yaml 文件路径；None 时取 config/llm_profiles.yaml

           Raises:
               ValueError: LLM_TIMEOUT / LLM_CONCURRENCY 非整数；或 base_url / model
                            既无 env 也无 yaml 提供（required 字段无默认）
           """
           env = os.environ
           resolved_name = name if name is not None else env.get(_ENV_PROFILE, _DEFAULT_PROFILE_NAME)
           yaml_profiles = _load_profiles_yaml(yaml_path)
           yaml_dict = yaml_profiles.get(resolved_name, {}) if isinstance(yaml_profiles, dict) else {}

           def _pick_str(env_key, yaml_key, default=None):
               v = env.get(env_key)
               if v is not None:
                   return v
               if yaml_key in yaml_dict and yaml_dict[yaml_key] is not None:
                   return yaml_dict[yaml_key]
               return default

           def _pick_int(env_key, yaml_key, default):
               v = env.get(env_key)
               if v is not None:
                   # 非静默 fallback：解析失败抛 ValueError（CONTEXT.md `<code_context>` 已锁定）
                   return int(v)
               if yaml_key in yaml_dict and yaml_dict[yaml_key] is not None:
                   return int(yaml_dict[yaml_key])
               return default

           base_url = _pick_str(_ENV_BASE_URL, "base_url")
           model = _pick_str(_ENV_MODEL, "model")
           api_key_env = _pick_str(_ENV_API_KEY_ENV, "api_key_env", default="LLM_API_KEY")
           structured_mode = _pick_str(_ENV_STRUCTURED_MODE, "structured_mode", default="json_schema")
           timeout_s = _pick_int(_ENV_TIMEOUT, "timeout_s", default=180)
           concurrency = _pick_int(_ENV_CONCURRENCY, "concurrency", default=5)

           if base_url is None:
               raise ValueError(
                   f"base_url is required: set {_ENV_BASE_URL} env or define "
                   f"profiles.{resolved_name}.base_url in {yaml_path or _DEFAULT_YAML_PATH}"
               )
           if model is None:
               raise ValueError(
                   f"model is required: set {_ENV_MODEL} env or define "
                   f"profiles.{resolved_name}.model in {yaml_path or _DEFAULT_YAML_PATH}"
               )

           return cls(
               name=resolved_name,
               base_url=base_url,
               model=model,
               api_key_env=api_key_env,
               timeout_s=timeout_s,
               structured_mode=structured_mode,
               concurrency=concurrency,
           )
       ```

       **说明**：`max_tokens` 和 `temperature` 当前**不**被 env / yaml 覆盖路径消费（保持 dataclass 默认 4096 / 0.1）。CONTEXT.md D-10 字段表中包含这两项，但 CFG-01 只要求 7 个 LLM_* env wired —— max_tokens / temperature 不在 7 项中，因此本 task 不为它们加 env wiring。Plan 02-02 严格按 CONTEXT.md `<decisions>` 字面落地。如未来需要可在 v3.2 追加 `LLM_MAX_TOKENS` / `LLM_TEMPERATURE`，本 plan 不做。

    5. **不要触碰** Plan 02-01 落地的 `AsyncLLMClient` / `LLMFailure` / `SchemaError` / `PATIENT_RECOMMENDATION_SCHEMA` / `_EVIDENCE_LEVEL_ENUM` / `_DEFAULT_FEEDBACK_TEMPLATE` —— 验证手段：`git diff scripts/llm_client.py | grep -E "^-" | grep -v "^---"` 输出应仅含「为了在已有 import 区追加 `from pathlib import Path` 和 `import yaml` 而移动空行」的零星变更（< 5 行 `-`），**绝不**含任何 class/function/常量 body 删除。
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, '.'); from scripts.llm_client import LLMProfile, _load_profiles_yaml, _DEFAULT_YAML_PATH, AsyncLLMClient, PATIENT_RECOMMENDATION_SCHEMA, _EVIDENCE_LEVEL_ENUM, LLMFailure, SchemaError, _DEFAULT_FEEDBACK_TEMPLATE; import inspect; assert hasattr(LLMProfile, 'from_env'); assert inspect.ismethod(LLMProfile.from_env) or callable(LLMProfile.from_env); assert callable(_load_profiles_yaml); assert len(_EVIDENCE_LEVEL_ENUM) == 23; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from scripts.llm_client import LLMProfile; assert hasattr(LLMProfile, 'from_env'); assert callable(LLMProfile.from_env)"` 退出码 0
    - `python3 -c "from scripts.llm_client import _load_profiles_yaml; assert _load_profiles_yaml('/nonexistent/path.yaml') == {}"` 退出码 0
    - `python3 -c "from scripts.llm_client import _load_profiles_yaml; d = _load_profiles_yaml('config/llm_profiles.yaml'); assert 'qwen3-vllm-lan' in d and 'deepseek-cloud' in d"` 退出码 0
    - `python3 -c "import os; from scripts.llm_client import LLMProfile; os.environ['LLM_PROFILE'] = 'qwen3-vllm-lan'; os.environ.pop('LLM_BASE_URL', None); os.environ.pop('LLM_MODEL', None); p = LLMProfile.from_env(); assert p.name == 'qwen3-vllm-lan'; assert p.base_url.startswith('http://'); assert p.model == 'Qwen3.5-35B-A3B'"` 退出码 0（验 yaml fallback 路径）
    - `python3 -c "import os; from scripts.llm_client import LLMProfile; os.environ['LLM_PROFILE'] = 'nonexistent'; os.environ.pop('LLM_BASE_URL', None); os.environ.pop('LLM_MODEL', None);\\nfrom scripts.llm_client import LLMProfile;\\ntry: LLMProfile.from_env()\\nexcept ValueError as e: assert 'base_url is required' in str(e); print('OK')"` 退出码 0
    - `grep -c "def from_env" scripts/llm_client.py` 输出 `1`
    - `grep -c "def _load_profiles_yaml" scripts/llm_client.py` 输出 `1`
    - `grep -c "import yaml" scripts/llm_client.py` 输出 `1`
    - `grep -c "from pathlib import Path" scripts/llm_client.py` 输出 `1`
    - **`python3 -c "from scripts.llm_client import AsyncLLMClient, PATIENT_RECOMMENDATION_SCHEMA, _EVIDENCE_LEVEL_ENUM, LLMFailure, SchemaError, _DEFAULT_FEEDBACK_TEMPLATE; assert len(_EVIDENCE_LEVEL_ENUM) == 23"` 退出码 0**（Plan 02-01 已落地 surface 完整保留）
    - **`python3 -m pytest tests/test_llm_client.py -q 2>&1 | tail -2` 全绿**（Plan 02-01 测试零回归 —— 本 task 仅追加，不改已有方法）
    - **`python3 -m pytest tests/test_retriever.py tests/test_retriever_async.py tests/test_kb_metadata.py -q 2>&1 | tail -2` 全绿**（Phase 1 基线零回归）
    - `python3 -c "import ast; ast.parse(open('scripts/llm_client.py').read())"` 退出码 0（语法）
  </acceptance_criteria>
  <done>scripts/llm_client.py 含 LLMProfile.from_env classmethod + _load_profiles_yaml 模块函数；Plan 02-01 surface 字节级保留（AsyncLLMClient/LLMFailure/SchemaError/PATIENT_RECOMMENDATION_SCHEMA/23-enum/feedback template 不变）；Plan 02-01 测试 + Phase 1 测试零回归</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/test_llm_profile.py 覆盖 from_env + yaml 加载 + 优先级 + 错误路径</name>
  <read_first>
    - `tests/test_llm_client.py` 全文（Plan 02-01 落地的 fixture 风格）
    - `tests/test_kb_metadata.py`（Phase 1 yaml 加载测试参考）
    - PATTERNS.md §2.3 表 2（CFG-01/02 测试 case 清单）
    - Task 2 已交付的 `LLMProfile.from_env` 与 `_load_profiles_yaml` 完整契约
  </read_first>
  <files>
    - tests/test_llm_profile.py（新建，~150 LOC，9 case）
  </files>
  <action>
    1. **新建 `tests/test_llm_profile.py`**：
       ```python
       """Tests for LLMProfile.from_env + _load_profiles_yaml: env/yaml/default priority."""

       from __future__ import annotations

       import os
       from pathlib import Path

       import pytest

       from scripts.llm_client import (
           LLMProfile,
           _DEFAULT_YAML_PATH,
           _load_profiles_yaml,
       )


       _ALL_LLM_ENV = [
           "LLM_PROFILE", "LLM_BASE_URL", "LLM_MODEL", "LLM_API_KEY_ENV",
           "LLM_TIMEOUT", "LLM_STRUCTURED_MODE", "LLM_CONCURRENCY", "LLM_API_KEY",
       ]


       @pytest.fixture(autouse=True)
       def _clean_env(monkeypatch):
           """每个测试隔离 LLM_* env，避免外部 shell 污染。"""
           for var in _ALL_LLM_ENV:
               monkeypatch.delenv(var, raising=False)


       @pytest.fixture
       def yaml_with_two_profiles(tmp_path):
           """临时 yaml 文件，含 qwen3-vllm-lan + deepseek-cloud 两 profile."""
           p = tmp_path / "llm_profiles.yaml"
           p.write_text(
               "profiles:\\n"
               "  qwen3-vllm-lan:\\n"
               "    base_url: http://yaml-host:8000/v1\\n"
               "    model: yaml-model\\n"
               "    api_key_env: YAML_KEY\\n"
               "    timeout_s: 120\\n"
               "    structured_mode: json_schema\\n"
               "    concurrency: 3\\n"
               "  partial:\\n"
               "    base_url: http://partial/v1\\n"
               "    model: partial-model\\n"
               "    # 缺 timeout_s / concurrency，验证 fallback 到 dataclass 默认\\n",
               encoding="utf-8",
           )
           return p


       # ---- CFG-01: env 优先级 ----

       def test_llm_profile_from_env_all_set(monkeypatch, tmp_path):
           """C-1: 全部 LLM_* env 设 → 字段一一对应 env 值，yaml 不被读。"""
           monkeypatch.setenv("LLM_PROFILE", "custom")
           monkeypatch.setenv("LLM_BASE_URL", "http://env-host/v1")
           monkeypatch.setenv("LLM_MODEL", "env-model")
           monkeypatch.setenv("LLM_API_KEY_ENV", "MY_KEY")
           monkeypatch.setenv("LLM_TIMEOUT", "300")
           monkeypatch.setenv("LLM_STRUCTURED_MODE", "json_object")
           monkeypatch.setenv("LLM_CONCURRENCY", "20")
           # 即便指向不存在的 yaml，env 全设也应工作
           p = LLMProfile.from_env(yaml_path=tmp_path / "nope.yaml")
           assert p.name == "custom"
           assert p.base_url == "http://env-host/v1"
           assert p.model == "env-model"
           assert p.api_key_env == "MY_KEY"
           assert p.timeout_s == 300
           assert p.structured_mode == "json_object"
           assert p.concurrency == 20


       def test_llm_profile_env_overrides_yaml(monkeypatch, yaml_with_two_profiles):
           """C-2 part: env LLM_BASE_URL 覆盖 yaml 中的 base_url."""
           monkeypatch.setenv("LLM_PROFILE", "qwen3-vllm-lan")
           monkeypatch.setenv("LLM_BASE_URL", "http://env-wins/v1")
           p = LLMProfile.from_env(yaml_path=yaml_with_two_profiles)
           assert p.base_url == "http://env-wins/v1"      # env wins
           assert p.model == "yaml-model"                  # yaml fills the gap


       # ---- CFG-02: yaml fallback ----

       def test_llm_profile_from_env_partial_falls_back_to_yaml(monkeypatch, yaml_with_two_profiles):
           """C-2: 仅 LLM_PROFILE 设，其余从 yaml 取."""
           monkeypatch.setenv("LLM_PROFILE", "qwen3-vllm-lan")
           p = LLMProfile.from_env(yaml_path=yaml_with_two_profiles)
           assert p.name == "qwen3-vllm-lan"
           assert p.base_url == "http://yaml-host:8000/v1"
           assert p.model == "yaml-model"
           assert p.api_key_env == "YAML_KEY"
           assert p.timeout_s == 120
           assert p.structured_mode == "json_schema"
           assert p.concurrency == 3


       def test_llm_profile_yaml_falls_back_to_default(monkeypatch, yaml_with_two_profiles):
           """C-3: yaml 该 profile 缺 timeout_s/concurrency → dataclass 默认值."""
           monkeypatch.setenv("LLM_PROFILE", "partial")
           p = LLMProfile.from_env(yaml_path=yaml_with_two_profiles)
           assert p.timeout_s == 180        # dataclass default
           assert p.concurrency == 5         # dataclass default
           assert p.api_key_env == "LLM_API_KEY"  # dataclass default
           assert p.structured_mode == "json_schema"  # dataclass default
           # base_url / model 由 yaml 提供
           assert p.base_url == "http://partial/v1"


       def test_load_profiles_yaml_missing_returns_empty(tmp_path):
           """C-8: 文件不存在 → {} (不抛错)."""
           assert _load_profiles_yaml(tmp_path / "nope.yaml") == {}


       def test_load_profiles_yaml_malformed_returns_empty(tmp_path):
           """C-9: malformed yaml → {} (防御)."""
           p = tmp_path / "bad.yaml"
           p.write_text("profiles:\\n  - this is not a dict\\n: bad: indent", encoding="utf-8")
           result = _load_profiles_yaml(p)
           assert result == {}


       def test_load_profiles_yaml_real_file():
           """C-10: 加载入仓的 config/llm_profiles.yaml → 含两 profile."""
           # 仅当文件存在时跑（Task 1 已交付，本测试在 PR 集成阶段必须有 config/llm_profiles.yaml）
           if not _DEFAULT_YAML_PATH.exists():
               pytest.skip("config/llm_profiles.yaml not present in working tree")
           d = _load_profiles_yaml(_DEFAULT_YAML_PATH)
           assert set(d.keys()) == {"qwen3-vllm-lan", "deepseek-cloud"}
           assert d["qwen3-vllm-lan"]["model"] == "Qwen3.5-35B-A3B"
           assert d["deepseek-cloud"]["api_key_env"] == "DEEPSEEK_API_KEY"


       # ---- 错误路径 ----

       def test_llm_timeout_invalid_raises(monkeypatch, yaml_with_two_profiles):
           """C-5: LLM_TIMEOUT=abc → ValueError, 非静默 fallback."""
           monkeypatch.setenv("LLM_PROFILE", "qwen3-vllm-lan")
           monkeypatch.setenv("LLM_TIMEOUT", "abc")
           with pytest.raises(ValueError):
               LLMProfile.from_env(yaml_path=yaml_with_two_profiles)


       def test_llm_concurrency_invalid_raises(monkeypatch, yaml_with_two_profiles):
           """C-6: LLM_CONCURRENCY=xyz → ValueError."""
           monkeypatch.setenv("LLM_PROFILE", "qwen3-vllm-lan")
           monkeypatch.setenv("LLM_CONCURRENCY", "xyz")
           with pytest.raises(ValueError):
               LLMProfile.from_env(yaml_path=yaml_with_two_profiles)


       def test_llm_profile_required_field_missing(monkeypatch, tmp_path):
           """C-4: 既无 env 也无 yaml 提供 base_url/model → ValueError."""
           monkeypatch.setenv("LLM_PROFILE", "nonexistent")
           # 指向空 yaml
           empty = tmp_path / "empty.yaml"
           empty.write_text("profiles: {}\\n", encoding="utf-8")
           with pytest.raises(ValueError, match="base_url is required"):
               LLMProfile.from_env(yaml_path=empty)


       # ---- 安全 / 懒解析 ----

       def test_api_key_not_in_profile_repr(monkeypatch, yaml_with_two_profiles):
           """C-7: profile.repr() 不含明文 api_key 值（即便 LLM_API_KEY 设为 sk-xxx）."""
           monkeypatch.setenv("LLM_PROFILE", "qwen3-vllm-lan")
           monkeypatch.setenv("LLM_API_KEY", "sk-leak-test")
           p = LLMProfile.from_env(yaml_path=yaml_with_two_profiles)
           assert "sk-leak-test" not in repr(p)
           # 仅存 api_key_env 字段（指针），值由 AsyncLLMClient._auth_headers 懒解析
           assert p.api_key_env == "YAML_KEY"   # yaml 中该 profile 写的 api_key_env
       ```

    2. 不要新建 `conftest.py` 改动 —— pytest-asyncio strict mode 已在 Phase 1 启用，本测试文件全 sync case 无需 asyncio decorator。
    3. 不要触碰 `tests/test_llm_client.py`（Plan 02-01 落地的 16 case 字节级保留）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_llm_profile.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_llm_profile.py -v 2>&1 | tail -3` 末行匹配 `1[1-2] passed`（11-12 case 全绿）
    - `python3 -m pytest tests/ -q --no-header 2>&1 | tail -2` 整体测试通过数 ≥ 基线（195 from Plan 02-01）+ 11 = 206
    - `grep -c "def test_" tests/test_llm_profile.py` ≥ 11
    - `grep -c "monkeypatch" tests/test_llm_profile.py` ≥ 8
    - `grep -c "from scripts.llm_client import" tests/test_llm_profile.py` ≥ 1
    - **`python3 -m pytest tests/test_llm_client.py -q 2>&1 | tail -2` 全绿**（Plan 02-01 16 case 零回归）
    - **`python3 -m pytest tests/test_retriever.py tests/test_retriever_async.py tests/test_kb_metadata.py tests/test_qmd_integration.py -q 2>&1 | tail -2` 全绿**（Phase 1 基线零回归）
    - **Nyquist 验证矩阵**：
      | Req | Test case | 观察什么 |
      |-----|-----------|---------|
      | LLM-03 | `test_llm_profile_from_env_partial_falls_back_to_yaml` + `test_load_profiles_yaml_real_file` | qwen3-vllm-lan + deepseek-cloud 两 profile 可加载 |
      | CFG-01 | `test_llm_profile_from_env_all_set` | 7 个 env 全 wired |
      | CFG-01 | `test_llm_timeout_invalid_raises` + `test_llm_concurrency_invalid_raises` | int 字段非静默 |
      | CFG-02 | `test_llm_profile_from_env_partial_falls_back_to_yaml` + `test_llm_profile_yaml_falls_back_to_default` + `test_llm_profile_env_overrides_yaml` | 三级优先级（env > yaml > default）观察性触发 |
      | CFG-02 | `test_load_profiles_yaml_missing_returns_empty` + `test_load_profiles_yaml_malformed_returns_empty` | yaml 加载防御性 |
      | CFG-02 | `test_load_profiles_yaml_real_file` | 入仓 yaml 文件可解析（端到端验证 Task 1 落库内容 + Task 2 加载逻辑） |
  </acceptance_criteria>
  <done>tests/test_llm_profile.py 落地，11+ case 全绿；三级优先级 env > yaml > default 观察性验证；yaml 加载防御（missing / malformed）验证；LLM_TIMEOUT/CONCURRENCY 非整数抛 ValueError 验证；api_key 不入 repr 验证；Plan 02-01 + Phase 1 测试零回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLMProfile.from_env → os.environ | 进程级 env 是攻击面（`ps e` 可见）；本 plan 不存 LLM_API_KEY 的**值**，仅存 `api_key_env` **指针** |
| _load_profiles_yaml → 文件系统 | yaml 路径由调用方传入或默认 `config/llm_profiles.yaml`；yaml.safe_load 防御反序列化攻击 |
| config/llm_profiles.yaml → git 仓库 | 文件入仓，必须**严格不含** secret；只允许 base_url / model / api_key_env 指针 / timeout / mode / concurrency |
| LLM_API_KEY_ENV → 任意 env 变量名 | 用户可指向任意 env 变量名（指针），api_key 值在 AsyncLLMClient._auth_headers 内消费 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | api_key 明文进 yaml 入仓导致 git 历史泄露 | mitigate | Task 1 acceptance：`grep -v '^#' config/llm_profiles.yaml \\| grep -c "api_key:"` 必须为 0；yaml 仅含 `api_key_env` 字段名（指针），值在 env |
| T-02-02-02 | Information Disclosure | profile.repr() 含 api_key 明文 | mitigate | LLMProfile dataclass 字段**不**存 key 值；C-7 测试 `assert "sk-leak-test" not in repr(p)` 显式验证 |
| T-02-02-03 | Tampering | yaml 反序列化 RCE | mitigate | 使用 `yaml.safe_load`（禁用 Python 对象构造）；`_load_profiles_yaml` 对非 dict 返回值也防御性返回 {} |
| T-02-02-04 | Denial of Service | malformed yaml 让 from_env 整体崩溃 | mitigate | `_load_profiles_yaml` 捕获 `yaml.YAMLError` 返回 {}（C-9 测试） |
| T-02-02-05 | Spoofing | LLM_API_KEY_ENV 指向恶意 env（如 PATH） | accept | 信任本地用户；指针指向任意 env 是设计意图（支持 DEEPSEEK_API_KEY 等多 profile）；攻击者已能控制 env 时无更高威胁 |
| T-02-02-06 | Tampering | 入仓 yaml 中 base_url 被改成攻击者控制的 endpoint | accept | git review 是常规防御；运维侧 env LLM_BASE_URL 优先级最高，可在 deployment 时强制覆盖 |
| T-02-02-07 | Information Disclosure | LLM_TIMEOUT=abc 的 ValueError trace 含 env 值 | accept | env 值由用户自己设置，不构成 trust boundary 跨越 |
| T-02-02-08 | Elevation of Privilege | _load_profiles_yaml 读任意 path（如 /etc/passwd） | mitigate | path 仅由调用方传入；默认 `config/llm_profiles.yaml` 相对路径；yaml.safe_load 不执行任意 yaml 标签构造 |
</threat_model>

<verification>
- `python3 -m pytest tests/test_llm_profile.py tests/test_llm_client.py -v` 全 PASS
- `python3 -m pytest tests/ --no-header -q 2>&1 | tail -2` 整体测试通过数 ≥ 206（Phase 1 + Plan 02-01 + Plan 02-02）
- `python3 -c "from scripts.llm_client import LLMProfile, _load_profiles_yaml, AsyncLLMClient, PATIENT_RECOMMENDATION_SCHEMA, _EVIDENCE_LEVEL_ENUM, LLMFailure, SchemaError, _DEFAULT_FEEDBACK_TEMPLATE; assert len(_EVIDENCE_LEVEL_ENUM) == 23; assert hasattr(LLMProfile, 'from_env')"` 退出码 0
- `test -f config/llm_profiles.yaml && python3 -c "import yaml; yaml.safe_load(open('config/llm_profiles.yaml'))"` 退出码 0
- `grep -v '^#' config/llm_profiles.yaml | grep -cE "^[[:space:]]*api_key:"` 输出 `0`（**无**明文 api_key 字段，只有 api_key_env 字段名）
- `git diff scripts/retriever.py scripts/batch_pipeline.py scripts/kb_metadata.py | wc -l` 输出 `0`（Phase 1 模块零修改）
</verification>

<success_criteria>
- ROADMAP.md Phase 2 Success Criterion #3 达成：`LLM_PROFILE / LLM_BASE_URL / LLM_MODEL / LLM_API_KEY / LLM_TIMEOUT / LLM_STRUCTURED_MODE / LLM_CONCURRENCY` 全部 env wired（LLM_API_KEY 的值通过 AsyncLLMClient._auth_headers 在请求时消费 —— Plan 02-01 已落地；其余 6 项进入 LLMProfile 字段 —— 本 plan 落地。LLM_API_KEY_ENV 作为指针覆盖也落地）；可选 config/llm_profiles.yaml 支持 qwen3-vllm-lan / deepseek-cloud 两 profile，env 优先级 > yaml
- LLM-03: ✓ LLMProfile 数据类支持多 profile（qwen3-vllm-lan / deepseek-cloud），from_env 按 name 索引 yaml profile（`test_load_profiles_yaml_real_file` + `test_llm_profile_from_env_partial_falls_back_to_yaml`）
- CFG-01: ✓ 7 个 LLM_* env 全部 wired —— LLM_PROFILE / LLM_BASE_URL / LLM_MODEL / LLM_API_KEY_ENV / LLM_TIMEOUT / LLM_STRUCTURED_MODE / LLM_CONCURRENCY 进 LLMProfile.from_env（本 plan）；LLM_API_KEY 的值进 AsyncLLMClient._auth_headers 懒解析（Plan 02-01）
- CFG-02: ✓ `config/llm_profiles.yaml` 入仓，含 qwen3-vllm-lan + deepseek-cloud；env 优先级 > yaml > 内置默认（`test_llm_profile_env_overrides_yaml` + `test_llm_profile_yaml_falls_back_to_default` 双向验证）
</success_criteria>

<output>
After completion, create `.planning/phases/02-llm-client-schema-unit-tests/02-02-SUMMARY.md` recording:
- LLMProfile.from_env 三级优先级语义（env > yaml > dataclass default）落定
- 7 个 LLM_* env 变量 + 1 个 LLM_API_KEY_ENV 指针覆盖共 8 个 env 入口的最终 wiring 表
- yaml 入仓策略（仅元数据 / 占位符 IP / 不含 api_key 明文）
- _load_profiles_yaml 防御性设计（FileNotFoundError → {} / YAMLError → {} / 非 dict 数据 → {}）
- Plan 02-01 surface 字节级保留的回归验证结论
- Phase 3 接入提示：`profile = LLMProfile.from_env(name=args.llm_profile)`；`client = AsyncLLMClient(profile, http=shared_httpx_client)`
- Phase 2 整体达成：7/7 requirements（LLM-01..05 + CFG-01/02）观察性 Nyquist 验证
</output>
</content>
</invoke>