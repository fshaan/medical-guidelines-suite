---
phase: 01-async-retriever-kb-metadata-sidecar
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - scripts/batch_pipeline.py
autonomous: true
requirements: [KBM-01, KBM-02]
tags: [cmd_index, sidecar_integration, batch_pipeline]

must_haves:
  truths:
    - "`batch_pipeline.py index --kb-root $KB` 运行后，$KB/.metadata/chunks.json 存在且 JSON 合法"
    - "$KB/.metadata/org_disease_coverage.json 存在且为 {org: [canonical_keys]} schema"
    - "$KB/.metadata/synonym_map.yaml 存在且含 ≥10 个 canonical_key"
    - "cmd_index 内的 `qmd embed` 与 `qmd context add` 调用一字未改（保留 Metal exit-code 134 特例）"
    - "首次 index 与重复 index 行为一致（synonym_map.yaml 已存在时跳过保护人工 overrides）"
  artifacts:
    - path: "scripts/batch_pipeline.py"
      provides: "cmd_index 扩展为同时产出三个 .metadata/ 侧车文件"
      contains: "kb_metadata.build_sidecar"
  key_links:
    - from: "scripts/batch_pipeline.py::cmd_index"
      to: "scripts/kb_metadata.py::build_sidecar"
      via: "module-level import + 一次函数调用"
      pattern: "kb_metadata\\.build_sidecar"
---

<objective>
在 `scripts/batch_pipeline.py::cmd_index` 末尾接入 `kb_metadata.build_sidecar(kb_root, orgs_found)`，让一次 `index` 命令同时完成 QMD 索引构建 + 三个 `.metadata/*` 侧车文件产出。

Purpose: KBM-01/02 要求 `cmd_index` 在构建 QMD 索引时**额外产出** chunks.json + org_disease_coverage.json + synonym_map.yaml，避免引入新的 CLI 子命令；与 QMD `embed` 同步原子完成（D-05）。

Output:
- `scripts/batch_pipeline.py` 顶部 import 区域追加 `from scripts import kb_metadata`
- `cmd_index` 函数体在最后一句 `print("Index complete: ...")` 之前插入 build_sidecar 调用 + summary print
- 不动 `qmd collection add` / `qmd embed` / `qmd context add` 任一行
- 不动 `_DISEASE_KEYWORD_MAP` / `_extract_disease_keywords` / `filter_orgs_by_disease`（D-11，Phase 3 切换后再清理）

Scope guardrails:
- 这是 Phase 1 唯一动 `batch_pipeline.py` 的 plan，diff 必须严格控制在 cmd_index 函数体内 + 一行 module-level import
- 不允许在本 plan 中触碰 `cmd_orchestrate / cmd_validate / cmd_generate / cmd_merge / cmd_verify_batch / cmd_parse / cmd_split` 任一函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-CONTEXT.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-PATTERNS.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-02-PLAN.md
@docs/refactor_plan_2026-05-11.md
@scripts/batch_pipeline.py

<interfaces>
上游契约（来自 Plan 01-02 交付）：

  from scripts import kb_metadata
  summary = kb_metadata.build_sidecar(kb_root, orgs_found)
  # summary = {
  #   "chunks_path": Path, "coverage_path": Path, "synonym_path": Path,
  #   "n_chunks": int, "n_orgs": int,
  # }

build_sidecar 形参：
  kb_root: Path
  orgs_found: list[tuple[str, Path, list[Path]]]   # (org_name, org_dir, md_files)

现有 cmd_index 数据流（scripts/batch_pipeline.py:1249-1335）：
  cmd_index(args)
    -> kb_root = resolve_kb_root(...)
    -> orgs_found = [(org_name, org_dir, md_files), ...]    # 已是 build_sidecar 入参格式
    -> for org in orgs_found: qmd collection add ...
    -> qmd embed (Metal exit-code 134 特例 — 不动)
    -> for org in orgs_found: qmd context add + qmd ls ...
    -> print("Index complete: {N} organizations")           # 在此之前插入 build_sidecar
</interfaces>

<integration_constraint>
**唯一允许的修改面**：
1. 模块顶部 import 区域：加 `from scripts import kb_metadata`（或函数体内 lazy import — 二选一，按 executor 验证哪种零回归选哪种）
2. `cmd_index` 函数体内：在 `print("\nIndex complete: ...")` 之前插入 3-7 行 build_sidecar 调用 + summary print + try/except 错误处理

**禁止修改面**：
- cmd_index 内 lines 1255-1265（orgs_found 扫描循环）
- cmd_index 内 lines 1273-1291（qmd collection add）
- cmd_index 内 lines 1293-1299（qmd embed + Metal 134 特例）
- cmd_index 内 lines 1301-1333（qmd context add + qmd ls 归一化）
- 任何 batch_pipeline.py 中非 cmd_index 的函数/常量（含 _DISEASE_KEYWORD_MAP、filter_orgs_by_disease、cmd_validate、cmd_generate 等）

**Diff 上限**：`git diff scripts/batch_pipeline.py | grep -cE "^[+-]"` 应该 ≤ 16 行（含 +/- 符号本身；约 1 行 import + 6-8 行新插入 + 头尾 context 标记）。
</integration_constraint>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: cmd_index 末尾接入 build_sidecar 调用</name>
  <read_first>
    - `scripts/batch_pipeline.py:1249-1335`（cmd_index 完整函数体）
    - `scripts/batch_pipeline.py` 顶部前 30 行（确认现有 import 风格 — 绝对 `from scripts.xxx` 还是相对）
    - Plan 01-02 已交付的 `scripts/kb_metadata.py::build_sidecar` 签名（上方 interfaces 块）
    - PATTERNS.md §2.3（cmd_index 扩展点 + Hard rule）
    - PATTERNS.md §4.4（Metal exit-code 134 特例 — 必须保留）
    - **前置依赖澄清**：本 plan 通过 wave 拓扑隐式依赖 01-01（requirements.txt 落盘提供 pyyaml）与 01-02（scripts/kb_metadata.py 提供 build_sidecar）；executor 进入 Task 1 前 `ls scripts/kb_metadata.py requirements.txt` 应都存在。
  </read_first>
  <action>
    1. **顶部 import**：在 `scripts/batch_pipeline.py` 现有 import 区域末尾追加：
       ```
       from scripts import kb_metadata
       ```
       说明：scripts/ 已是 Python package（pytest 用 `scripts.retriever` 调用证明 `scripts/__init__.py` 存在或 sys.path 已含项目根）。若 executor 实测顶部 import 触发循环导入（极少见，因为 kb_metadata.py 只依赖 stdlib + yaml），改为在 cmd_index 函数体内做 lazy import：
       ```
       def cmd_index(args):
           ...
           from scripts import kb_metadata  # lazy import 避免循环依赖
           ...
       ```
       executor 自行确认哪条 import 路径能让 `pytest tests/` 零回归。

    2. **cmd_index 函数体修改**：在当前最后一行 `print(f"\nIndex complete: {len(orgs_found)} organizations")` 之前插入：
       ```
           # 病种侧车元数据（Plan 01-03 / KBM-01, KBM-02）
           # build_sidecar 失败不阻断 index（QMD 索引已建好；侧车可下次 re-run）
           try:
               sidecar = kb_metadata.build_sidecar(kb_root, orgs_found)
               print(
                   f"\nSidecar metadata: {sidecar['chunks_path'].parent}"
                   f"  ({sidecar['n_chunks']} chunks, {sidecar['n_orgs']} orgs)"
               )
           except Exception as exc:
               print(
                   f"\nWARNING: sidecar metadata generation failed: {exc}",
                   file=sys.stderr,
               )
       ```

       **失败处理策略落定**：采用「warn but do not abort」。理由：
       - cmd_index 现有的 Metal 134 也是 warn-only 容忍（line 1298）
       - D-05 是「一次性原子产出」语义，但 PATTERNS.md §4.4 显式建议「侧车失败不应回滚已建索引」
       - 若后续 Phase 3 发现侧车缺失让 pipeline 崩溃，再升级为 hard fail（v3.2 范围）

    3. **不修改**：
       - cmd_index 内 line 1255-1265 的扫描循环
       - line 1273-1333 的 qmd 命令调用块（含 Metal 134 特例）
       - 任何其他 batch_pipeline.py 函数（含 _DISEASE_KEYWORD_MAP 和 filter_orgs_by_disease — D-11）

    4. **不创建/修改 .gitignore**：D-12 明确 `.metadata/` 在 `$MEDICAL_GUIDELINES_DIR` 外部（仓库外），无需 gitignore。

    5. **不修改 SKILL.md / README.md / CHANGELOG.md**：本 plan 仅交付代码改动；文档同步留 Phase 4 一次性处理（refactor_plan §八末尾已明确）。
  </action>
  <verify>
    <automated>python3 -c "import ast; tree = ast.parse(open('scripts/batch_pipeline.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'cmd_index' in funcs" &amp;&amp; test "$(grep -c 'kb_metadata.build_sidecar' scripts/batch_pipeline.py)" = "1" &amp;&amp; test "$(grep -c 'qmd embed' scripts/batch_pipeline.py)" -ge 1 &amp;&amp; test "$(grep -c '134' scripts/batch_pipeline.py)" -ge 1 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('scripts/batch_pipeline.py').read())"` 退出码 0（语法正确）
    - `grep -c "kb_metadata.build_sidecar" scripts/batch_pipeline.py` 输出 `1`
    - `grep -c "from scripts import kb_metadata" scripts/batch_pipeline.py` 输出 `1`（顶部或 lazy 路径任一）
    - `grep -c "134" scripts/batch_pipeline.py` ≥ 1（Metal exit-code 特例未被误删）
    - `grep -nE "qmd.*embed|qmd.*collection.*add|qmd.*context.*add" scripts/batch_pipeline.py | wc -l` ≥ 3（三个 qmd 调用全部保留）
    - `git diff scripts/batch_pipeline.py | grep -cE "^[+-]" ` ≤ 16
    - `git diff scripts/batch_pipeline.py -- :^scripts/batch_pipeline.py` 内 cmd_index 之外的函数无 diff（用 `git diff -U0 scripts/batch_pipeline.py` 人工 verify diff 集中在 1249-1335 + 顶部 import）
    - `python3 -m pytest tests/test_index.py -v --no-header 2>&amp;1 | tail -3` 显示已有 index 相关测试零回归（若该文件存在）
    - `python3 -m pytest tests/ -q --no-header 2>&amp;1 | tail -2` 整体测试通过数 ≥ 173 基线（不出现 import error / cmd_index collect 失败）
  </acceptance_criteria>
  <done>cmd_index 在三个 qmd 调用之后、最终 print 之前插入了 build_sidecar 调用；现有 173 测试零回归；scripts/batch_pipeline.py diff 集中在 import 行 + cmd_index 末尾 7-9 行</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 真实 KB 端到端 index 运行验证</name>
  <what-built>
    Plan 01-01 + 01-02 + 01-03 累计交付：
    - `AsyncQMDService` 异步类
    - `scripts/kb_metadata.py` 6 个函数
    - `cmd_index` 扩展产出三个 .metadata/* 侧车文件
  </what-built>
  <how-to-verify>
    1. 确认依赖已装：
       ```
       pip install -r requirements.txt
       python3 -c "import httpx, yaml, pytest_asyncio; print('deps ok')"
       ```
    2. 跑全量测试套件（应全绿）：
       ```
       python3 -m pytest tests/ -v --no-header 2>&amp;1 | tail -10
       ```
       期望：旧 173 测试全绿 + 新增 ~20 test case 全绿（test_retriever_async.py ~7 个 + test_kb_metadata.py ~13 个）

    3. 真实 KB index（需要 `MEDICAL_GUIDELINES_DIR` 已设 + `qmd` CLI 已装）：
       ```
       echo "MEDICAL_GUIDELINES_DIR=$MEDICAL_GUIDELINES_DIR"
       ls "$MEDICAL_GUIDELINES_DIR" | head -10
       PYTHONPATH=. python3 scripts/batch_pipeline.py index --kb-root "$MEDICAL_GUIDELINES_DIR"
       ```
       期望 stdout 末尾出现：
       ```
       Sidecar metadata: /path/to/$KB/.metadata
         (N chunks, M orgs)

       Index complete: M organizations
       ```

    4. 三个侧车文件存在 + 合法：
       ```
       ls -la "$MEDICAL_GUIDELINES_DIR/.metadata/"
       python3 -c "import json; d=json.load(open('$MEDICAL_GUIDELINES_DIR/.metadata/chunks.json')); print(f'chunks: {len(d)} entries'); print('sample:', list(d.items())[0])"
       python3 -c "import json; d=json.load(open('$MEDICAL_GUIDELINES_DIR/.metadata/org_disease_coverage.json')); print(f'orgs: {sorted(d.keys())}'); print(f'NCCN coverage: {d.get(\"NCCN\", [])}')"
       python3 -c "import yaml; d=yaml.safe_load(open('$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml')); print(f'canonical keys: {sorted(d.keys())}')"
       ```
       期望：
       - chunks.json: 至少 ~10-30 个 entry（取决于 KB 实际文件数）
       - chunks.json 的 key 全部以 `qmd://` 开头且小写
       - 每个 entry 有 disease_tags/org/guideline_version 三字段
       - org_disease_coverage.json: ≥ 5 个 org（NCCN/ESMO/CSCO/JGCA/CACA 至少其中几个）
       - NCCN coverage 至少含 `gastric`（KB 含 NCCN-gastric-*.md）
       - synonym_map.yaml: 10 个 canonical_key 全在

    5. 重复 run（idempotency check）：
       ```
       PYTHONPATH=. python3 scripts/batch_pipeline.py index --kb-root "$MEDICAL_GUIDELINES_DIR"
       ```
       期望：synonym_map.yaml 内容**不变**（保护 overrides），chunks.json / coverage.json 重新计算覆盖。

    6. 手工编辑 overrides 后再 run：
       ```
       cp "$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml" "$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml.bak"
       echo "  - 用户测试 alias" >> "$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml"   # 仅作为粗略验证；正式编辑应规整 yaml
       # 实际更稳的测试：用 python 编辑 yaml 加一个 canonical 或 alias
       PYTHONPATH=. python3 scripts/batch_pipeline.py index --kb-root "$MEDICAL_GUIDELINES_DIR"
       diff "$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml" "$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml.bak"
       ```
       期望：仅本次手工编辑的 diff 行，build_sidecar 没有覆盖人工 overrides。

    7. 现有 test_qmd_integration.py 集成测试（如 QMD 可用）：
       ```
       QMD_AVAILABLE=1 python3 -m pytest tests/test_qmd_integration.py -v
       ```
       期望：env-gated 集成测试通过（同步类作为 thin shim 通过路径不变 — RTR-04）。
  </how-to-verify>
  <resume-signal>
    完成验证后回复 "approved" 继续 Phase 1 收尾（gsd commit + state 更新）；
    或描述具体问题（"chunks.json 缺 disease_tags 字段" / "重复 run 后 synonym_map 被覆盖" 等）进入 gap-closure。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cmd_index` ← CLI `--kb-root` 参数 | 来自命令行，已被 `resolve_kb_root()` 验证（CLAUDE.md 显示该函数有优先级与存在性检查）|
| `cmd_index` -> `build_sidecar` -> `$KB/.metadata/*` 写盘 | 写入用户自有目录，权限受 OS 保护；侧车路径由 Plan 01-02 内 startswith 防御保证 |
| `cmd_index` -> `subprocess.run(["qmd", ...])` | 已是 list 形式调用，无 shell=True；本 plan 不改动 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `from scripts import kb_metadata` 顶部 import | accept | 模块仅依赖 stdlib + pyyaml；无循环依赖风险（kb_metadata 不 import batch_pipeline）|
| T-03-02 | Denial of Service | build_sidecar 抛异常导致 index 退出码非零 | mitigate | try/except 包裹 + warn-only 输出到 stderr；保证 QMD index 已完成时整体 exit code = 0 |
| T-03-03 | Information Disclosure | Sidecar print 输出包含 KB 根路径 | accept | 路径本就是用户 CLI 参数传入，stdout 显示属于预期行为；与现有 `print(f"知识库路径: {kb_root}")` (line 1252) 同水位 |
| T-03-04 | Tampering | 攻击者预置恶意 synonym_map.yaml | mitigate | 已由 Plan 01-02 的 `yaml.safe_load` + 类型强转防御；本 plan 不引入新攻击面 |
| T-03-05 | Elevation of Privilege | cmd_index 在 import 时执行 kb_metadata seed 副作用 | mitigate | seed 副作用仅在 build_sidecar 被调用时触发（cmd_index 函数体内）；模块顶部 import 仅注册符号，不写盘 |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q --no-header 2>&1 | tail -2` 测试总数 ≥ 193（173 基线 + ~20 新增）且 0 failed
- `grep -c "kb_metadata.build_sidecar" scripts/batch_pipeline.py` 输出 `1`
- `git diff scripts/batch_pipeline.py | grep -cE "^[+-]"` ≤ 16
- 手工运行 `PYTHONPATH=. python3 scripts/batch_pipeline.py index --kb-root "$MEDICAL_GUIDELINES_DIR"` 后：
  - `ls "$MEDICAL_GUIDELINES_DIR/.metadata/"` 输出 `chunks.json  org_disease_coverage.json  synonym_map.yaml`
  - `python3 -c "import json; print(len(json.load(open('$MEDICAL_GUIDELINES_DIR/.metadata/chunks.json'))) > 0)"` 输出 `True`
  - `python3 -c "import yaml; d=yaml.safe_load(open('$MEDICAL_GUIDELINES_DIR/.metadata/synonym_map.yaml')); print(len(d) >= 10)"` 输出 `True`
- ROADMAP.md Phase 1 Success Criterion #2 达成（侧车三文件 schema 合法 + 实际生成）
</verification>

<success_criteria>
- ROADMAP.md Phase 1 Success Criterion #2 完整达成：`batch_pipeline.py index` 一次产出三个 `.metadata/*` 文件
- KBM-01: ✓ chunks.json 由 cmd_index 触发生成，schema 为 `{file_path: {disease_tags, org, guideline_version}}`
- KBM-02: ✓ org_disease_coverage.json 由 cmd_index 触发生成，schema 为 `{org: [canonical_keys]}`
- KBM-03 间接达成：cmd_index 触发 seed_synonym_map（来自 Plan 01-02 函数）
- Phase 1 整体 acceptance：执行 Plan 01-01 + 01-02 + 01-03 后，ROADMAP §Phase 1 Success Criteria 3 条全部达成（#1 pytest 全绿 / #2 三侧车存在 / #3 synonym_map 覆盖 10 癌种 + 归一化测试通过）
</success_criteria>

<output>
After completion, create `.planning/phases/01-async-retriever-kb-metadata-sidecar/01-03-SUMMARY.md` recording:
- cmd_index 修改 diff 范围（仅 1 行 import + 7-9 行函数体末尾）
- 侧车失败策略落定：warn but do not abort（与 Metal 134 容忍精神对齐）
- 真实 KB 实际产出统计（n_chunks / n_orgs / NCCN coverage 列表）
- Phase 3 接入提示：`pipeline.py` 顶部 `kb_meta = kb_metadata.load_synonym_map(kb_root)` + `chunks_meta = json.load(open(kb_root/'.metadata'/'chunks.json'))` + `coverage = json.load(open(kb_root/'.metadata'/'org_disease_coverage.json'))`；调用 `filter_orgs_by_disease` 决定 query 哪些 org，调用 `filter_chunks_by_disease` 在 dedupe 后过滤
- 已知边界：cmd_index 重复 run 会重新生成 chunks.json / coverage.json，但保护 synonym_map.yaml（运维 overrides 安全）
</output>
