---
phase: 01-async-retriever-kb-metadata-sidecar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/retriever.py
  - tests/test_retriever_async.py
  - requirements.txt
autonomous: true
requirements: [RTR-01, RTR-02, RTR-03, RTR-04]
tags: [async, httpx, retriever, mcp, qmd]

must_haves:
  truths:
    - "AsyncQMDService 可作为 `async with` 上下文管理器使用"
    - "并发调用 query() 时全局 in-flight 数受 asyncio.Semaphore 限制（默认 8，可注入覆盖）"
    - "MCP Mcp-Session-Id 在 __aenter__ 完成时已就绪；中途 HTTP 400 或缺 session header 触发一次 re-initialize 自动重试"
    - "同步 QMDService 字节级 API 兼容：tests/test_retriever.py 与 tests/test_qmd_integration.py 不修改全部通过"
    - "异步类用 httpx.AsyncClient；同步类继续用 requests（两条独立 HTTP 路径，互不调用）"
  artifacts:
    - path: "scripts/retriever.py"
      provides: "AsyncQMDService 类 + 保留同步 QMDService"
      contains: "class AsyncQMDService"
    - path: "tests/test_retriever_async.py"
      provides: "异步 retriever 单元测试套件"
      contains: "AsyncQMDService"
    - path: "requirements.txt"
      provides: "依赖清单（新建）"
      contains: "httpx"
  key_links:
    - from: "scripts/retriever.py::AsyncQMDService"
      to: "httpx.AsyncClient"
      via: "self._http.post"
      pattern: "httpx\\.AsyncClient"
    - from: "scripts/retriever.py::AsyncQMDService.query"
      to: "self._sem (asyncio.Semaphore)"
      via: "async with self._sem"
      pattern: "async with self\\._sem"
    - from: "scripts/retriever.py::AsyncQMDService"
      to: "QMDService._parse_mcp_response"
      via: "staticmethod 复用"
      pattern: "_parse_mcp_response"
---

<objective>
为 Phase 1 引入第一段异步代码：在 `scripts/retriever.py` 中新增 `AsyncQMDService` 类（基于 `httpx.AsyncClient` + `asyncio.Semaphore` + 注入式 client/sem），同时保留同步 `QMDService` 现有 `requests`-based 实现路径**完全不变**，确保 173 测试基线不动。

Purpose: 后续 Phase 3 `pipeline.py` 要按患者并发执行 QMD 查询（39 次串行 3min → 并发 8 路 30-60s），需要一个真正异步的 QMD 客户端；同步类作为 Phase 2 仍在使用的兼容入口必须保留。

Output:
- `scripts/retriever.py` 新增 `AsyncQMDService` 类（~150 LOC），同步 `QMDService` 4 个方法体保持原样
- `tests/test_retriever_async.py` 新建，使用 `pytest-asyncio` + `AsyncMock` 覆盖生命周期、session retry、Semaphore 限流、并发正确性 4 类 case
- `requirements.txt` 新建并固化 `httpx>=0.27,<1.0` + `pytest-asyncio>=0.23,<1.0` 两条新依赖（CLAUDE.md 显示 npm/uv/pip 工具链，但仓库当前无 `requirements.txt` / `pyproject.toml`，本 plan 新建）
- 每个 async test case 显式加 `@pytest.mark.asyncio` 装饰器（pytest-asyncio strict mode；conftest 无需任何修改：pytest-asyncio 通过 entry point 自动注册 asyncio marker）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-CONTEXT.md
@.planning/phases/01-async-retriever-kb-metadata-sidecar/01-PATTERNS.md
@docs/refactor_plan_2026-05-11.md
@scripts/retriever.py
@tests/test_retriever.py
@tests/test_qmd_integration.py
@tests/conftest.py

<interfaces>
<!-- 同步 QMDService 的 4 个对外契约（不变，必须保留）：scripts/retriever.py:43-216 -->
class QMDService:
    def __init__(self, port: int | None = None): ...
    def __enter__(self) -> "QMDService": ...
    def __exit__(self, exc_type, exc_val, exc_tb) -> None: ...
    def query(self, text: str, top_k: int = 10, min_score: float = 0.3) -> list[dict]: ...
    def search(self, text: str, top_k: int = 10) -> list[dict]: ...

# 现有同步测试 patch 的入口（tests/test_retriever.py:9-10, 29-30）：
#   @patch("scripts.retriever.subprocess.Popen")
#   @patch("scripts.retriever.requests.post")
# 这两个模块属性必须仍可被 patch（即同步类继续使用 requests，绝不改成 asyncio.run(self._async.method())）

# AsyncQMDService 新契约（本 plan 交付）：
class AsyncQMDService:
    def __init__(
        self,
        port: int | None = None,
        *,
        timeout_s: float = 60.0,
        semaphore: asyncio.Semaphore | None = None,
        http_client: "httpx.AsyncClient | None" = None,
    ): ...
    async def __aenter__(self) -> "AsyncQMDService": ...
    async def __aexit__(self, exc_type, exc_val, exc_tb) -> None: ...
    async def query(self, text: str, top_k: int = 10, min_score: float = 0.3) -> list[dict]: ...
    async def search(self, text: str, top_k: int = 10) -> list[dict]: ...
</interfaces>

<runtime_constraints>
- Python 3.9.6（已确认 `python3 -c "import sys; print(sys.version)"` 输出 3.9.6） — **禁止** 使用 `asyncio.TaskGroup` / `asyncio.timeout` / `except*`（这些是 3.11+）。本 plan 内部不需要 TaskGroup（Phase 3 才需），但要为后续注入式 Semaphore 留出契约。
- `from __future__ import annotations` 必须出现在 retriever.py 顶部（已有，保留），新文件同样加上 — 否则 `str | None`、`asyncio.Semaphore | None` 在 3.9 求值期报 TypeError。
- 仓库当前**无 httpx / asyncio / async def** 任何先例（grep 已确认空命中），本 plan 是第一次引入。
</runtime_constraints>
</context>

<critical_conflict_resolution>
**D-01 vs RTR-04 释义落定**（PATTERNS.md §5.6 明确要求 planner 选定）：

- **CONTEXT.md D-01 字面释义**："单一异步实现，同步 QMDService 降级为 thin shim（内部 asyncio.run）"
- **冲突**：tests/test_retriever.py:10,30,82,90 用 `@patch("scripts.retriever.requests.post")` —— 若同步类内部改走 `asyncio.run(self._async.method())` + `httpx`，patch 目标漂移到 httpx，旧测试**全部红**，直接违反 RTR-04。
- **本 plan 采取的实施层解读**（PATTERNS.md §5.6 推荐方案）：
  - `AsyncQMDService` 使用 `httpx.AsyncClient`（真异步）
  - 同步 `QMDService` 保留**独立 requests-based 实现**（原 217 LOC 结构基本不动）
  - D-01 重新表述为「**调用方语义**：所有并发 caller（Phase 3 pipeline.py）一律使用 AsyncQMDService；同步 QMDService 仅作为 BC stub 供 Phase 2 之前的旧代码与外部脚本使用」
  - 协议常量 `_MCP_HEADERS / _INIT_PAYLOAD` + staticmethod `_parse_mcp_response` 跨两类共享（已是 module-level，无需改）
- **Trade-off 公开**：两条独立 HTTP 路径意味着 ~80 LOC 代码重复（lifecycle + HTTP 调用骨架），换取 RTR-04 零违规。在 Phase 3 之后若全调用方已迁到 async，可在后续 milestone 删除同步类。
- **本释义在 Task 1 acceptance_criteria 中以 `git diff tests/test_retriever.py` 与 `tests/test_qmd_integration.py` 输出为空硬验证**。
</critical_conflict_resolution>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 引入 httpx + pytest-asyncio 依赖与项目级 asyncio 配置</name>
  <read_first>
    - 检查仓库根：`ls /Users/f.sh/Workspace/devs/medical-guidelines-suite/requirements*.txt pyproject.toml setup.cfg 2>/dev/null`（已知：均不存在）
    - CLAUDE.md「Dependencies」表 — 现有依赖通过 `pip install docling`/`uv tool install mineru`/`npm install -g @tobilu/qmd` 散装安装，但 Python 测试依赖（pytest 等）必然已被 `pip install` 过；本 plan 是首次把 Python 依赖固化到 `requirements.txt`
  </read_first>
  <files>
    - requirements.txt（新建）
  </files>
  <action>
    1. **新建 `requirements.txt`**，写入以下内容（按字母排序，pin 上界但留升级空间；版本上界保守锁到当前主版本内）：
       ```
       docling>=2.0
       httpx>=0.27,<1.0
       openpyxl>=3.1
       pytest>=7.0
       pytest-asyncio>=0.23,<1.0
       pyyaml>=6.0
       ```
       说明：`pyyaml` 提前列入是为 Plan 01-02 复用同一份 requirements；`mineru` 与 `qmd` 不在此处（前者 uv-tool，后者 npm，CLAUDE.md 已规定）。
    2. 不修改 `pyproject.toml`（不存在），不新建 `setup.cfg`。
    3. **不要执行 `pip install`**（用户的 venv 已经能跑 pytest，executor 可在 Task 3 跑测试时按需执行 `pip install httpx pytest-asyncio`；本 task 只锚定 requirements.txt）。

    **说明**：pytest-asyncio 通过 entry point 自动注册 `asyncio` marker，无需 conftest 协助；测试文件按 strict mode 习惯逐 case 加 `@pytest.mark.asyncio` 即可（Task 3 测试文件统一遵循）。
  </action>
  <verify>
    <automated>test -f requirements.txt && grep -q "^httpx" requirements.txt && grep -q "^pytest-asyncio" requirements.txt && grep -q "^pyyaml" requirements.txt && pip install -q -r requirements.txt && python3 -c "import httpx, pytest_asyncio, yaml" && python3 -m pytest tests/test_retriever.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cat requirements.txt | grep -cE "^(httpx|pytest-asyncio|pyyaml)" ` 输出 `3`
    - 执行 `pip install -r requirements.txt` 后 `python3 -c "import httpx, pytest_asyncio, yaml"` exit code 0
    - `python3 -m pytest tests/test_retriever.py -v` exit code 0（同步基线测试在引入 httpx + pytest-asyncio 依赖后**仍**通过——这是真正的回归保护）
    - `python3 -m pytest tests/ -q --no-header 2>&1 | tail -2` 整体通过数 ≥ 173（基线零回归；不出现 import error）
    - **git diff scripts/ 输出为空**（本 task 不动 scripts/）
    - **git diff tests/conftest.py 输出为空**（本 task 不动 conftest）
  </acceptance_criteria>
  <done>requirements.txt 包含 httpx / pytest-asyncio / pyyaml 三条新依赖；同步基线 tests/test_retriever.py 在新依赖下仍全绿；scripts/ 与 tests/conftest.py 零修改</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 AsyncQMDService 类（保留同步 QMDService 原路径）</name>
  <read_first>
    - `scripts/retriever.py` 全文（已读，217 行）— 重点 lines 26-40（协议常量）、63-93（__enter__/__exit__ 同步生命周期）、155-186（_check_port_available + _wait_for_ready）、188-216（_parse_mcp_response staticmethod）
    - `.planning/phases/01-async-retriever-kb-metadata-sidecar/01-PATTERNS.md` §3.1-3.3（异步生命周期 + Semaphore 注入 + session retry 三段示例代码）
    - `.planning/phases/01-async-retriever-kb-metadata-sidecar/01-PATTERNS.md` §5.1（mcp-session-id header 是小写）、§5.2（双重 poll）、§5.5（httpx 用 httpx.ConnectError 而非 requests.ConnectionError）
    - `docs/refactor_plan_2026-05-11.md` §四.2（async retriever 设计）
    - `tests/test_retriever.py` 全文（已读，99 行）— 确认 patch 入口
  </read_first>
  <behavior>
    本 task 引入新代码，按 TDD 流程（Task 3 先写测试也可，但本任务 batch 在一起；executor 可按以下行为先在 retriever.py 中 stub 出空类签名，再在 Task 3 中补测试，再回填实现 — 简化起见，本 task 直接实现，Task 3 验证）。

    AsyncQMDService 必须满足的可测行为：
    - **B-1 生命周期**：`async with AsyncQMDService(port=9999) as svc:` 启动 `subprocess.Popen` + 通过 httpx.AsyncClient post `_INIT_PAYLOAD` 拿 `mcp-session-id` header；svc.process 暴露 Popen handle；退出时 terminate（poll 仍存活时）→ 5s 超时 kill；自创 http_client 在退出时 aclose
    - **B-2 端口冲突**：若 `_check_port_available()` 检测到端口被占，抛 `QMDStartupError(...already in use...)`；不启动子进程
    - **B-3 启动失败回滚**：若 `_wait_for_ready_async` 抛异常，self.process 被 kill 且置 None；自创的 http_client 被 aclose
    - **B-4 query 走 Semaphore**：`await svc.query(text)` 在进入 HTTP post 前 `async with self._sem:`；注入 `Semaphore(2)`，并发触发 5 个 `asyncio.gather(svc.query(...) × 5)`，**任一时刻 in-flight ≤ 2**
    - **B-5 session retry**：第一次 `tools/call` 返回 HTTP 400（或 response 缺 mcp-session-id header）→ 内部重发 `_INIT_PAYLOAD` 拿新 session → 再发原 payload 一次；**只重试一次**，第二次仍失败抛 `httpx.HTTPStatusError`
    - **B-6 并发顺序**：`asyncio.gather(svc.query("q1"), svc.query("q2"), svc.query("q3"))` 返回的 list[0]/list[1]/list[2] 与 q1/q2/q3 对应（asyncio.gather 默认保序，验证未被自定义代码破坏）
    - **B-7 同步 QMDService 字节级兼容**：tests/test_retriever.py 4 个 case + tests/test_qmd_integration.py 全部测试在本 task 完成后仍通过；`git diff tests/test_retriever.py tests/test_qmd_integration.py` 输出为空
  </behavior>
  <files>
    - scripts/retriever.py（修改 — 在文件末尾追加 AsyncQMDService 类，~150 LOC；module 顶部增加 `import asyncio` 与 `import httpx`；module-level 常量 `_MCP_HEADERS` / `_INIT_PAYLOAD` 与 staticmethod `_parse_mcp_response` 不动；同步 QMDService 的 4 个方法体一行不动）
  </files>
  <action>
    1. **顶部 imports**（修改 lines 13-19 区域）：
       ```python
       from __future__ import annotations

       import asyncio
       import os
       import socket
       import subprocess
       import time

       import httpx
       import requests
       ```
       — `asyncio` 与 `httpx` 是新增；`requests` 保留供同步类使用（不删！）。
    2. **同步 QMDService 类（lines 43-216）一字不动**。
    3. **将 `_parse_mcp_response` 提升为模块级 staticmethod 别名**（可选优化）：本 task 不重构，保持其为 `QMDService._parse_mcp_response`，`AsyncQMDService` 直接调 `QMDService._parse_mcp_response(...)` 即可。
    4. **在文件末尾追加 `AsyncQMDService` 类**（结构按 PATTERNS.md §3.1-3.3 推荐 + 上述 B-1..B-6 行为）：
       ```python
       class AsyncQMDService:
           """Async QMD MCP client (httpx-based) for concurrent retrieval.

           Phase 1 (RTR-01..04): 第一段异步代码，与同步 QMDService 共存。
           Phase 3 pipeline.py 通过注入式 semaphore/http_client 共享并发预算。
           """

           def __init__(
               self,
               port: int | None = None,
               *,
               timeout_s: float = 60.0,
               semaphore: asyncio.Semaphore | None = None,
               http_client: httpx.AsyncClient | None = None,
           ):
               self.port = port or int(os.environ.get("QMD_PORT", "8181"))
               self.base_url = f"http://localhost:{self.port}/mcp"
               self._timeout = timeout_s
               self._sem = semaphore or asyncio.Semaphore(8)   # D-03 默认 8
               self._http = http_client
               self._owns_http = http_client is None
               self.process: subprocess.Popen | None = None
               self._session_id: str | None = None

           @property
           def _session_headers(self) -> dict:
               if not self._session_id:
                   raise RuntimeError("QMD session not initialized")
               return {**_MCP_HEADERS, "Mcp-Session-Id": self._session_id}

           def _check_port_available(self) -> None:
               # 直接复用同步类的同名方法的逻辑（端口检查同步无成本）
               with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
                   if s.connect_ex(("localhost", self.port)) == 0:
                       raise QMDStartupError(
                           f"Port {self.port} already in use. "
                           f"Kill the existing process or set QMD_PORT env var."
                       )

           async def __aenter__(self) -> "AsyncQMDService":
               self._check_port_available()
               self.process = subprocess.Popen(
                   ["qmd", "mcp", "--http", "--port", str(self.port)],
                   stdout=subprocess.DEVNULL,
                   stderr=subprocess.DEVNULL,
               )
               if self._owns_http:
                   self._http = httpx.AsyncClient(timeout=self._timeout)
               try:
                   await self._wait_for_ready_async(timeout=30)
               except Exception:
                   if self.process and self.process.poll() is None:
                       self.process.kill()
                       self.process.wait(timeout=3)
                   self.process = None
                   if self._owns_http and self._http is not None:
                       await self._http.aclose()
                       self._http = None
                   raise
               return self

           async def __aexit__(self, exc_type, exc_val, exc_tb) -> None:
               self._session_id = None
               try:
                   if self._owns_http and self._http is not None:
                       await self._http.aclose()
               finally:
                   self._http = None
                   if self.process is None:
                       return
                   if self.process.poll() is not None:
                       self.process = None
                       return
                   self.process.terminate()
                   try:
                       self.process.wait(timeout=5)
                   except subprocess.TimeoutExpired:
                       self.process.kill()
                       self.process.wait(timeout=3)
                   self.process = None

           async def _wait_for_ready_async(self, timeout: int = 30) -> None:
               """Poll QMD HTTP server async; store mcp-session-id from first 200."""
               deadline = time.monotonic() + timeout
               while time.monotonic() < deadline:
                   # 双重 poll（PATTERNS.md §5.2）：进 HTTP 前先确认子进程仍活
                   if self.process.poll() is not None:
                       raise QMDStartupError(
                           f"QMD exited during startup (exit code: {self.process.returncode})"
                       )
                   try:
                       resp = await self._http.post(
                           self.base_url,
                           headers=_MCP_HEADERS,
                           json=_INIT_PAYLOAD,
                           timeout=5,
                       )
                       if resp.status_code == 200:
                           # 第二重 poll（health 200 后子进程仍可能瞬间挂）
                           if self.process.poll() is not None:
                               raise QMDStartupError("QMD exited immediately after health check")
                           # mcp-session-id header 是小写（PATTERNS.md §5.1）
                           self._session_id = resp.headers.get("mcp-session-id")
                           return
                   except httpx.ConnectError:   # 不是 ConnectionError！PATTERNS.md §5.5
                       pass
                   await asyncio.sleep(0.5)
               raise QMDStartupError(f"QMD not ready after {timeout}s")

           async def _reinitialize_session(self) -> None:
               """Re-send initialize and capture new mcp-session-id (D-04 retry path)."""
               resp = await self._http.post(
                   self.base_url, headers=_MCP_HEADERS, json=_INIT_PAYLOAD, timeout=5,
               )
               resp.raise_for_status()
               self._session_id = resp.headers.get("mcp-session-id")

           async def _post_tools_call(self, payload: dict) -> list[dict]:
               """POST tools/call with one re-initialize retry on session invalidation."""
               resp = await self._http.post(
                   self.base_url,
                   headers=self._session_headers,
                   json=payload,
                   timeout=self._timeout,
               )
               # D-04：400 OR 响应缺 session header → 重新 initialize + 重发一次
               session_invalid = (
                   resp.status_code == 400
                   or resp.headers.get("mcp-session-id") is None
               )
               if session_invalid:
                   await self._reinitialize_session()
                   resp = await self._http.post(
                       self.base_url,
                       headers=self._session_headers,
                       json=payload,
                       timeout=self._timeout,
                   )
               resp.raise_for_status()
               return QMDService._parse_mcp_response(resp.json())

           async def query(
               self, text: str, top_k: int = 10, min_score: float = 0.3
           ) -> list[dict]:
               """Async hybrid query (BM25 + vector + LLM reranking).

               Semaphore-gated: at most `self._sem._value` in-flight requests.
               """
               async with self._sem:
                   return await self._post_tools_call({
                       "jsonrpc": "2.0",
                       "id": 2,
                       "method": "tools/call",
                       "params": {
                           "name": "query",
                           "arguments": {
                               "searches": [
                                   {"type": "lex", "query": text},
                                   {"type": "vec", "query": text},
                               ],
                               "intent": text,
                               "limit": top_k,
                               "minScore": min_score,
                           },
                       },
                   })

           async def search(self, text: str, top_k: int = 10) -> list[dict]:
               """Async pure BM25 keyword search (no LLM, faster)."""
               async with self._sem:
                   return await self._post_tools_call({
                       "jsonrpc": "2.0",
                       "id": 2,
                       "method": "tools/call",
                       "params": {
                           "name": "query",
                           "arguments": {
                               "searches": [{"type": "lex", "query": text}],
                               "intent": text,
                               "limit": top_k,
                           },
                       },
                   })
       ```
    5. **不修改 `_DISEASE_KEYWORD_MAP` / `_extract_disease_keywords` / `filter_orgs_by_disease`**（在 batch_pipeline.py，Plan 01-02 处理；D-11 明确 Phase 1 不删旧函数）。
    6. **不修改 `cmd_index`**（Plan 01-03 处理）。
    7. **不创建 `scripts/kb_metadata.py`**（Plan 01-02 处理）。
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, '.'); from scripts.retriever import QMDService, AsyncQMDService, QMDStartupError, _MCP_HEADERS, _INIT_PAYLOAD; assert hasattr(AsyncQMDService, '__aenter__'); assert hasattr(AsyncQMDService, '__aexit__'); assert hasattr(AsyncQMDService, 'query'); assert hasattr(AsyncQMDService, 'search'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from scripts.retriever import AsyncQMDService; import inspect; assert inspect.iscoroutinefunction(AsyncQMDService.query); assert inspect.iscoroutinefunction(AsyncQMDService.search); assert inspect.iscoroutinefunction(AsyncQMDService.__aenter__); print('OK')"` 输出 `OK`
    - `grep -c "^class AsyncQMDService" scripts/retriever.py` 输出 `1`
    - `grep -c "^class QMDService" scripts/retriever.py` 输出 `1`（同步类仍存在）
    - `grep -n "import requests" scripts/retriever.py` 命中 1 次（同步类继续走 requests）
    - `grep -n "import httpx" scripts/retriever.py` 命中 1 次（异步类用 httpx）
    - `python3 -m pytest tests/test_retriever.py -v --no-header 2>&1 | tail -5` 显示 4 passed
    - **`git diff tests/test_retriever.py tests/test_qmd_integration.py` 输出为空**（RTR-04 硬约束 — 一字不改）
    - `python3 -c "import ast; tree = ast.parse(open('scripts/retriever.py').read()); print('syntax-ok')"` 输出 `syntax-ok`
    - 同步 QMDService 类 lines（43-216 区间）的字节级 diff 仅限于「`requests` 与 `httpx` import 增加」`+ 文件尾部追加 AsyncQMDService`；同步类方法体 0 修改 — 通过 `git diff scripts/retriever.py | grep -E "^-" | grep -v "^---" | wc -l` 应输出 `0`（没有任何 `-` 行）
  </acceptance_criteria>
  <done>scripts/retriever.py 含 AsyncQMDService + 原同步 QMDService 两个共存类；test_retriever.py / test_qmd_integration.py 字节级未改且原测试全绿；模块可被 Python 3.9.6 import 成功</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/test_retriever_async.py 4 类异步测试覆盖</name>
  <read_first>
    - `tests/test_retriever.py` 全文（已读，99 行）— 复用其 mock 风格、port conflict case 结构
    - PATTERNS.md §2.4（async test 改写要点：`AsyncMock` + `httpx.AsyncClient` patch 目标）、§3.4（pytest-asyncio 配置示例）
    - Python 3.9 `unittest.mock.AsyncMock` 标准库自带，无需第三方
    - Task 2 已交付的 `AsyncQMDService` 完整契约（上方 interfaces 块）
  </read_first>
  <files>
    - tests/test_retriever_async.py（新建，~120 LOC，6-8 个 case）
  </files>
  <action>
    1. **新建 `tests/test_retriever_async.py`**，按下面骨架写 6 个 case（覆盖 B-1..B-6）：
       ```python
       """Tests for AsyncQMDService context manager and async query API.

       Coverage: lifecycle / port conflict / startup failure rollback /
       Semaphore gating / session-invalid retry / concurrent gather ordering.
       """

       from __future__ import annotations

       import asyncio
       from unittest.mock import AsyncMock, MagicMock, patch

       import pytest


       def _mk_http_response(*, status=200, session_id="s1", body=None):
           """Helper: 构造 httpx.Response 的 MagicMock。"""
           resp = MagicMock()
           resp.status_code = status
           resp.headers = {"mcp-session-id": session_id} if session_id else {}
           resp.json = MagicMock(return_value=body if body is not None else {"result": {}})
           resp.raise_for_status = MagicMock(return_value=None)
           return resp


       def _empty_qmd_result():
           """空的 QMD MCP 响应体（_parse_mcp_response 处理后返回 [])。"""
           return {"result": {"structuredContent": {"results": []}}}


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_service_lifecycle(mock_client_cls, mock_popen):
           """B-1: __aenter__ 启动 Popen + 抓 session_id；__aexit__ terminate."""
           proc = MagicMock()
           proc.poll.return_value = None
           proc.pid = 12345
           mock_popen.return_value = proc

           mock_client = AsyncMock()
           mock_client.post = AsyncMock(return_value=_mk_http_response())
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService

           async with AsyncQMDService(port=9999) as svc:
               assert svc.process is not None
               assert svc.port == 9999
               assert svc._session_id == "s1"

           proc.terminate.assert_called_once()
           mock_client.aclose.assert_awaited()


       @pytest.mark.asyncio
       @patch("scripts.retriever.socket.socket")
       async def test_async_qmd_detects_port_conflict(mock_socket_cls):
           """B-2: 端口被占 → QMDStartupError, Popen 不被调用."""
           mock_sock = MagicMock()
           mock_sock.connect_ex.return_value = 0
           mock_sock.__enter__ = lambda s: mock_sock
           mock_sock.__exit__ = MagicMock(return_value=False)
           mock_socket_cls.return_value = mock_sock

           from scripts.retriever import AsyncQMDService, QMDStartupError
           with pytest.raises(QMDStartupError, match="already in use"):
               async with AsyncQMDService(port=9999):
                   pass


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_startup_failure_kills_process(mock_client_cls, mock_popen):
           """B-3: _wait_for_ready_async 抛异常 → process 被 kill, http_client aclose."""
           proc = MagicMock()
           # 模拟健康检查 timeout，子进程仍在跑（poll 返回 None）
           proc.poll.return_value = None
           mock_popen.return_value = proc

           mock_client = AsyncMock()
           # 让 post 永远超时，触发 deadline 超过 → QMDStartupError
           import httpx as _httpx
           mock_client.post = AsyncMock(side_effect=_httpx.ConnectError("no connection"))
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService, QMDStartupError
           svc = AsyncQMDService(port=9999, timeout_s=0.01)
           # 让 _wait_for_ready_async 立刻 deadline：手动 patch timeout 到极小
           with patch.object(AsyncQMDService, "_wait_for_ready_async",
                             new=AsyncMock(side_effect=QMDStartupError("timeout"))):
               with pytest.raises(QMDStartupError, match="timeout"):
                   await svc.__aenter__()

           proc.kill.assert_called_once()
           mock_client.aclose.assert_awaited()


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_semaphore_limits_in_flight(mock_client_cls, mock_popen):
           """B-4: 注入 Semaphore(2)，5 个并发 query 时 in-flight 上限为 2."""
           proc = MagicMock()
           proc.poll.return_value = None
           mock_popen.return_value = proc

           in_flight = {"current": 0, "peak": 0}

           async def slow_post(*args, **kwargs):
               # initialize 调用（第一次）直接返回 health resp
               body = kwargs.get("json", {})
               if body.get("method") == "initialize":
                   return _mk_http_response()
               # tools/call 走限流路径：进出计数
               in_flight["current"] += 1
               in_flight["peak"] = max(in_flight["peak"], in_flight["current"])
               await asyncio.sleep(0.05)
               in_flight["current"] -= 1
               return _mk_http_response(body=_empty_qmd_result())

           mock_client = AsyncMock()
           mock_client.post = AsyncMock(side_effect=slow_post)
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService

           sem = asyncio.Semaphore(2)
           async with AsyncQMDService(port=9999, semaphore=sem) as svc:
               results = await asyncio.gather(*[svc.query(f"q{i}") for i in range(5)])

           assert len(results) == 5
           assert in_flight["peak"] <= 2, f"in-flight peak {in_flight['peak']} > 2"


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_session_invalid_retries_once(mock_client_cls, mock_popen):
           """B-5: 第一次 tools/call HTTP 400 → re-initialize → 第二次成功."""
           proc = MagicMock()
           proc.poll.return_value = None
           mock_popen.return_value = proc

           # post 序列：
           #   1) initialize（__aenter__ 时）→ 200, session_id=s1
           #   2) tools/call → 400（session 失效）
           #   3) re-initialize → 200, session_id=s2
           #   4) tools/call retry → 200, 返回 empty_qmd_result
           posts = [
               _mk_http_response(session_id="s1"),
               _mk_http_response(status=400, session_id=None,
                                 body={"error": {"code": -32000, "message": "session expired"}}),
               _mk_http_response(session_id="s2"),
               _mk_http_response(session_id="s2", body=_empty_qmd_result()),
           ]
           # 第二次 raise_for_status 不应抛（因为是 400 触发 retry，不是 raise）；
           # 但 httpx 真实 400 的 raise_for_status 会抛 — mock 时手动让 [1] 的 raise_for_status 为 noop
           # （因为 _post_tools_call 在 400 路径上不调 raise_for_status，先判 session_invalid）
           # — 当前实现先 await post 再判 400，确实不调 raise_for_status，所以 mock 默认 noop 是对的。

           mock_client = AsyncMock()
           mock_client.post = AsyncMock(side_effect=posts)
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService

           async with AsyncQMDService(port=9999) as svc:
               results = await svc.query("test")

           assert results == []
           # 验证总共发了 4 次 post（1 init + 1 fail + 1 reinit + 1 retry）
           assert mock_client.post.await_count == 4
           # session_id 应已切到 s2
           # （__aexit__ 已清空，这里读 mock 历史确认）
           reinit_call = mock_client.post.await_args_list[2]
           assert reinit_call.kwargs.get("json", {}).get("method") == "initialize"


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_concurrent_query_ordering(mock_client_cls, mock_popen):
           """B-6: asyncio.gather(q1, q2, q3) 返回顺序与查询顺序一致."""
           proc = MagicMock()
           proc.poll.return_value = None
           mock_popen.return_value = proc

           async def echo_post(*args, **kwargs):
               body = kwargs.get("json", {})
               if body.get("method") == "initialize":
                   return _mk_http_response()
               q = body["params"]["arguments"]["intent"]
               return _mk_http_response(body={
                   "result": {"structuredContent": {"results": [
                       {"snippet": q, "file": "f.md", "score": 1.0, "context": ""}
                   ]}}
               })

           mock_client = AsyncMock()
           mock_client.post = AsyncMock(side_effect=echo_post)
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService
           async with AsyncQMDService(port=9999) as svc:
               r1, r2, r3 = await asyncio.gather(svc.query("q1"), svc.query("q2"), svc.query("q3"))

           assert r1[0]["content"] == "q1"
           assert r2[0]["content"] == "q2"
           assert r3[0]["content"] == "q3"


       @pytest.mark.asyncio
       @patch("scripts.retriever.subprocess.Popen")
       @patch("scripts.retriever.httpx.AsyncClient")
       async def test_async_qmd_exit_handles_crashed_process(mock_client_cls, mock_popen):
           """同步类对应 case 的 async 版本：__aexit__ 时若 process 已挂，不再 terminate."""
           proc = MagicMock()
           # 第一次 poll（__aenter__ 的 _wait_for_ready_async 内 health check 前）None
           # 第二次 poll（健康 200 后再确认）None
           # 第三次 poll（__aexit__ 入口判活）返回 1 (crashed)
           proc.poll.side_effect = [None, None, 1]
           mock_popen.return_value = proc

           mock_client = AsyncMock()
           mock_client.post = AsyncMock(return_value=_mk_http_response())
           mock_client.aclose = AsyncMock()
           mock_client_cls.return_value = mock_client

           from scripts.retriever import AsyncQMDService
           async with AsyncQMDService(port=9999):
               pass

           proc.terminate.assert_not_called()
       ```
    2. 不要再造 `mock_kb` 或 `pyproject.toml`。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_retriever_async.py -v --no-header 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_retriever_async.py -v 2>&1 | tail -3` 末行匹配 `7 passed`（6 个 B-case + 1 个 crashed-process case）— 若 executor 决定合并/拆分 case 数量可在 6-8 之间，但全部必须 PASS
    - `python3 -m pytest tests/test_retriever.py -v 2>&1 | tail -3` 末行匹配 `4 passed`（同步基线零回归）
    - `python3 -m pytest tests/ -v --co 2>&1 | grep -c "test_async_qmd"` ≥ 6
    - `grep -c "@pytest.mark.asyncio" tests/test_retriever_async.py` ≥ 6
    - `grep -c "AsyncMock\|httpx.AsyncClient" tests/test_retriever_async.py` ≥ 4
    - **`git diff tests/test_retriever.py tests/test_qmd_integration.py` 输出为空**
  </acceptance_criteria>
  <done>tests/test_retriever_async.py 落地，6+ 个 async case 全绿；同步基线测试零回归；session retry / Semaphore / 并发顺序三条核心 invariant 显式验证</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncQMDService → QMD MCP server (HTTP localhost) | 本机进程间通信；攻击面仅限本机用户已具有的权限 |
| AsyncQMDService → 用户传入的 `text` 参数 | 调用方提供的查询字符串，作为 JSON payload 透传给 QMD；不会进入 shell/SQL |
| AsyncQMDService.__aenter__ → subprocess.Popen(["qmd", ...]) | 启动 QMD 子进程，参数 list 形式硬编码 + 仅 port 数字注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `subprocess.Popen(["qmd", "mcp", "--http", "--port", str(self.port)])` | mitigate | 参数已是 list 形式（避免 shell=True）；`self.port` 经 `int(...)` 强制类型，杜绝 cmd-injection；env `QMD_PORT` 同样走 `int()` |
| T-01-02 | Information Disclosure | `httpx.AsyncClient` 默认 verify TLS | accept | base_url 仅 `http://localhost:<port>` 本机回环，明文无 PII；不涉及外部网络 |
| T-01-03 | Denial of Service | session retry 无限循环风险 | mitigate | `_post_tools_call` 内仅 retry 一次（明确 D-04），第二次 `raise_for_status` 抛出；超时由 `timeout_s=60` 上限保护 |
| T-01-04 | Tampering | `mock_client.post` 的 `body` mock 路径 | accept | 仅测试代码，不进 production；`_parse_mcp_response` 已对 `"error"` key 显式 raise |
| T-01-05 | Spoofing | 攻击者监听 localhost:8181 伪装 QMD | accept | 本机攻击假设：若攻击者已在本机执行，QMD 协议层无认证（QMD 自身设计），与 sync QMDService 同风险水位 |
| T-01-06 | Repudiation | session 失效 retry 无日志 | accept | Phase 1 不引入日志框架（OBS-01 在 v2 deferred）；retry 行为通过测试断言验证而非运行时日志 |
| T-01-07 | Elevation of Privilege | `httpx.AsyncClient` 注入式 `http_client` 参数 | mitigate | `_owns_http` 标志保证 ownership 不混淆；`__aexit__` 仅关闭自创 client，避免误关 Phase 3 共享 client |
</threat_model>

<verification>
- `python3 -m pytest tests/test_retriever.py tests/test_retriever_async.py -v` 全 PASS
- `python3 -m pytest tests/ --no-header -q 2>&1 | tail -2` 整体测试通过数 ≥ 173（基线）+ 6（新增）= 179
- `python3 -c "import sys; sys.path.insert(0, '.'); from scripts.retriever import QMDService, AsyncQMDService; import asyncio, inspect; assert inspect.iscoroutinefunction(AsyncQMDService.query)" ` 退出码 0
- `git diff tests/test_retriever.py tests/test_qmd_integration.py | wc -l` 输出 0
- `grep -c "^class " scripts/retriever.py` 输出 ≥ 2（QMDStartupError + QMDService + AsyncQMDService）
</verification>

<success_criteria>
- ROADMAP.md Phase 1 Success Criterion #1 部分达成：`pytest tests/test_retriever_async.py tests/test_retriever.py -v` 全绿（test_kb_metadata.py 由 Plan 01-02 补齐）
- RTR-01: ✓ AsyncQMDService 用 `httpx.AsyncClient` 实现，`async with` 上下文 OK
- RTR-02: ✓ `asyncio.Semaphore(8)` 默认 + 注入参数 `semaphore` 可覆盖
- RTR-03: ✓ `_session_id` 在 `__aenter__` 完成时已就绪；`_post_tools_call` 内 HTTP 400 或缺 session header 触发一次 `_reinitialize_session()` 重试
- RTR-04: ✓ 同步 QMDService 字节级保留；test_retriever.py / test_qmd_integration.py 零修改 + 全绿
</success_criteria>

<output>
After completion, create `.planning/phases/01-async-retriever-kb-metadata-sidecar/01-01-SUMMARY.md` recording:
- AsyncQMDService 公开契约（构造参数、4 个公共方法签名）
- 与同步 QMDService 并存策略与 D-01 释义落定（caller-level async-only，不做物理代码合并）
- 测试覆盖矩阵（B-1..B-6 + crashed-process 共 7 case）
- 已知 trade-off：~80 LOC sync/async lifecycle 代码重复，留待 milestone 后清理
- Phase 3 接入提示：注入 `semaphore=asyncio.Semaphore(opts.concurrency_qmd)` + `http_client=shared_httpx_client`
</output>
