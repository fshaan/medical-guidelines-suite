# v2.2 orchestrate 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 batch_pipeline.py 新增 `orchestrate` 子命令，实现确定性批处理编排（自动扫描知识库、全特征提取、预生成 grep 命令和 prompt），并强化 SKILL.md 约束规则。

**Architecture:** `orchestrate` 在 LLM 执行前预生成所有路径、关键词和 grep 命令，输出自包含的 batch prompt 文件。LLM 只需按 prompt 执行 grep 并分析结果，不再自由编排。三层防御（CONTEXT_RESET + validate 相似度检测 + HARD_CONSTRAINT）保障批次隔离。

**Tech Stack:** Python 3.10+, argparse, json, re, pathlib。无新外部依赖。

**参考文档:**
- `docs/v2.2-fix-plan.md` — 完整修复计划
- `docs/architecture.md` — 工程评审（5 决策 + 3 P0 缺陷）
- `docs/v2.2-decisions.md` — 9 个已确认设计决策（D1-D9）

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `scripts/batch_pipeline.py` | 修改 | 新增 orchestrate 子命令 + 增强 validate/generate + 提取 _split_patients() |
| `SKILL.md` | 修改 | 新增 HARD_CONSTRAINT（执行模式+全覆盖）+ 改写 Step 3 + 扩展 Forbidden Actions |
| `tests/test_orchestrate.py` | 新建 | orchestrate 相关函数的单元测试 |
| `tests/conftest.py` | 新建 | pytest fixtures（mock patients, mock kb） |
| `tests/test_validate_enhance.py` | 新建 | validate 增强功能测试（覆盖率+相似度） |
| `tests/test_generate_enhance.py` | 新建 | generate 增强功能测试（--output-dir, 音序排列） |

---

## Task 1: 测试基础设施 + _split_patients() 提取

**Files:**
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`
- Create: `tests/test_split.py`
- Modify: `scripts/batch_pipeline.py:174-209` (提取 `_split_patients()`)

**决策引用:** D5A — 从 cmd_split 提取分批逻辑为内部函数

- [ ] **Step 1: 创建测试基础设施**

```python
# tests/__init__.py — 空文件

# tests/conftest.py
import json
import pytest
from pathlib import Path

@pytest.fixture
def sample_patients():
    """12 位模拟患者（覆盖 structured + narrative 格式）"""
    patients = []
    for i in range(10):
        patients.append({
            "patient_id": f"P{i+1:03d}",
            "patient_name": f"患者{i+1}",
            "gender": "男" if i % 2 == 0 else "女",
            "age": 50 + i,
            "primary_site": "胃体" if i % 3 != 0 else "食管胃结合部",
            "siewert_type": "II" if i % 3 == 0 else None,
            "pathology": "腺癌" if i % 2 == 0 else "印戒细胞癌",
            "patient_type": "zq_初治" if i < 5 else "sq_术前治疗后（包括外院治疗）",
            "prior_treatment": None if i < 5 else f"{i}C SOX+PD-1",
            "biopsy_molecular": "hj_pMMR/MSS,hj_Her2 0,hj_CPS<1" if i % 2 == 0 else "hj_dMMR/MSI-H,hj_Her2 3+,hj_CPS≥10",
            "gross_molecular": None,
            "abnormal_markers": "CEA,CA199" if i % 3 == 0 else None,
            "marker_change": "升高" if i % 3 == 0 else None,
            "staging_prefix": "c" if i < 5 else "yc",
            "t_stage": f"T{min(i % 4 + 1, 4)}{'b' if i == 3 else 'a' if i % 4 == 3 else ''}",
            "t4b_invasion": "胰腺" if i == 3 else None,
            "n_stage": f"N{min(i % 4, 3)}",
            "m_stage": "M0" if i < 7 else "M1",
            "m_sites": None if i < 7 else "腹膜" if i == 7 else "肝",
            "staging_notes": None,
            "symptom_change": None,
            "response": "不适用" if i < 5 else ["PD", "SD", "PR"][i % 3],
            "tumor_emergency": "出血" if i == 9 else "无",
            "comorbidities": "糖尿病" if i == 8 else None,
            "clinical_narrative": None,
        })
    # 2 位 narrative 格式
    patients.append({
        "patient_id": "N001",
        "patient_name": "叙述患者1",
        "clinical_narrative": "男，69岁，胃中部（M）印戒细胞癌，ycT4aN1M1腹膜转移，活检hj_pMMR/MSS，4C SOX+PD-1，PD。",
        **{k: None for k in [
            "gender", "age", "primary_site", "siewert_type", "pathology",
            "patient_type", "prior_treatment", "lesion_count", "biopsy_molecular",
            "gross_molecular", "abnormal_markers", "marker_change", "staging_prefix",
            "t_stage", "t4b_invasion", "n_stage", "m_stage", "m_sites",
            "staging_notes", "symptom_change", "response", "tumor_emergency", "comorbidities",
        ]},
    })
    patients.append({
        "patient_id": "N002",
        "patient_name": "叙述患者2",
        "clinical_narrative": "女，45岁，胃窦低分化腺癌，cT2N0M0。",
        **{k: None for k in [
            "gender", "age", "primary_site", "siewert_type", "pathology",
            "patient_type", "prior_treatment", "lesion_count", "biopsy_molecular",
            "gross_molecular", "abnormal_markers", "marker_change", "staging_prefix",
            "t_stage", "t4b_invasion", "n_stage", "m_stage", "m_sites",
            "staging_notes", "symptom_change", "response", "tumor_emergency", "comorbidities",
        ]},
    })
    return patients

@pytest.fixture
def patients_json(tmp_path, sample_patients):
    """写入 patients.json 并返回路径"""
    data = {
        "input_format": "structured",
        "input_file": "/mock/input.xlsx",
        "parsed_at": "2026-03-25",
        "patient_count": len(sample_patients),
        "patients": sample_patients,
    }
    p = tmp_path / "patients.json"
    p.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return p

@pytest.fixture
def mock_kb(tmp_path):
    """模拟知识库目录结构"""
    kb = tmp_path / "guidelines"
    for org in ["NCCN", "ESMO", "CSCO"]:
        ext_dir = kb / org / "extracted"
        ext_dir.mkdir(parents=True)
        # 创建模拟 txt 文件
        (ext_dir / f"{org}_GastricCancer.txt").write_text(
            f"Line 1: {org} Gastric Cancer Guideline\n" * 100,
            encoding="utf-8",
        )
        # 创建 org data_structure.md
        (kb / org / "data_structure.md").write_text(
            f"# {org} 指南\n\n## 文件清单\n\n### 提取文件（推荐使用）\n\n"
            f"| 文件 | 版本 | 状态 | 行数 | 说明 |\n"
            f"|------|------|------|------|------|\n"
            f"| extracted/{org}_GastricCancer.txt | 2026 | **默认** | 100 | 胃癌 |\n\n"
            f"## 常用检索关键词\n\n### 诊断相关\n- diagnosis, 诊断\n- HER2\n\n"
            f"### 治疗相关\n- surgery, 手术\n- chemotherapy, 化疗\n",
            encoding="utf-8",
        )
    # 创建根 data_structure.md
    (kb / "data_structure.md").write_text(
        "# 临床指南知识库总览\n\n## 指南目录\n\n"
        "### NCCN/\n- **机构**: National Comprehensive Cancer Network\n- **版本**: 2026\n\n"
        "### ESMO/\n- **机构**: European Society for Medical Oncology\n- **版本**: 2024\n\n"
        "### CSCO/\n- **机构**: Chinese Society of Clinical Oncology\n- **版本**: 2025\n\n"
        "## 常见临床问题 → 指南映射\n\n"
        "| 临床问题类别 | 首选指南 | 补充参考 |\n"
        "|-------------|---------|----------|\n"
        "| 手术术式选择 | NCCN | CSCO |\n"
        "| 围手术期化疗 | NCCN, ESMO | CSCO |\n"
        "| 晚期一线治疗 | NCCN, ESMO | CSCO |\n",
        encoding="utf-8",
    )
    return kb
```

- [ ] **Step 2: 写 _split_patients 的失败测试**

```python
# tests/test_split.py
from scripts.batch_pipeline import _split_patients

def test_split_exact_division(sample_patients):
    """12 患者 / 4 = 3 批"""
    batches = _split_patients(sample_patients, batch_size=4)
    assert len(batches) == 3
    assert all(len(b) == 4 for b in batches)

def test_split_remainder(sample_patients):
    """12 患者 / 5 = 2 批(5) + 1 批(2)"""
    batches = _split_patients(sample_patients, batch_size=5)
    assert len(batches) == 3
    assert len(batches[0]) == 5
    assert len(batches[2]) == 2

def test_split_single_batch():
    """3 患者 / 5 = 1 批"""
    patients = [{"patient_id": f"P{i}"} for i in range(3)]
    batches = _split_patients(patients, batch_size=5)
    assert len(batches) == 1
    assert len(batches[0]) == 3

def test_split_empty():
    batches = _split_patients([], batch_size=5)
    assert batches == []
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd /Users/f.sh/Workspace/Claude/GC_RAG/skills/medical-guidelines-suite && python -m pytest tests/test_split.py -v`
Expected: FAIL — `ImportError: cannot import name '_split_patients'`

- [ ] **Step 4: 提取 _split_patients() 并修改 cmd_split**

在 `scripts/batch_pipeline.py` 中：
1. 在 `cmd_split()` 之前添加 `_split_patients(patients, batch_size)` 函数
2. `cmd_split()` 改为调用 `_split_patients()`

```python
# 在 line 172 (split 区块) 之后添加:

def _split_patients(patients: list[dict], batch_size: int) -> list[list[dict]]:
    """将患者列表分成多个批次（纯函数，供 split 和 orchestrate 共用）"""
    if not patients:
        return []
    return [patients[i:i + batch_size] for i in range(0, len(patients), batch_size)]


# cmd_split 中的 line 192 替换:
# 旧: batches = [patients[i:i + batch_size] for i in range(0, len(patients), batch_size)]
# 新: batches = _split_patients(patients, batch_size)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/f.sh/Workspace/Claude/GC_RAG/skills/medical-guidelines-suite && python -m pytest tests/test_split.py -v`
Expected: 4 passed

- [ ] **Step 6: 提交**

```bash
git add tests/__init__.py tests/conftest.py tests/test_split.py scripts/batch_pipeline.py
git commit -m "refactor: extract _split_patients() for reuse by orchestrate

D5A: share split logic between cmd_split and cmd_orchestrate"
```

---

## Task 2: resolve_kb_root()

**Files:**
- Create: `tests/test_resolve_kb.py`
- Modify: `scripts/batch_pipeline.py` (新增函数)

**决策引用:** v2.2-fix-plan §1.1 — 按优先级解析知识库路径

- [ ] **Step 1: 写失败测试**

```python
# tests/test_resolve_kb.py
import os
import pytest
from scripts.batch_pipeline import resolve_kb_root

def test_explicit_path(mock_kb):
    assert resolve_kb_root(str(mock_kb)) == mock_kb

def test_explicit_path_invalid(tmp_path):
    with pytest.raises(SystemExit):
        resolve_kb_root(str(tmp_path / "nonexistent"))

def test_env_var(mock_kb, monkeypatch):
    monkeypatch.setenv("MEDICAL_GUIDELINES_DIR", str(mock_kb))
    assert resolve_kb_root(None) == mock_kb

def test_local_guidelines(mock_kb, monkeypatch, tmp_path):
    monkeypatch.chdir(tmp_path)
    monkeypatch.delenv("MEDICAL_GUIDELINES_DIR", raising=False)
    # 创建 ./guidelines/data_structure.md
    g = tmp_path / "guidelines"
    g.mkdir()
    (g / "data_structure.md").write_text("# test")
    assert resolve_kb_root(None) == g

def test_local_knowledge(mock_kb, monkeypatch, tmp_path):
    """./knowledge/ 作为备选路径"""
    monkeypatch.chdir(tmp_path)
    monkeypatch.delenv("MEDICAL_GUIDELINES_DIR", raising=False)
    k = tmp_path / "knowledge"
    k.mkdir()
    (k / "data_structure.md").write_text("# test")
    assert resolve_kb_root(None) == k

def test_no_kb_found(monkeypatch, tmp_path):
    monkeypatch.chdir(tmp_path)
    monkeypatch.delenv("MEDICAL_GUIDELINES_DIR", raising=False)
    with pytest.raises(SystemExit):
        resolve_kb_root(None)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_resolve_kb.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 resolve_kb_root()**

在 `scripts/batch_pipeline.py` 中，在 split 区块之后、merge 区块之前添加新的 orchestrate 区块：

```python
# ─── orchestrate 子命令 ──────────────────────────────────────────────────────


def resolve_kb_root(explicit_path: str | None) -> Path:
    """按优先级解析知识库根路径。

    优先级: --kb-root > MEDICAL_GUIDELINES_DIR > ./guidelines/ > ./knowledge/
    验证: 目标路径下存在 data_structure.md
    """
    candidates = []

    if explicit_path:
        candidates.append(Path(explicit_path).resolve())
    else:
        env = os.environ.get("MEDICAL_GUIDELINES_DIR")
        if env:
            candidates.append(Path(env).resolve())
        candidates.append(Path("guidelines").resolve())
        candidates.append(Path("knowledge").resolve())

    for p in candidates:
        if p.is_dir() and (p / "data_structure.md").exists():
            return p

    tried = ", ".join(str(c) for c in candidates)
    print(f"无法找到知识库（需含 data_structure.md）。已尝试: {tried}", file=sys.stderr)
    sys.exit(1)
```

注意：在文件顶部 import 区域添加 `import os`。

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_resolve_kb.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_resolve_kb.py scripts/batch_pipeline.py
git commit -m "feat: add resolve_kb_root() for orchestrate KB path resolution"
```

---

## Task 3: scan_knowledge_base()

**Files:**
- Create: `tests/test_scan_kb.py`
- Modify: `scripts/batch_pipeline.py` (新增函数)

**决策引用:** D1 — 正则匹配表头定位表格，fallback 到目录枚举

- [ ] **Step 1: 写失败测试**

```python
# tests/test_scan_kb.py
import pytest
from scripts.batch_pipeline import scan_knowledge_base

def test_scan_normal(mock_kb):
    """正常解析包含 data_structure.md 的知识库"""
    profile = scan_knowledge_base(mock_kb)
    assert set(profile["orgs"]) == {"NCCN", "ESMO", "CSCO"}
    # 每个 org 有文件列表
    for org in profile["orgs"]:
        assert org in profile["org_files"]
        assert len(profile["org_files"][org]) > 0
    # 根索引内容被保存
    assert "知识库总览" in profile["root_index_content"]

def test_scan_fallback_no_root_ds(mock_kb):
    """根 data_structure.md 格式异常时 fallback 到目录枚举"""
    (mock_kb / "data_structure.md").write_text("这不是有效的markdown表格")
    profile = scan_knowledge_base(mock_kb)
    # fallback 仍应检测到 org 目录
    assert len(profile["orgs"]) == 3

def test_scan_org_no_extracted(mock_kb):
    """某 org 无 extracted/ 子目录时跳过该 org"""
    import shutil
    shutil.rmtree(mock_kb / "ESMO" / "extracted")
    profile = scan_knowledge_base(mock_kb)
    assert "ESMO" not in profile["orgs"]

def test_scan_empty_kb(tmp_path):
    """空知识库"""
    (tmp_path / "data_structure.md").write_text("# Empty")
    profile = scan_knowledge_base(tmp_path)
    assert profile["orgs"] == []

def test_scan_org_no_ds(mock_kb):
    """某 org 无 data_structure.md 时仍可通过 extracted/ 发现"""
    import os
    os.remove(mock_kb / "NCCN" / "data_structure.md")
    profile = scan_knowledge_base(mock_kb)
    assert "NCCN" in profile["orgs"]  # 仍可扫描到（有 extracted/）

def test_scan_clinical_question_map(mock_kb):
    """解析临床问题→指南映射表"""
    profile = scan_knowledge_base(mock_kb)
    if profile.get("clinical_question_map"):
        assert isinstance(profile["clinical_question_map"], dict)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_scan_kb.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 scan_knowledge_base()**

```python
import re

def scan_knowledge_base(kb_root: Path) -> dict:
    """自动扫描知识库结构，返回 kb_profile 字典。

    解析策略 (D1): 正则匹配表头定位 markdown 表格。
    Fallback: 解析失败时通过目录枚举发现 org。
    """
    profile = {
        "orgs": [],
        "org_files": {},
        "org_keywords": {},
        "clinical_question_map": {},
        "root_index_content": "",
    }

    # 1. 读取根 data_structure.md
    root_ds = kb_root / "data_structure.md"
    root_text = ""
    if root_ds.exists():
        root_text = root_ds.read_text(encoding="utf-8")
        profile["root_index_content"] = root_text

    # 2. 发现 org 目录：先尝试从 markdown 解析，fallback 到目录枚举
    parsed_orgs = _parse_org_names_from_root(root_text)
    if not parsed_orgs:
        print("  ⚠ 根 data_structure.md 解析失败，fallback 到目录枚举", file=sys.stderr)
        parsed_orgs = _enumerate_org_dirs(kb_root)

    # 3. 验证每个 org 并扫描详情
    for org in parsed_orgs:
        org_dir = kb_root / org
        extracted_dir = org_dir / "extracted"
        if not extracted_dir.is_dir():
            print(f"  ⚠ {org}/ 无 extracted/ 子目录，跳过", file=sys.stderr)
            continue
        txt_files = sorted(extracted_dir.glob("*.txt"))
        if not txt_files:
            print(f"  ⚠ {org}/extracted/ 无 .txt 文件，跳过", file=sys.stderr)
            continue

        profile["orgs"].append(org)
        profile["org_files"][org] = [
            {
                "file": f.name,
                "path": str(f),
                "lines": sum(1 for _ in f.open(encoding="utf-8")),
            }
            for f in txt_files
        ]

        # 解析 org 级 data_structure.md 的关键词
        org_ds = org_dir / "data_structure.md"
        if org_ds.exists():
            profile["org_keywords"][org] = _parse_keywords_from_ds(
                org_ds.read_text(encoding="utf-8")
            )

    # 4. 解析临床问题→指南映射（从根索引）
    profile["clinical_question_map"] = _parse_clinical_question_map(root_text)

    print(f"  知识库扫描完成: {len(profile['orgs'])} 个组织, "
          f"{sum(len(v) for v in profile['org_files'].values())} 个文件")
    return profile


def _parse_org_names_from_root(text: str) -> list[str]:
    """从根 data_structure.md 解析组织名（### OrgName/ 模式）"""
    orgs = []
    for m in re.finditer(r'^###\s+(\w[\w-]*)/\s*$', text, re.MULTILINE):
        orgs.append(m.group(1))
    return orgs


def _enumerate_org_dirs(kb_root: Path) -> list[str]:
    """Fallback: 枚举知识库根目录下的子目录"""
    return sorted(
        d.name for d in kb_root.iterdir()
        if d.is_dir() and not d.name.startswith(".")
    )


def _parse_keywords_from_ds(text: str) -> dict[str, list[str]]:
    """从 data_structure.md 解析'常用检索关键词'区块"""
    keywords = {}
    current_category = None
    in_keyword_section = False

    for line in text.split("\n"):
        if "检索关键词" in line and line.startswith("#"):
            in_keyword_section = True
            continue
        if in_keyword_section:
            if line.startswith("---") or (line.startswith("#") and "检索" not in line):
                break
            cat_match = re.match(r'^###\s+(.+)', line)
            if cat_match:
                current_category = cat_match.group(1).strip()
                keywords[current_category] = []
                continue
            if current_category and line.startswith("- "):
                items = [k.strip() for k in line[2:].split(",")]
                keywords[current_category].extend(items)

    return keywords


def _parse_clinical_question_map(text: str) -> dict:
    """从根 data_structure.md 解析'临床问题→指南映射'表格"""
    cq_map = {}
    in_table = False

    for line in text.split("\n"):
        if "临床问题" in line and "指南映射" in line:
            in_table = True
            continue
        if in_table:
            if line.startswith("|") and "---" not in line and "临床问题" not in line:
                cols = [c.strip() for c in line.split("|")[1:-1]]
                if len(cols) >= 3:
                    question = cols[0]
                    primary = [g.strip() for g in cols[1].split(",") if g.strip()]
                    supplementary = [g.strip() for g in cols[2].split(",") if g.strip()]
                    cq_map[question] = {"primary": primary, "supplementary": supplementary}
            elif not line.startswith("|") and line.strip():
                break

    return cq_map
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_scan_kb.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_scan_kb.py scripts/batch_pipeline.py
git commit -m "feat: add scan_knowledge_base() with D1 regex+fallback strategy"
```

---

## Task 4: escape_grep_keyword() + generate_grep_commands()

**Files:**
- Create: `tests/test_grep.py`
- Modify: `scripts/batch_pipeline.py` (新增函数)

**决策引用:** D4 — 全部转义 + 自动生成去括号变体

- [ ] **Step 1: 写失败测试**

```python
# tests/test_grep.py
import pytest
from scripts.batch_pipeline import escape_grep_keyword, generate_grep_commands

class TestEscapeGrepKeyword:
    def test_plain_keyword(self):
        assert escape_grep_keyword("HER2") == ["HER2"]

    def test_brackets(self):
        result = escape_grep_keyword("CPS(≥10)")
        assert len(result) == 2
        assert "CPS\\(≥10\\)" in result  # 转义版
        assert any("CPS.*10" in v for v in result)  # 去括号版

    def test_square_brackets(self):
        result = escape_grep_keyword("T4b[胰腺]")
        assert any("\\[" in v for v in result)

    def test_dot_and_star(self):
        result = escape_grep_keyword("V2.0")
        assert "V2\\.0" in result

    def test_no_parens_no_variant(self):
        """无括号关键词不生成变体"""
        result = escape_grep_keyword("gastric")
        assert len(result) == 1


class TestGenerateGrepCommands:
    def test_basic_generation(self, mock_kb):
        """基本 grep 命令生成"""
        features = {
            "diagnosis_keywords": ["胃癌", "gastric"],
            "staging_keywords": ["T4a", "N1"],
            "all_keywords": ["胃癌", "gastric", "T4a", "N1"],
        }
        profile = {
            "orgs": ["NCCN", "CSCO"],
            "org_files": {
                "NCCN": [{"file": "NCCN_GC.txt", "path": "/kb/NCCN/extracted/NCCN_GC.txt"}],
                "CSCO": [{"file": "CSCO_GC.txt", "path": "/kb/CSCO/extracted/CSCO_GC.txt"}],
            },
        }
        cmds = generate_grep_commands(features, profile, mock_kb)
        # 每个 org 都有命令
        org_names = [c["org"] for c in cmds]
        assert "NCCN" in org_names
        assert "CSCO" in org_names
        # 命令包含 grep
        for c in cmds:
            assert c["command"].startswith("grep")

    def test_special_chars_escaped(self, mock_kb):
        """含特殊字符的关键词被正确转义"""
        features = {
            "molecular_keywords": ["CPS(≥10)", "HER2"],
            "all_keywords": ["CPS(≥10)", "HER2"],
        }
        profile = {
            "orgs": ["NCCN"],
            "org_files": {"NCCN": [{"file": "x.txt", "path": "/kb/NCCN/extracted/x.txt"}]},
        }
        cmds = generate_grep_commands(features, profile, mock_kb)
        # 命令中括号已转义
        for c in cmds:
            assert "CPS(" not in c["command"]  # raw parens not in command

    def test_empty_keywords(self, mock_kb):
        features = {"all_keywords": []}
        profile = {"orgs": ["NCCN"], "org_files": {"NCCN": [{"file": "x.txt", "path": "/p/x.txt"}]}}
        cmds = generate_grep_commands(features, profile, mock_kb)
        assert cmds == []
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_grep.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 escape_grep_keyword() 和 generate_grep_commands()**

```python
# grep 特殊字符转义
_GREP_SPECIAL = re.compile(r'([\[\]().*+?{}\\^$|])')
_PAREN_CONTENT = re.compile(r'\(([^)]*)\)')


def escape_grep_keyword(keyword: str) -> list[str]:
    """转义 grep 特殊字符，含括号时额外生成去括号变体 (D4)。

    Returns: 1-2 个转义后的关键词列表
    """
    escaped = _GREP_SPECIAL.sub(r'\\\1', keyword)
    variants = [escaped]

    # D4: 含括号时生成去括号变体
    if "(" in keyword or "[" in keyword:
        # 提取括号外的核心部分和括号内的数字/关键内容
        stripped = re.sub(r'[()[\]]', '', keyword)
        # 提取核心 token（去掉标点和空白后的非空部分）
        tokens = [t for t in re.split(r'[^\w\u4e00-\u9fff≥≤><]+', stripped) if t]
        if len(tokens) >= 2:
            variant = ".*".join(_GREP_SPECIAL.sub(r'\\\1', t) for t in tokens)
            variants.append(variant)

    return variants


def generate_grep_commands(
    patient_features: dict,
    kb_profile: dict,
    kb_root: Path,
) -> list[dict]:
    """为一位患者生成覆盖所有 org 的 grep 命令。

    按临床维度合并关键词到单条 grep（用 \\| 分隔），减少命令数。
    返回: [{"org": str, "dimension": str, "command": str}, ...]
    """
    all_kw = patient_features.get("all_keywords", [])
    if not all_kw:
        return []

    # 按维度分组（去掉 all_keywords 这个合并键）
    dimensions = {}
    for key, val in patient_features.items():
        if key == "all_keywords" or not val:
            continue
        if key.endswith("_keywords"):
            dim_name = key.replace("_keywords", "")
            dimensions[dim_name] = val

    # 如果没有维度分组，将所有关键词作为一个维度
    if not dimensions:
        dimensions["general"] = all_kw

    commands = []
    for org in kb_profile["orgs"]:
        files = kb_profile["org_files"].get(org, [])
        if not files:
            continue
        # 构建 glob pattern
        glob_pattern = str(kb_root / org / "extracted" / "*.txt")

        for dim_name, keywords in dimensions.items():
            # 转义所有关键词并展平变体
            all_variants = []
            for kw in keywords:
                all_variants.extend(escape_grep_keyword(kw))

            if not all_variants:
                continue

            # 合并到单条 grep (用 \| 分隔)
            pattern = "\\|".join(all_variants)
            cmd = f'grep -n -i "{pattern}" {glob_pattern}'
            commands.append({
                "org": org,
                "dimension": dim_name,
                "command": cmd,
            })

    return commands
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_grep.py -v`
Expected: 7 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_grep.py scripts/batch_pipeline.py
git commit -m "feat: add escape_grep_keyword() and generate_grep_commands()

D4: escape all grep special chars + auto-generate parenthesis-stripped variants"
```

---

## Task 5: extract_patient_features()

**Files:**
- Create: `tests/test_features.py`
- Modify: `scripts/batch_pipeline.py` (新增函数)

**决策引用:** D2 — 正则扫描 + confidence 标记

- [ ] **Step 1: 写失败测试**

```python
# tests/test_features.py
import pytest
from scripts.batch_pipeline import extract_patient_features

def test_structured_full_fields(sample_patients):
    """结构化格式，字段齐全的患者"""
    p = sample_patients[0]  # P001: 男, 50岁, 胃体, 腺癌, cT1N0M0, pMMR/MSS
    features = extract_patient_features(p)
    assert "胃" in " ".join(features["diagnosis_keywords"])
    assert any("T1" in k or "t1" in k.lower() for k in features["staging_keywords"])
    assert "pMMR" in " ".join(features["molecular_keywords"]) or \
           "MSS" in " ".join(features["molecular_keywords"])
    assert features["confidence"] == "high"
    assert len(features["all_keywords"]) > 0

def test_structured_m1_with_metastasis(sample_patients):
    """M1 患者应提取转移部位关键词"""
    p = sample_patients[7]  # P008: M1 腹膜
    features = extract_patient_features(p)
    assert any("腹膜" in k or "peritoneal" in k.lower()
               for k in features["metastasis_keywords"])

def test_structured_with_comorbidity(sample_patients):
    """合并症应被提取"""
    p = sample_patients[8]  # P009: 糖尿病
    features = extract_patient_features(p)
    assert any("糖尿病" in k for k in features["comorbidity_keywords"])

def test_structured_tumor_emergency(sample_patients):
    """肿瘤急症应被提取"""
    p = sample_patients[9]  # P010: 出血
    features = extract_patient_features(p)
    assert any("出血" in k or "bleeding" in k.lower()
               for k in features["event_keywords"])

def test_structured_prior_treatment(sample_patients):
    """既往治疗应被提取"""
    p = sample_patients[5]  # P006: 5C SOX+PD-1
    features = extract_patient_features(p)
    assert any("SOX" in k or "PD-1" in k for k in features["treatment_keywords"])

def test_narrative_format(sample_patients):
    """Narrative 格式通过正则提取"""
    p = sample_patients[10]  # N001: 胃中部印戒细胞癌 ycT4aN1M1腹膜转移
    features = extract_patient_features(p)
    assert len(features["all_keywords"]) > 0
    assert any("胃" in k for k in features["diagnosis_keywords"])

def test_narrative_sparse_low_confidence(sample_patients):
    """信息稀疏的 narrative → confidence: low"""
    p = sample_patients[11]  # N002: "女，45岁，胃窦低分化腺癌，cT2N0M0。"
    features = extract_patient_features(p)
    # 这个叙述覆盖了诊断+分期，可能刚好 ≤2 维度取决于实现
    assert features["confidence"] in ("high", "low")

def test_all_none_patient():
    """所有字段为 None"""
    p = {k: None for k in ["patient_id", "patient_name", "primary_site", "pathology",
                            "biopsy_molecular", "t_stage", "n_stage", "m_stage",
                            "m_sites", "comorbidities", "clinical_narrative"]}
    p["patient_id"] = "EMPTY"
    features = extract_patient_features(p)
    assert features["confidence"] == "low"
    assert features["all_keywords"] == [] or len(features["all_keywords"]) == 0

def test_feature_dimensions():
    """返回结构包含 9 个维度 + all_keywords + confidence"""
    p = {"patient_id": "X", "primary_site": "胃", "pathology": "腺癌",
         "biopsy_molecular": "hj_HER2 3+", "t_stage": "T3", "n_stage": "N1",
         "m_stage": "M0", "clinical_narrative": None}
    features = extract_patient_features(p)
    expected_keys = {
        "diagnosis_keywords", "staging_keywords", "metastasis_keywords",
        "molecular_keywords", "treatment_keywords", "marker_keywords",
        "event_keywords", "comorbidity_keywords", "special_keywords",
        "all_keywords", "confidence",
    }
    assert expected_keys.issubset(set(features.keys()))
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_features.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 extract_patient_features()**

这是最大的单个函数（~120 行）。核心逻辑：

```python
# ─── 临床特征正则字典（用于 narrative 格式扫描）───

_MOLECULAR_PATTERNS = re.compile(
    r'(HER2|Her2|her2|MSI-H|MSS|dMMR|pMMR|PD-L1|CPS[≥<>\d]+|EGFR|ALK|ROS1|'
    r'NTRK|BRAF|KRAS|NRAS|PIK3CA|FGFR2|Claudin[\s-]?18)',
    re.IGNORECASE,
)
_STAGING_PATTERNS = re.compile(
    r'((?:yc|c|p)?T[1-4][a-d]?|N[0-3][a-b]?|M[01]|'
    r'stage\s*(?:I{1,3}V?|IV)|[IⅠⅡⅢⅣ]+[A-C]?期)',
    re.IGNORECASE,
)
_METASTASIS_SITES = {
    "腹膜": ["腹膜", "peritoneal", "peritoneum"],
    "肝": ["肝", "liver", "hepatic"],
    "肺": ["肺", "lung", "pulmonary"],
    "骨": ["骨", "bone", "osseous"],
    "脑": ["脑", "brain", "cerebral"],
    "卵巢": ["卵巢", "ovarian", "Krukenberg"],
    "淋巴结": ["远处淋巴结", "distant lymph", "Virchow"],
}
_TREATMENT_PATTERNS = re.compile(
    r'(SOX|XELOX|CAPOX|FOLFOX|FLOT|S-1|替吉奥|卡培他滨|奥沙利铂|'
    r'PD-1|PD-L1|pembrolizumab|nivolumab|trastuzumab|'
    r'曲妥珠单抗|帕博利珠单抗|纳武利尤单抗|信迪利单抗|替雷利珠单抗|'
    r'化疗|靶向|免疫|放疗|内镜|手术|'
    r'\d+C\s+\w+)',
    re.IGNORECASE,
)
_EMERGENCY_KEYWORDS = ["出血", "bleeding", "梗阻", "obstruction", "穿孔", "perforation", "急症"]
_COMORBIDITY_PATTERNS = re.compile(
    r'(糖尿病|diabetes|高血压|hypertension|肾功能不全|renal|心[脏功]|cardiac|'
    r'肝硬化|cirrhosis|COPD|肺功能|elderly|高龄)',
    re.IGNORECASE,
)


def extract_patient_features(patient: dict) -> dict:
    """从患者数据提取 9 维临床特征关键词 (D2: 正则扫描 + confidence)。

    structured 格式: 从字段直接提取
    narrative 格式: 正则扫描 clinical_narrative
    """
    features = {
        "diagnosis_keywords": [],
        "staging_keywords": [],
        "metastasis_keywords": [],
        "molecular_keywords": [],
        "treatment_keywords": [],
        "marker_keywords": [],
        "event_keywords": [],
        "comorbidity_keywords": [],
        "special_keywords": [],
    }

    narrative = patient.get("clinical_narrative")
    is_narrative = narrative and not patient.get("primary_site")

    if is_narrative:
        _extract_from_narrative(narrative, features)
    else:
        _extract_from_structured(patient, features)

    # 去重合并
    all_kw = []
    seen = set()
    for key in features:
        for kw in features[key]:
            if kw.lower() not in seen:
                seen.add(kw.lower())
                all_kw.append(kw)
    features["all_keywords"] = all_kw

    # D2: confidence 标记
    dimensions_hit = sum(1 for k, v in features.items()
                         if k.endswith("_keywords") and k != "all_keywords" and v)
    features["confidence"] = "low" if dimensions_hit <= 2 else "high"

    return features


def _extract_from_structured(p: dict, features: dict):
    """从结构化字段提取关键词"""
    # 诊断维度
    if p.get("primary_site"):
        features["diagnosis_keywords"].extend([p["primary_site"], "gastric", "胃癌"])
    if p.get("pathology"):
        features["diagnosis_keywords"].append(p["pathology"])

    # 分期维度
    for field in ("staging_prefix", "t_stage", "n_stage", "m_stage"):
        val = p.get(field)
        if val:
            features["staging_keywords"].append(val)
    # 拼接完整分期
    prefix = p.get("staging_prefix", "")
    t = p.get("t_stage", "")
    n = p.get("n_stage", "")
    m = p.get("m_stage", "")
    if t and n:
        features["staging_keywords"].append(f"{prefix}{t}{n}{m}")

    # 转移维度
    if p.get("m_sites"):
        sites = re.split(r'[,，、/]', p["m_sites"])
        for s in sites:
            s = s.strip()
            if s:
                features["metastasis_keywords"].append(s)
                for en_name, aliases in _METASTASIS_SITES.items():
                    if any(a in s for a in aliases):
                        features["metastasis_keywords"].extend(aliases)
                        break
    if p.get("t4b_invasion"):
        features["metastasis_keywords"].append(p["t4b_invasion"])

    # 分子维度
    for field in ("biopsy_molecular", "gross_molecular"):
        val = p.get(field)
        if val:
            # 去掉 hj_ 前缀，按逗号分割
            items = re.split(r'[,，]', val.replace("hj_", ""))
            features["molecular_keywords"].extend(i.strip() for i in items if i.strip())

    # 治疗维度
    if p.get("prior_treatment"):
        matches = _TREATMENT_PATTERNS.findall(p["prior_treatment"])
        features["treatment_keywords"].extend(matches)
    if p.get("patient_type"):
        if "初治" in p["patient_type"]:
            features["treatment_keywords"].extend(["初治", "treatment-naive", "first-line"])
        elif "术前" in p["patient_type"] or "sq_" in p["patient_type"]:
            features["treatment_keywords"].extend(["术前治疗后", "post-neoadjuvant"])
    if p.get("response"):
        r = p["response"]
        if r not in ("不适用", None, ""):
            features["treatment_keywords"].append(r)

    # 标记物维度
    if p.get("abnormal_markers"):
        markers = re.split(r'[,，、]', p["abnormal_markers"])
        features["marker_keywords"].extend(m.strip() for m in markers if m.strip())
    if p.get("marker_change"):
        features["marker_keywords"].append(p["marker_change"])

    # 临床事件维度
    if p.get("tumor_emergency") and p["tumor_emergency"] != "无":
        features["event_keywords"].append(p["tumor_emergency"])
        for ek in _EMERGENCY_KEYWORDS:
            if ek in p["tumor_emergency"]:
                features["event_keywords"].append(ek)

    # 合并症维度
    if p.get("comorbidities"):
        features["comorbidity_keywords"].append(p["comorbidities"])
        matches = _COMORBIDITY_PATTERNS.findall(p["comorbidities"])
        features["comorbidity_keywords"].extend(matches)

    # 特殊维度
    if p.get("siewert_type"):
        features["special_keywords"].extend([
            f"Siewert {p['siewert_type']}",
            "EGJ", "食管胃结合部",
        ])
    age = p.get("age")
    if age and isinstance(age, int) and age >= 75:
        features["special_keywords"].extend(["高龄", "elderly"])


def _extract_from_narrative(text: str, features: dict):
    """从 narrative 文本正则扫描所有维度"""
    # 诊断
    site_patterns = ["胃", "食管", "结肠", "直肠", "gastric", "esophag", "colon", "rectal"]
    for sp in site_patterns:
        if sp in text.lower() or sp in text:
            features["diagnosis_keywords"].append(sp)
    path_types = ["腺癌", "印戒", "鳞癌", "adenocarcinoma", "signet", "squamous"]
    for pt in path_types:
        if pt in text.lower() or pt in text:
            features["diagnosis_keywords"].append(pt)

    # 分期
    features["staging_keywords"].extend(_STAGING_PATTERNS.findall(text))

    # 转移
    for cn_name, aliases in _METASTASIS_SITES.items():
        for a in aliases:
            if a in text:
                features["metastasis_keywords"].extend(aliases)
                break

    # 分子
    features["molecular_keywords"].extend(_MOLECULAR_PATTERNS.findall(text))

    # 治疗
    features["treatment_keywords"].extend(_TREATMENT_PATTERNS.findall(text))

    # 标记物
    marker_pats = ["CEA", "CA199", "CA724", "CA125", "AFP"]
    for mp in marker_pats:
        if mp in text.upper():
            features["marker_keywords"].append(mp)

    # 事件
    for ek in _EMERGENCY_KEYWORDS:
        if ek in text.lower() or ek in text:
            features["event_keywords"].append(ek)

    # 合并症
    features["comorbidity_keywords"].extend(_COMORBIDITY_PATTERNS.findall(text))

    # 特殊
    siewert_match = re.search(r'Siewert\s*(?:type\s*)?([IⅠⅡⅢ123]+)', text, re.IGNORECASE)
    if siewert_match:
        features["special_keywords"].extend(["Siewert", "EGJ", "食管胃结合部"])
    age_match = re.search(r'(\d{2,3})\s*岁', text)
    if age_match and int(age_match.group(1)) >= 75:
        features["special_keywords"].extend(["高龄", "elderly"])
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_features.py -v`
Expected: 9 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_features.py scripts/batch_pipeline.py
git commit -m "feat: add extract_patient_features() with 9 clinical dimensions

D2: regex extraction + confidence tagging (low when ≤2 dimensions hit)"
```

---

## Task 6: generate_batch_prompt() + prompt 大小检测

**Files:**
- Create: `tests/test_prompt.py`
- Modify: `scripts/batch_pipeline.py` (新增函数)

**决策引用:** D3 — 自动拆分子批次；D6 — 默认 80K tokens

- [ ] **Step 1: 写失败测试**

```python
# tests/test_prompt.py
import pytest
from scripts.batch_pipeline import generate_batch_prompt, estimate_tokens

def test_prompt_contains_context_reset():
    """prompt 开头包含 CONTEXT_RESET"""
    prompt = generate_batch_prompt(
        batch=[{"patient_id": "P001", "patient_name": "测试", "features": {
            "all_keywords": ["胃癌"], "confidence": "high",
            "diagnosis_keywords": ["胃癌"],
        }, "grep_commands": [{"org": "NCCN", "dimension": "diagnosis", "command": "grep -n -i 胃癌 /path/*.txt"}]}],
        kb_profile={"root_index_content": "# 测试知识库", "orgs": ["NCCN"]},
        kb_root="/path/to/kb",
        batch_idx=1, total_batches=3,
    )
    assert "<CONTEXT_RESET>" in prompt
    assert "MANDATORY_RULES" in prompt

def test_prompt_contains_all_orgs():
    """prompt 包含所有 org 的 grep 命令"""
    batch = [{
        "patient_id": "P001", "patient_name": "测试",
        "features": {"all_keywords": ["HER2"], "confidence": "high", "molecular_keywords": ["HER2"]},
        "grep_commands": [
            {"org": "NCCN", "dimension": "molecular", "command": "grep -n -i HER2 /nccn/*.txt"},
            {"org": "ESMO", "dimension": "molecular", "command": "grep -n -i HER2 /esmo/*.txt"},
        ],
    }]
    prompt = generate_batch_prompt(
        batch=batch,
        kb_profile={"root_index_content": "# KB", "orgs": ["NCCN", "ESMO"]},
        kb_root="/kb", batch_idx=1, total_batches=1,
    )
    assert "NCCN" in prompt
    assert "ESMO" in prompt

def test_prompt_contains_patient_info():
    """prompt 包含患者完整信息"""
    batch = [{
        "patient_id": "P001", "patient_name": "张三",
        "primary_site": "胃体",
        "features": {"all_keywords": ["胃癌"], "confidence": "high", "diagnosis_keywords": ["胃癌"]},
        "grep_commands": [{"org": "NCCN", "dimension": "d", "command": "grep x"}],
    }]
    prompt = generate_batch_prompt(
        batch=batch,
        kb_profile={"root_index_content": "# KB", "orgs": ["NCCN"]},
        kb_root="/kb", batch_idx=1, total_batches=1,
    )
    assert "张三" in prompt
    assert "P001" in prompt

def test_estimate_tokens():
    """token 估算（1 token ≈ 4 字符）"""
    text = "a" * 400
    assert 90 <= estimate_tokens(text) <= 110  # ~100 tokens

def test_prompt_low_confidence_hint():
    """confidence: low 时 prompt 包含提示"""
    batch = [{
        "patient_id": "P001", "patient_name": "稀疏",
        "features": {"all_keywords": ["胃"], "confidence": "low", "diagnosis_keywords": ["胃"]},
        "grep_commands": [{"org": "NCCN", "dimension": "d", "command": "grep x"}],
    }]
    prompt = generate_batch_prompt(
        batch=batch,
        kb_profile={"root_index_content": "# KB", "orgs": ["NCCN"]},
        kb_root="/kb", batch_idx=1, total_batches=1,
    )
    assert "稀疏" in prompt or "补充" in prompt or "low" in prompt.lower()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_prompt.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 generate_batch_prompt() 和 estimate_tokens()**

```python
def estimate_tokens(text: str) -> int:
    """粗略估算 token 数（中英文混合按 1 token ≈ 4 字符）"""
    return len(text) // 4


def generate_batch_prompt(
    batch: list[dict],
    kb_profile: dict,
    kb_root: str,
    batch_idx: int,
    total_batches: int,
    output_file: str = "",
) -> str:
    """生成自包含的批次 prompt 文件内容。"""
    lines = []

    # 头部 + CONTEXT_RESET
    lines.append(f"# 批次 {batch_idx:03d}/{total_batches:03d} 检索任务\n")
    lines.append("<CONTEXT_RESET>")
    lines.append("请忽略此消息之前的所有检索结果和患者信息。")
    lines.append("以下是一个全新的、独立的批次任务，从零开始处理。")
    lines.append("不得引用或参考任何其他批次的结果。")
    lines.append("不得使用"同上"、"与前面类似"、"参考前述"等表述。")
    lines.append("</CONTEXT_RESET>\n")

    # MANDATORY_RULES
    lines.append("<MANDATORY_RULES>")
    lines.append("1. 必须逐条执行以下所有 grep 命令，不得跳过任何组织")
    lines.append("2. 每位患者的每个指南组织都必须有检索结果")
    lines.append("3. 如果某指南未涉及该问题，记录: \"该指南未涉及此临床问题\"")
    lines.append("4. 输出必须为简体中文")
    lines.append("5. 可以补充脚本未生成的关键词，但不得删减已有的 grep 命令")
    lines.append("</MANDATORY_RULES>\n")

    # 知识库信息
    lines.append(f"## 知识库\n路径: {kb_root}\n")
    lines.append("根索引:\n---")
    lines.append(kb_profile.get("root_index_content", ""))
    lines.append("---\n")

    # 患者列表
    lines.append(f"## 患者列表（本批次 {len(batch)} 人）\n")

    for pi, patient in enumerate(batch, 1):
        pid = patient.get("patient_id", "?")
        pname = patient.get("patient_name", "?")
        features = patient.get("features", {})
        grep_cmds = patient.get("grep_commands", [])

        lines.append(f"### 患者 {pi}: {pname} ({pid})\n")

        # 完整临床信息（所有非空字段）
        lines.append("**临床信息:**")
        for k, v in patient.items():
            if k in ("features", "grep_commands") or v is None:
                continue
            lines.append(f"- {k}: {v}")

        # confidence 提示
        confidence = features.get("confidence", "high")
        lines.append(f"\n**脚本提取置信度**: {confidence}")
        if confidence == "low":
            lines.append("⚠ 该患者信息稀疏，请从临床叙述中补充推断关键词并扩展检索范围。")

        # 关键词
        lines.append("\n**脚本提取的关键词:**")
        for dim_key in sorted(features.keys()):
            if dim_key.endswith("_keywords") and dim_key != "all_keywords":
                kws = features[dim_key]
                if kws:
                    dim_name = dim_key.replace("_keywords", "")
                    lines.append(f"- {dim_name}: {', '.join(kws)}")

        # grep 命令
        if grep_cmds:
            lines.append(f"\n#### 必须执行的 grep 命令（共 {len(grep_cmds)} 条，不得跳过）\n")
            current_org = None
            cmd_num = 0
            for gc in grep_cmds:
                if gc["org"] != current_org:
                    current_org = gc["org"]
                    lines.append(f"**{current_org}（必须）:**")
                cmd_num += 1
                lines.append(f"{cmd_num}. {gc['command']}")

        lines.append("\n#### LLM 补充检索空间")
        lines.append("如果在上述 grep 结果中发现需要进一步深入的线索，")
        lines.append("可补充执行额外的 grep 命令（限同一知识库范围内）。\n")

    # 输出要求（需要由调用方传入 output_file 路径）
    # generate_batch_prompt 签名需新增 output_file 参数
    lines.append("## 输出要求\n")
    lines.append(f"- 文件路径: {{output_file}}")
    lines.append("- 格式: JSON")
    lines.append("- 每个 guideline_results 条目必须包含: guideline, version, recommendation, evidence_level, source_file, source_lines")
    lines.append("- 输出语言: 简体中文")

    return "\n".join(lines)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_prompt.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
git add tests/test_prompt.py scripts/batch_pipeline.py
git commit -m "feat: add generate_batch_prompt() with CONTEXT_RESET and MANDATORY_RULES

D3/D6: prompt generation with auto-split support (80K default threshold)"
```

---

## Task 7: cmd_orchestrate() 主入口

**Files:**
- Create: `tests/test_orchestrate.py`
- Modify: `scripts/batch_pipeline.py` (新增 cmd_orchestrate + CLI 注册)

**决策引用:** D3（自动拆分）, D5（plan schema）, D7（并发保护）

- [ ] **Step 1: 写失败测试**

```python
# tests/test_orchestrate.py
import json
import pytest
from unittest.mock import patch
from pathlib import Path

def test_orchestrate_creates_plan(mock_kb, patients_json, tmp_path):
    """orchestrate 生成 orchestration_plan.json"""
    from scripts.batch_pipeline import cmd_orchestrate
    import argparse

    output_dir = tmp_path / "batches"
    args = argparse.Namespace(
        patients=str(patients_json),
        kb_root=str(mock_kb),
        output_dir=str(output_dir),
        batch_size=5,
        max_prompt_tokens=80000,
    )
    cmd_orchestrate(args)

    plan_file = output_dir / "orchestration_plan.json"
    assert plan_file.exists()
    plan = json.loads(plan_file.read_text(encoding="utf-8"))
    assert plan["version"] == "2.2"
    assert plan["total_patients"] == 12
    assert len(plan["batches"]) >= 1
    assert plan["stats"]["orgs_covered"] == ["CSCO", "ESMO", "NCCN"]

def test_orchestrate_creates_prompt_files(mock_kb, patients_json, tmp_path):
    """orchestrate 为每个批次生成 prompt 文件"""
    from scripts.batch_pipeline import cmd_orchestrate
    import argparse

    output_dir = tmp_path / "batches"
    args = argparse.Namespace(
        patients=str(patients_json),
        kb_root=str(mock_kb),
        output_dir=str(output_dir),
        batch_size=5,
        max_prompt_tokens=80000,
    )
    cmd_orchestrate(args)

    prompt_files = sorted(output_dir.glob("batch_*_prompt.md"))
    assert len(prompt_files) >= 2  # 12 patients / 5 = 3 batches

    # 每个 prompt 包含 CONTEXT_RESET
    for pf in prompt_files:
        content = pf.read_text(encoding="utf-8")
        assert "<CONTEXT_RESET>" in content

def test_orchestrate_checkpoint_detection(mock_kb, patients_json, tmp_path):
    """已有 rag_batch 文件时标记为 completed"""
    from scripts.batch_pipeline import cmd_orchestrate
    import argparse

    output_dir = tmp_path / "batches"
    output_dir.mkdir(parents=True)
    # 模拟已完成的 batch_001
    (output_dir / "rag_batch_001.json").write_text(
        json.dumps({"results": [{"patient_id": "P001"}]}), encoding="utf-8"
    )

    args = argparse.Namespace(
        patients=str(patients_json),
        kb_root=str(mock_kb),
        output_dir=str(output_dir),
        batch_size=5,
        max_prompt_tokens=80000,
    )
    cmd_orchestrate(args)

    plan = json.loads((output_dir / "orchestration_plan.json").read_text(encoding="utf-8"))
    assert plan["batches"][0]["status"] == "completed"

def test_orchestrate_existing_plan_resume(mock_kb, patients_json, tmp_path):
    """D7: 已有 plan 时进入 checkpoint 续跑模式"""
    from scripts.batch_pipeline import cmd_orchestrate
    import argparse

    output_dir = tmp_path / "batches"
    output_dir.mkdir(parents=True)
    # 放一个旧 plan
    old_plan = {"version": "2.2", "batches": [{"id": "batch_001", "status": "pending"}]}
    (output_dir / "orchestration_plan.json").write_text(json.dumps(old_plan), encoding="utf-8")

    args = argparse.Namespace(
        patients=str(patients_json),
        kb_root=str(mock_kb),
        output_dir=str(output_dir),
        batch_size=5,
        max_prompt_tokens=80000,
    )
    # 应该正常运行（覆盖旧 plan）
    cmd_orchestrate(args)
    plan = json.loads((output_dir / "orchestration_plan.json").read_text(encoding="utf-8"))
    assert plan["total_patients"] == 12
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_orchestrate.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 cmd_orchestrate()**

```python
from datetime import datetime


def cmd_orchestrate(args):
    """orchestrate 子命令入口 — 自动编排批处理流程"""
    # 1. 解析知识库路径
    kb_root = resolve_kb_root(getattr(args, 'kb_root', None))
    print(f"知识库路径: {kb_root}")

    # 2. 自动扫描知识库
    kb_profile = scan_knowledge_base(kb_root)
    if not kb_profile["orgs"]:
        print("知识库为空（无有效 org 目录）", file=sys.stderr)
        sys.exit(1)

    # 3. 加载患者数据
    patients_path = Path(args.patients).resolve()
    if not patients_path.exists():
        print(f"患者文件不存在: {patients_path}", file=sys.stderr)
        sys.exit(1)
    patients_data = json.loads(patients_path.read_text(encoding="utf-8"))
    patients = patients_data.get("patients", [])
    if not patients:
        print("患者列表为空", file=sys.stderr)
        sys.exit(1)

    # 4. 对每个患者提取特征 + 生成 grep 命令
    enriched_patients = []
    total_grep = 0
    total_kw = 0
    for p in patients:
        features = extract_patient_features(p)
        grep_cmds = generate_grep_commands(features, kb_profile, kb_root)
        enriched = {**p, "features": features, "grep_commands": grep_cmds}
        enriched_patients.append(enriched)
        total_grep += len(grep_cmds)
        total_kw += len(features.get("all_keywords", []))

    # 5. 分批
    batch_size = args.batch_size
    max_tokens = args.max_prompt_tokens
    batches = _split_patients(enriched_patients, batch_size)

    # D3: 自动拆分超限批次
    final_batches = []
    for batch in batches:
        prompt = generate_batch_prompt(batch, kb_profile, str(kb_root),
                                       len(final_batches) + 1, len(batches))
        tokens = estimate_tokens(prompt)
        if tokens > max_tokens and len(batch) > 1:
            # 递归拆分
            sub_batches = _auto_split_batch(batch, kb_profile, str(kb_root),
                                            max_tokens)
            final_batches.extend(sub_batches)
        else:
            final_batches.append(batch)

    # 6. 输出目录
    output_dir = Path(args.output_dir).resolve()
    output_dir.mkdir(parents=True, exist_ok=True)

    # D7: 检测已有 plan（差异化处理）
    existing_plan_path = output_dir / "orchestration_plan.json"
    if existing_plan_path.exists():
        try:
            old_plan = json.loads(existing_plan_path.read_text(encoding="utf-8"))
            old_batches = old_plan.get("batches", [])
            pending = [b for b in old_batches if b.get("status") == "pending"]
            completed = [b for b in old_batches if b.get("status") == "completed"]
            if pending:
                print(f"  ℹ 检测到已有计划: {len(completed)} 批已完成, {len(pending)} 批待处理。进入续跑模式。")
            elif completed:
                print(f"  ℹ 检测到已有计划且全部完成 ({len(completed)} 批)。将重新生成。")
                print(f"     如需保留旧结果，请指定不同的 --output-dir。")
        except (json.JSONDecodeError, KeyError):
            print(f"  ⚠ 已有 orchestration_plan.json 格式异常，将重新生成。")

    # 7. 写 prompt 文件 + 构建 plan
    plan_batches = []
    for bi, batch in enumerate(final_batches, 1):
        batch_id = f"batch_{bi:03d}"
        prompt_file = output_dir / f"{batch_id}_prompt.md"
        output_file = output_dir / f"rag_{batch_id}.json"

        # 检测 checkpoint
        status = "pending"
        if output_file.exists():
            try:
                check = json.loads(output_file.read_text(encoding="utf-8"))
                if check.get("results"):
                    status = "completed"
                    print(f"  ✓ {batch_id} 已完成 (checkpoint)")
            except (json.JSONDecodeError, KeyError):
                pass

        # 只为 pending 批次生成 prompt
        if status == "pending":
            prompt = generate_batch_prompt(
                batch, kb_profile, str(kb_root), bi, len(final_batches)
            )
            prompt_file.write_text(prompt, encoding="utf-8")

        plan_batches.append({
            "id": batch_id,
            "prompt_file": str(prompt_file),
            "output_file": str(output_file),
            "patients": [p.get("patient_id") for p in batch],
            "status": status,
        })

    # 8. 写 orchestration_plan.json (D5)
    plan = {
        "version": "2.2",
        "created_at": datetime.now().isoformat(),
        "kb_root": str(kb_root),
        "kb_profile": {
            "orgs": kb_profile["orgs"],
            "org_files": kb_profile["org_files"],
        },
        "total_patients": len(patients),
        "batch_size": batch_size,
        "batches": plan_batches,
        "next_steps": [
            f"python scripts/batch_pipeline.py merge --input-dir {output_dir} --output {output_dir.parent / 'rag_results.json'}",
            f"python scripts/batch_pipeline.py validate --input {output_dir.parent / 'rag_results.json'} --patients {patients_path}",
            f"python scripts/batch_pipeline.py generate --input {output_dir.parent / 'rag_results.json'} --format all",
        ],
        "stats": {
            "total_grep_commands": total_grep,
            "orgs_covered": sorted(kb_profile["orgs"]),
            "avg_keywords_per_patient": round(total_kw / len(patients), 1) if patients else 0,
        },
    }
    plan_path = output_dir / "orchestration_plan.json"
    plan_path.write_text(json.dumps(plan, ensure_ascii=False, indent=2), encoding="utf-8")

    # 报告
    pending = sum(1 for b in plan_batches if b["status"] == "pending")
    completed = sum(1 for b in plan_batches if b["status"] == "completed")
    print(f"\n编排完成:")
    print(f"  患者: {len(patients)}")
    print(f"  批次: {len(final_batches)} (待处理: {pending}, 已完成: {completed})")
    print(f"  grep 命令总数: {total_grep}")
    print(f"  组织覆盖: {', '.join(sorted(kb_profile['orgs']))}")
    print(f"  计划文件: {plan_path}")


def _auto_split_batch(
    batch: list[dict],
    kb_profile: dict,
    kb_root: str,
    max_tokens: int,
) -> list[list[dict]]:
    """D3: 递归拆分超限批次"""
    if len(batch) <= 1:
        return [batch]

    mid = len(batch) // 2
    left, right = batch[:mid], batch[mid:]
    result = []

    for sub in (left, right):
        prompt = generate_batch_prompt(sub, kb_profile, kb_root, 1, 999)
        if estimate_tokens(prompt) > max_tokens and len(sub) > 1:
            result.extend(_auto_split_batch(sub, kb_profile, kb_root, max_tokens))
        else:
            result.append(sub)

    return result
```

- [ ] **Step 4: 注册 CLI 子命令**

在 `main()` 函数中（`scripts/batch_pipeline.py` 尾部），在 split 和 merge 之间添加：

```python
    # orchestrate
    p_orch = sub.add_parser("orchestrate", help="自动编排批处理流程（扫描知识库+生成 prompt）")
    p_orch.add_argument("--patients", required=True, help="patients.json 路径")
    p_orch.add_argument("--kb-root", default=None, help="知识库根路径（可选）")
    p_orch.add_argument("--output-dir", default="Output/batches", help="输出目录")
    p_orch.add_argument("--batch-size", type=int, default=5, help="每批患者数 (默认 5)")
    p_orch.add_argument("--max-prompt-tokens", type=int, default=80000,
                         help="单个 prompt 最大 token 数 (默认 80000)")
```

在 `args.command` 分发中添加：

```python
    elif args.command == "orchestrate":
        cmd_orchestrate(args)
```

同时更新文件顶部的 docstring，添加 orchestrate 描述。

- [ ] **Step 5: 运行测试确认通过**

Run: `python -m pytest tests/test_orchestrate.py -v`
Expected: 4 passed

- [ ] **Step 6: 运行全部测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 所有测试通过

- [ ] **Step 7: 提交**

```bash
git add tests/test_orchestrate.py scripts/batch_pipeline.py
git commit -m "feat: add cmd_orchestrate() — deterministic batch processing orchestration

Implements: resolve_kb_root, scan_knowledge_base, extract_patient_features,
generate_grep_commands, generate_batch_prompt.
D3: auto-split oversized batches. D5: orchestration_plan.json schema.
D7: checkpoint detection for resume support."
```

---

## Task 8: validate 增强 — 组织覆盖率 + 跨批次相似度检测

**Files:**
- Create: `tests/test_validate_enhance.py`
- Modify: `scripts/batch_pipeline.py:264-364` (增强 cmd_validate)

**决策引用:** D9 — 字符 bigram Jaccard 相似度，阈值 0.8

- [ ] **Step 1: 写失败测试**

```python
# tests/test_validate_enhance.py
import json
import pytest
from scripts.batch_pipeline import _bigram_jaccard, _check_cross_batch_similarity

def test_bigram_jaccard_identical():
    assert _bigram_jaccard("完全相同的文本", "完全相同的文本") == 1.0

def test_bigram_jaccard_different():
    score = _bigram_jaccard("胃癌一线治疗方案", "肺癌诊断标准指南")
    assert score < 0.5

def test_bigram_jaccard_empty():
    assert _bigram_jaccard("", "") == 0.0
    assert _bigram_jaccard("abc", "") == 0.0

def test_cross_batch_similar_warning():
    """不同患者的推荐高度相似时报 WARNING"""
    results = [
        {
            "patient_id": "P001", "batch_source": "batch_001",
            "clinical_questions": [{"guideline_results": [
                {"recommendation": "HER2阳性胃癌一线推荐曲妥珠单抗联合化疗，证据等级Category 1"}
            ]}],
        },
        {
            "patient_id": "P002", "batch_source": "batch_002",
            "clinical_questions": [{"guideline_results": [
                {"recommendation": "HER2阳性胃癌一线推荐曲妥珠单抗联合化疗，证据等级Category 1"}
            ]}],
        },
    ]
    warnings = _check_cross_batch_similarity(results, threshold=0.8)
    assert len(warnings) > 0
    assert "P001" in warnings[0] or "P002" in warnings[0]

def test_cross_batch_no_warning_different():
    """不同推荐不触发 WARNING"""
    results = [
        {
            "patient_id": "P001", "batch_source": "batch_001",
            "clinical_questions": [{"guideline_results": [
                {"recommendation": "HER2阳性胃癌一线推荐曲妥珠单抗联合化疗"}
            ]}],
        },
        {
            "patient_id": "P002", "batch_source": "batch_002",
            "clinical_questions": [{"guideline_results": [
                {"recommendation": "MSI-H胃癌推荐免疫检查点抑制剂单药治疗帕博利珠单抗"}
            ]}],
        },
    ]
    warnings = _check_cross_batch_similarity(results, threshold=0.8)
    assert len(warnings) == 0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_validate_enhance.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 _bigram_jaccard() 和 _check_cross_batch_similarity()**

在 `scripts/batch_pipeline.py` 的 validate 区块中添加：

```python
def _char_bigrams(text: str) -> set:
    """生成字符 bigram 集合"""
    text = text.strip()
    if len(text) < 2:
        return set()
    return {text[i:i+2] for i in range(len(text) - 1)}


def _bigram_jaccard(a: str, b: str) -> float:
    """D9: 字符 bigram Jaccard 相似度"""
    sa, sb = _char_bigrams(a), _char_bigrams(b)
    if not sa or not sb:
        return 0.0
    return len(sa & sb) / len(sa | sb)


def _check_cross_batch_similarity(
    results: list[dict],
    threshold: float = 0.8,
) -> list[str]:
    """检测跨批次内容雷同（D9: Jaccard > threshold 报 WARNING）"""
    warnings = []

    # 收集每个患者的推荐文本拼接
    patient_recs = []
    for r in results:
        pid = r.get("patient_id", "?")
        batch = r.get("batch_source", "?")
        text_parts = []
        for q in r.get("clinical_questions", []):
            for gr in q.get("guideline_results", []):
                text_parts.append(gr.get("recommendation", ""))
        full_text = " ".join(text_parts)
        if full_text.strip():
            patient_recs.append((pid, batch, full_text))

    # 跨批次两两比较
    for i in range(len(patient_recs)):
        for j in range(i + 1, len(patient_recs)):
            pid_i, batch_i, text_i = patient_recs[i]
            pid_j, batch_j, text_j = patient_recs[j]
            # 只比较不同批次的不同患者
            if batch_i == batch_j:
                continue
            score = _bigram_jaccard(text_i, text_j)
            if score > threshold:
                warnings.append(
                    f"[{pid_i}] 与 [{pid_j}] 推荐文本高度相似 "
                    f"(Jaccard={score:.2f}, 批次 {batch_i} vs {batch_j}) "
                    f"— 可能存在上下文污染"
                )

    return warnings
```

- [ ] **Step 4: 在 cmd_validate() 中集成相似度检测**

在 `cmd_validate()` 函数中，在"跨患者一致性"检测块之后（约 line 350），添加：

```python
    # 跨批次相似度检测 (D9)
    cross_warnings = _check_cross_batch_similarity(results)
    warnings.extend(cross_warnings)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `python -m pytest tests/test_validate_enhance.py -v`
Expected: 5 passed

- [ ] **Step 6: 运行全部测试确认无回归**

Run: `python -m pytest tests/ -v`
Expected: 所有测试通过

- [ ] **Step 7: 提交**

```bash
git add tests/test_validate_enhance.py scripts/batch_pipeline.py
git commit -m "feat: enhance validate with cross-batch similarity detection

D9: char bigram Jaccard (threshold 0.8) detects context pollution between batches"
```

---

## Task 9: SKILL.md 约束强化

**Files:**
- Modify: `SKILL.md:660-679` (Forbidden Actions)
- Modify: `SKILL.md:1201-1264` (Step 3 workflow)
- Modify: `SKILL.md:1252-1264` (HARD_CONSTRAINT)

**决策引用:** v2.2-fix-plan Phase 2

- [ ] **Step 1: 新增 HARD_CONSTRAINT — 执行模式约束**

在 `SKILL.md` line 1200（Step 3 之前）插入：

```markdown
<HARD_CONSTRAINT>

### Execution Mode Constraints

1. **禁止 subagent / 并行代理**: 所有批处理步骤在当前会话中顺序执行。
   禁止使用 Agent tool、Task tool 或任何并行机制。
   原因: subagent 无法继承知识库上下文，搜索质量无法保证一致性。

2. **禁止自行编码替代脚本**: 必须使用 scripts/batch_pipeline.py 的子命令。
   原因: 脚本已验证，自行编码引入格式差异和路径错误。

3. **所有输出路径以 orchestration_plan.json 为准**: 不自行决定文件存储位置。

4. **禁止跳过 grep 命令**: orchestrate 生成的命令必须全部执行。
   可以补充额外命令，但不得删减已有的。

</HARD_CONSTRAINT>
```

- [ ] **Step 2: 新增 HARD_CONSTRAINT — 全覆盖检索**

紧接着插入：

```markdown
<HARD_CONSTRAINT>

### Full-Coverage Retrieval Rules

1. **所有 org 必须检索**: 不得因"已找到足够信息"提前停止。
2. **所有患者临床特征必须纳入检索**: 不仅合并症，转移部位、分子分型、
   分期、治疗史、肿瘤急症等所有非空字段都应体现在检索中。
3. **不得以"类似"跳过**: 即使两位患者病情相似，也独立执行全部命令。
4. **检索深度一致**: 第 1 位和最后 1 位患者的检索深度必须相同。

</HARD_CONSTRAINT>
```

- [ ] **Step 3: 改写 Step 3 为 orchestrate 驱动**

替换 SKILL.md 中 `### Step 3: Determine Processing Mode` 到 `### Step 4: Merge and Validate` 之间的内容：

```markdown
### Step 3: Execute Retrieval via orchestrate

**所有批处理（无论患者数量）统一使用 orchestrate 驱动。**

```bash
python scripts/batch_pipeline.py orchestrate \
  --patients Output/patients.json --kb-root $KB_ROOT \
  --output-dir Output/batches --batch-size 5
```

读取生成的 `Output/batches/orchestration_plan.json`，报告给用户：
- 批次数量、待处理数、已完成数（checkpoint）
- 覆盖的指南组织
- grep 命令总数

**对 pending 批次逐一执行（严格顺序）：**

1. 读取 `batch_NNN_prompt.md`
   — 注意开头的 `<CONTEXT_RESET>` 指令，清除前批上下文
2. 按 prompt 中的 grep 命令逐条执行（不得跳过）
3. 可补充额外 grep（基于已有结果中的线索）
4. 提取推荐、证据等级、来源信息
5. 生成共识/差异分析
6. 写入 `rag_batch_NNN.json`
7. 报告进度: "已完成第 NNN 批（X/M 批），共 Y/N 位患者"

完成后执行 `orchestration_plan.json` 中 `next_steps` 的命令。
```

- [ ] **Step 4: 补充 §2.4 批次说明**

在 Step 3 的批处理流程中确保包含如下文字（对应 fix-plan §2.4）：

> "在开始处理新批次前，先读取该批次的 prompt 文件。prompt 开头的 CONTEXT_RESET 指令要求你从零开始——不引用前批的任何内容。"

- [ ] **Step 5: 扩展 Forbidden Actions**

在 `SKILL.md` 的 Forbidden Actions 列表（line 660-679 和 line 1316-1319）中追加：

```markdown
- **Never spawn subagents** for batch processing
- **Never write custom scripts** to replace batch_pipeline.py
- **Never process batches in parallel**
- **Never store output** outside orchestration_plan.json 指定的目录
- **Never skip any grep command** from the prompt
- **Never skip any org's** search results
- **Never ignore patient clinical features** in search
```

- [ ] **Step 6: 更新 SKILL.md 尾部日期**

```
*Last Updated: 2026-03-25*
```

- [ ] **Step 7: 提交**

```bash
git add SKILL.md
git commit -m "feat: strengthen SKILL.md with execution mode + full-coverage constraints

Phase 2: add 2 HARD_CONSTRAINTs, rewrite Step 3 to orchestrate-driven,
add batch CONTEXT_RESET instruction, expand Forbidden Actions with 7 new rules"
```

---

## Task 10: validate 增强 — 组织覆盖率 + 特征覆盖率

**Files:**
- Modify: `tests/test_validate_enhance.py` (追加测试)
- Modify: `scripts/batch_pipeline.py` (增强 cmd_validate)

**决策引用:** v2.2-fix-plan §1.8

- [ ] **Step 1: 写失败测试**

```python
# 追加到 tests/test_validate_enhance.py

def test_org_coverage_warning():
    """组织覆盖率不足时报 WARNING"""
    from scripts.batch_pipeline import _check_org_coverage
    results = [
        {
            "patient_id": "P001",
            "clinical_questions": [{"guideline_results": [
                {"guideline": "NCCN", "recommendation": "..."},
                {"guideline": "ESMO", "recommendation": "..."},
            ]}],
        },
    ]
    known_orgs = ["NCCN", "ESMO", "CSCO", "Japanese", "Korean", "CACA"]
    warnings = _check_org_coverage(results, known_orgs)
    assert len(warnings) > 0  # 只有 2/6 org

def test_org_coverage_pass():
    """全覆盖时无 WARNING"""
    from scripts.batch_pipeline import _check_org_coverage
    results = [
        {
            "patient_id": "P001",
            "clinical_questions": [{"guideline_results": [
                {"guideline": org, "recommendation": "..."} for org in ["NCCN", "ESMO", "CSCO"]
            ]}],
        },
    ]
    warnings = _check_org_coverage(results, ["NCCN", "ESMO", "CSCO"])
    assert len(warnings) == 0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_validate_enhance.py::test_org_coverage_warning -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 _check_org_coverage()**

```python
def _check_org_coverage(results: list[dict], known_orgs: list[str]) -> list[str]:
    """检查每位患者的指南组织覆盖率"""
    warnings = []
    for r in results:
        pid = r.get("patient_id", "?")
        covered_orgs = set()
        for q in r.get("clinical_questions", []):
            for gr in q.get("guideline_results", []):
                covered_orgs.add(gr.get("guideline", ""))
        missing = set(known_orgs) - covered_orgs
        if missing:
            coverage = len(covered_orgs) / len(known_orgs) * 100 if known_orgs else 0
            warnings.append(
                f"[{pid}] 组织覆盖率 {coverage:.0f}% ({len(covered_orgs)}/{len(known_orgs)}), "
                f"缺失: {', '.join(sorted(missing))}"
            )
    return warnings
```

在 `cmd_validate()` 中集成（在跨批次相似度检测之后）：

```python
    # 组织覆盖率检测 (§1.8)
    if args.kb_profile:
        # 从 orchestration_plan.json 读取 known_orgs
        plan_path = Path(args.kb_profile).resolve()
        if plan_path.exists():
            plan_data = json.loads(plan_path.read_text(encoding="utf-8"))
            known_orgs = plan_data.get("kb_profile", {}).get("orgs", [])
            if known_orgs:
                org_warnings = _check_org_coverage(results, known_orgs)
                warnings.extend(org_warnings)
```

同时在 validate CLI 参数中添加 `--kb-profile`：
```python
    p_validate.add_argument("--kb-profile", help="orchestration_plan.json 路径（可选，用于组织覆盖率检查）")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_validate_enhance.py -v`
Expected: 所有测试通过

- [ ] **Step 5: 提交**

```bash
git add tests/test_validate_enhance.py scripts/batch_pipeline.py
git commit -m "feat: add org coverage check to validate

Checks each patient's guideline_results against known orgs from orchestration_plan"
```

---

## Task 11: cmd_generate 增强 — --output-dir + 音序排列

**Files:**
- Create: `tests/test_generate_enhance.py`
- Modify: `scripts/batch_pipeline.py:871-932` (cmd_generate + CLI)

**决策引用:** v2.2-fix-plan §1.7

- [ ] **Step 1: 写失败测试**

```python
# tests/test_generate_enhance.py
import json
import pytest
from pathlib import Path

def _make_rag_results(tmp_path, names=None):
    """构造最小 rag_results.json"""
    names = names or [("P001", "张三"), ("P002", "李四"), ("P003", "王五")]
    results = []
    for pid, name in names:
        results.append({
            "patient_id": pid,
            "patient_name": name,
            "primary_site": "胃体",
            "disease_type": "胃癌",
            "diagnosis_summary": "测试诊断",
            "clinical_questions": [{
                "question": "测试问题",
                "guideline_results": [{
                    "guideline": "NCCN", "version": "2026",
                    "recommendation": "测试推荐", "evidence_level": "Category 1",
                    "source_file": "test.txt", "source_lines": "1-10",
                }],
                "consensus": ["共识1"],
                "differences": ["差异1"],
            }],
        })
    data = {"generated_at": "2026-03-25", "patient_count": len(results), "results": results}
    p = tmp_path / "rag_results.json"
    p.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return p

def test_generate_custom_output_dir(tmp_path):
    """--output-dir 指定自定义输出目录"""
    import argparse
    from scripts.batch_pipeline import cmd_generate
    rag_path = _make_rag_results(tmp_path)
    custom_dir = tmp_path / "custom_output"
    args = argparse.Namespace(
        input=str(rag_path), output_dir=str(custom_dir), format="xlsx",
    )
    cmd_generate(args)
    assert (custom_dir / "批量推荐汇总表.xlsx").exists()

def test_generate_patient_order_by_name(tmp_path):
    """患者按姓名拼音音序排列"""
    from scripts.batch_pipeline import _sort_results_by_name
    results = [
        {"patient_name": "张三"},
        {"patient_name": "李四"},
        {"patient_name": "王五"},
    ]
    sorted_results = _sort_results_by_name(results)
    # locale.strxfrm 排序：李 < 王 < 张 (按拼音 L < W < Z)
    names = [r["patient_name"] for r in sorted_results]
    assert names[0] == "李四"  # L
    assert names[1] == "王五"  # W
    assert names[2] == "张三"  # Z
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_generate_enhance.py -v`
Expected: FAIL — `ImportError`

- [ ] **Step 3: 实现 _sort_results_by_name() 和修改 cmd_generate()**

```python
import locale

def _sort_results_by_name(results: list[dict]) -> list[dict]:
    """按患者姓名排序（使用 locale.strxfrm，中文环境下近似拼音序）"""
    try:
        locale.setlocale(locale.LC_COLLATE, "zh_CN.UTF-8")
    except locale.Error:
        try:
            locale.setlocale(locale.LC_COLLATE, "")
        except locale.Error:
            pass
    return sorted(results, key=lambda r: locale.strxfrm(r.get("patient_name", "")))
```

在 `cmd_generate()` 中，在加载 data 之后、生成之前添加排序：

```python
    # 按姓名音序排列 (§1.7)
    if data.get("results"):
        data["results"] = _sort_results_by_name(data["results"])
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_generate_enhance.py -v`
Expected: 通过（注意：音序排序测试可能因系统 locale 不同而有差异，测试应容忍此情况）

- [ ] **Step 5: 提交**

```bash
git add tests/test_generate_enhance.py scripts/batch_pipeline.py
git commit -m "feat: enhance cmd_generate with name sorting

§1.7: sort results by patient name using locale.strxfrm for pinyin approximation"
```

---

## Task 12: 端到端验证

**Files:**
- 无新文件，使用现有脚本和测试知识库

**决策引用:** v2.2-fix-plan Phase 4

- [ ] **Step 1: 验证 orchestrate --help**

Run: `python scripts/batch_pipeline.py orchestrate --help`
Expected: 显示 --patients, --kb-root, --output-dir, --batch-size, --max-prompt-tokens 参数

- [ ] **Step 2: 使用真实知识库运行 orchestrate**

```bash
cd /Users/f.sh/Workspace/Claude/GC_RAG/skills/medical-guidelines-suite
python scripts/batch_pipeline.py orchestrate \
  --patients /Users/f.sh/Workspace/Claude/GC_RAG/Output/patients.json \
  --kb-root /Users/f.sh/Workspace/Claude/GC_RAG/guidelines \
  --output-dir /tmp/v22_test/batches \
  --batch-size 5
```

Expected:
- 输出知识库扫描结果（6 个组织）
- 生成 `orchestration_plan.json`
- 生成 `batch_*_prompt.md` 文件

- [ ] **Step 3: 验证 orchestration_plan.json 结构**

```bash
python -c "
import json
p = json.load(open('/tmp/v22_test/batches/orchestration_plan.json'))
print('version:', p['version'])
print('patients:', p['total_patients'])
print('batches:', len(p['batches']))
print('orgs:', p['stats']['orgs_covered'])
print('grep_cmds:', p['stats']['total_grep_commands'])
print('next_steps:', len(p['next_steps']))
"
```

- [ ] **Step 4: 验证 prompt 文件内容**

```bash
# 检查 CONTEXT_RESET 存在
head -15 /tmp/v22_test/batches/batch_001_prompt.md

# 检查所有 org 都有 grep 命令
grep -c "NCCN\|ESMO\|CSCO\|Japanese\|Korean\|CACA" /tmp/v22_test/batches/batch_001_prompt.md

# 统计 grep 命令数
grep -c "^[0-9]*\. grep" /tmp/v22_test/batches/batch_001_prompt.md
```

- [ ] **Step 5: 验证回归 — 原有子命令不受影响**

```bash
python scripts/batch_pipeline.py parse --help
python scripts/batch_pipeline.py split --help
python scripts/batch_pipeline.py merge --help
python scripts/batch_pipeline.py validate --help
python scripts/batch_pipeline.py generate --help
```

Expected: 所有子命令正常显示帮助

- [ ] **Step 6: 运行全部测试**

Run: `python -m pytest tests/ -v --tb=short`
Expected: 所有测试通过

- [ ] **Step 7: 清理测试目录 + 最终提交**

```bash
rm -rf /tmp/v22_test
git add -A
git commit -m "v2.2.0: orchestrate-driven deterministic batch processing

New: orchestrate subcommand (auto-scan KB, full-feature extraction,
pre-generated grep commands, self-contained batch prompts).
Enhanced: validate cross-batch similarity detection (D9).
Strengthened: SKILL.md execution mode + full-coverage constraints.

Ref: docs/v2.2-decisions.md (9 confirmed decisions)"
```

---

## 任务依赖关系

```
Task 1 (_split_patients)
  ↓
Task 2 (resolve_kb_root)
  ↓
Task 3 (scan_knowledge_base)
  ↓
Task 4 (escape_grep + generate_grep_commands)
  ↓
Task 5 (extract_patient_features)
  ↓
Task 6 (generate_batch_prompt)  →  依赖 Task 4, 5
  ↓
Task 7 (cmd_orchestrate)  →  集成 Task 1-6
  ↓
Task 8 (validate 相似度检测)  ─┐
Task 9 (SKILL.md 约束)        ├─ 三者互相独立，可并行
Task 10 (validate 覆盖率)     ─┘
  ↓
Task 11 (generate 增强)  →  独立
  ↓
Task 12 (端到端验证)  →  依赖全部
```

Task 1-6 按顺序开发。Task 7 集成后，Task 8/9/10/11 四者互相独立可并行。Task 12 最后执行。
